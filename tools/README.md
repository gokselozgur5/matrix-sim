# tools/ — process instruments

Scripts that execute the process the documents promise. Nothing here is part
of the daemon build (D-009 still holds: the SIMULATION needs only `javac`);
these are the shop's own jigs, kept under D-030's rule that crew tooling is
part of the shop.

**If this file conflicts, do not take a side.** Every row below is one line —
a markdown cell cannot hold a newline — so two units editing two *different*
rows still collide, and the conflict spans both. Taking one whole side leaves a
file that parses, renders and passes every check while silently reverting the
other unit's documentation. `.gitattributes` sets `merge=union` on this file for
that reason: the merge completes with every version of every affected row still
present, `advice.sh` breaks the build with `A_TOOL_WEARS_TWO_ROWS`, and the
repair is to delete the stale twin of each pair (#1370).

**Authority after D-063.** Tool output never admits or outranks work. `balance.sh` may break ties only among comparably valuable, independently justified ready units after human/world and dependency value, and every ordinary output shape carries an `AUTHORITY advisory=1 eligible_only=1 value_precedes=1 comparable_only=1 tie_breaker=1 creates_artifacts=0 splits_units=0 weakens_locks=0` line before any `DEFICIT` or `PLAN` can be read as an instruction. `attribution.sh` protects repository identity and provenance first; contribution visibility is a matching observation, not its licence to rewrite history.

| Tool | What it does |
|---|---|
| `orchestra.sh` | Composes the merge door without selecting work. `check PR ISSUE` requires the one observed closing reference to equal that OPEN `build-unit`, then verifies authenticated repository identity, same-repository exact head, current `main` base/ancestry, OPEN/non-draft/CLEAN/MERGEABLE state, `prstate` GREEN, `checkage` CURRENT, newest exact-head `litany`/`locks`, and—when configured—`orchestra/review=success`. `merge` repeats the full read, requires an exact manual-review SHA only while automation is disabled, and uses `gh pr merge --match-head-commit HEAD --rebase`; GitHub atomically locks the code head but exposes no compare-and-swap for mutable closing-issue metadata, so the receipt says `metadata_lock=unavailable` and the tool observes that binding immediately before and after instead of pretending it locked it. It then proves canonical-main reachability/tree identity and CLOSED/COMPLETED + `status:done` + Project #3 Done parity without silently repairing lag. Usage: `tools/orchestra.sh check <pr> <expected-issue>` · `tools/orchestra.sh merge <pr> <expected-issue>` · `--selftest` (fake transports; no network/merge) · `--help` / `-h`. Exit 0 PASS · 1 evidence FAILED · 2 the invocation was refused. |
| `orchestra-review.sh` | Implements the three-job Codex sensor boundary. `resolve` binds a successful `locks` head to one open same-repository `main` PR. In the no-secret prepare job, `prepare` verifies exact ancestry, renders at most 1 MB through `git diff --no-ext-diff --no-textconv --no-color`, hashes the hostile-text packet, and removes the PR checkout. The secret job downloads only that one packet, `verify-packet` checks its hash/head/base/size/single-file shape, and pinned Codex 0.153.4 receives it with `:read-only`, drop-sudo, no command-execution surface (`shell_tool=false`, no apply-patch/search/apps/plugins), and no PR checkout. `publish` validates bounded JSON plus `READY ⇔ no BLOCKING`, rereads head/base before every mutation, reconciles older bot-owned marker comments, and writes an exact-head status. `--policy-check` parses and pins the workflow, action/config/artifact commits, permissions, sole-secret/action-last placement and PR-checkout boundary; `--selftest` drives production transports and mutations, including a hostile diff driver that must not execute. Usage: workflow-only `resolve` · `prepare` · `verify-packet` · `publish`; public `--policy-check` · `--selftest` · `--help`. Exit 0 held · 1 evidence failed · 2 the invocation was refused. |
| `release.sh` | Cuts a phase release from `main` — refuses unless the locks are green at cut time, then stamps their live output into the notes under your prose. The locks are `locks.yml`'s 1 through 7, run in its order: a clean compile, `--selftest` at the standard budget and again over the full 6,000-tick arc, `--bench`, a clean probe compile, `probes/bench.sh`'s whole judged sweep, and the seal pinned in `.github/canonical-digest`. It ran the first, second and fourth of those and no others until #1052, which made the gate on the artifact that LEAVES the repository a strict subset of the gate on a merge into it: a tree whose seal had moved undeclared, or whose probe sweep was red, could be tagged and published while a pull request carrying the same bytes was being refused. The sweep is quoted in the notes by its summary line and the seal by the line the daemon printed, so the release notes carry the sha the release ships. `--check``--selftest` runs the same seven and prints the evidence block a cut would stamp, then stops before the tag: the locks used to be runnable only by cutting a release, so nobody ran them, and lock 2 spent 62 commits refusing a green `main` because it matched `--selftest`'s whole stdout against a line that had stopped being first (#972). The verdict is now found by an anchored grep over that stdout, so the next law to become an assertion adds a line without breaking the release. Wired into CI as the last step in the lane — it rebuilds `out/` and `probes/out` from nothing, so nothing may follow it — which runs `--check` and judges its `RELEASE CHECK … VERDICT PASS` line, so the release path is exercised by every pull request instead of by the next release. Usage: `--help` | `-h` prints the header's Usage clause and stops · `tools/release.sh vX.Y.Z "Title" notes.md` with notes written from `RELEASE_NOTES_TEMPLATE.md` · `tools/release.sh --check` (no tag, no push, no token). **Which locks a release skips, and why:** the ones that judge a PULL REQUEST rather than a tree — lock 0's evidence baseline, lock 9's declared digest move, `attribution.sh` — because a tag has no body, no base branch and no author to read, and by cut time those questions were answered at the merge; the lanes that judge the shop's own jigs rather than the daemon being shipped — `balance.sh --datecheck` and `--rulercheck`, the teleprinter's exit grammar — one of which also judges an account and not this repository; and three tree locks CI keeps that the cut does not — the charset pin (lock 8), the neutral control group (lock 10), and the teleprinter's golden day (lock 11). Those last three are cheap and they do judge this tree; they are outside the cut gate only because #1052 drew the floor at the daemon's own locks, and that is a difference stated here rather than left to be inferred from a grep. Exit 0 the locks are green and the cut may proceed · 1 a lock is red · 2 the invocation was refused — **which an unknown FLAG did not spend until #1546**: the first argument is a version, so `--nonsense` was refused as `FATAL version must be vX.Y.Z`, a refusal wearing the wrong code and a message about the wrong thing, while `--check` one line above had spent 2 for an extra argument all along. |
| `subissue.sh` | Cuts a child issue and hangs it on its parent in one motion — inherits the parent's milestone, refuses a closed parent (a tree does not grow from a closed branch). **Three of its refusals spent the wrong code until #1309** — `${1:?usage}` leaves with **1**, this tree's code for *the claim does not hold*, while two hand-written refusals in the same file used **2** correctly, so the tool disagreed with itself about what a refusal costs. Neither of `advice.sh`'s exit checks could see it: `codes_undocumented` reads LITERAL exits and bash's is not one, and `codes_unspent` asks whether a promised code is spent — 2 was, by the other two — so the hidden 1 was invisible from both directions. Usage: `--help` | `-h` prints the header's Usage clause and stops · `tools/subissue.sh <parent> "<title>" body.md [--label L] [--milestone M]` · `--selftest` runs the DOOR's cases: cutting an issue needs a token, refusing does not, and the token half stays out because a fixture that fakes `gh` tests the fake (#1273). Exit 0 the child was cut and hung on its parent · 1 a `--selftest` case failed — the door itself is broken, not the invocation · 2 the invocation was refused — no arguments, a parent that is not a number, a missing body file, an unknown flag, or a parent that is closed. |
| `backlog.sh` | Sorts the open backlog by EVIDENCE rather than by age: how many `build-unit` issues carry a measurement, and how many are a shape somebody recognised. #1246's argument is the day's own record — of the four issues filed from a run, none needed a correction; of the three filed from a shape, one was closed as already-checked, one had its central table wrong, and one had its timing claim withdrawn. A reader picking work up cannot tell those apart, and guessing wrong costs a unit. **It found its own commissioning defect:** #1246's headline measurement was `30` open and `20` unmeasured, taken with `gh issue list`'s undeclared 30-row default; the true figures are 508 and 425, so an issue about the danger of unmeasured claims was itself off by seventeen times. This tool pages explicitly and prints `limit=` for that reason. The classifier is generous on purpose — the word `measured` anywhere in the body counts — so `unmeasured=` is a FLOOR on the problem and never an exaggeration of it; a tool that argues its own case by rounding up is worth less than no tool. Reported, never judged: an issue is not an artefact CI can judge, and a lint demanding prose in a field is how a required field becomes `n/a` (#1246). **Both ends of the paging axis are refusals** (#1273): `NOTHING_READ` when the answer is empty — an empty answer is not an empty backlog (#1235) — and `TRUNCATED` when the count exactly fills the page, because the tool cannot tell a full page from a complete answer and guessing would be the defect it was written to name. The suite drives both over synthetic counts rather than over the API, since faking `gh` would be a bigger apparatus than the branch is worth and a fixture that fakes the transport tests the fake. **`--flow DATE` counts the day rather than the standing population** (#1323): a day that closed thirty-three and opened thirty-three prints the same `open=` as a day that closed none, and the ratio is what says whether a day drained the backlog or explored the tree — both correct at different times, since a survey unit SHOULD open more than it closes. There is no right ratio, only a visible one. Usage: `--help` | `-h` prints the header's Usage clause and stops · `tools/backlog.sh` · `--list` prints one row per unmeasured issue · `--flow YYYY-MM-DD` · `--selftest` runs the classifier's, the pager's, the flow door's and the template marker's cases (no token, no network). Exit 0 counted · 1 an issue TEMPLATE has no evidence field the query can reach — the one thing here that is an artefact rather than a backlog (#1251) · 2 the invocation was refused · 3 the backlog could not be read · 4 nothing was read at all, which is not an empty backlog · 5 the answer filled the page, so it is a page and not a backlog · 6 search hit its own 1,000-result cap, which raising the limit cannot lift. **Two stores, both named since #1352.** The census reads the SEARCH api and `--flow` reads the ISSUES api, and those are not one store: search is an index with its own refresh lag, so an issue filed moments before a run can be absent from one and present in the other. For a census of 525 that is noise; for a day's opened/closed ratio it is a percentage point of a small number. `store=search cap=1000` rides the census and `store=issues` rides the flow line, which buys the NEXT request rather than fixing a defect — `--flow` does not use search today, and the moment somebody narrows it to the census's three kinds they will reach for `--search` and the day's count becomes index-dependent without anyone deciding it should. **The cap is not the page**, and until #1352 they wore one number: search stops at 1,000 RESULTS however high `--limit` goes, `LIMIT` is also 1000, and at exactly 1,000 rows the tool said TRUNCATED — whose instruction is *raise the limit*, which for a capped api is advice that cannot work, and #1273's argument was precisely that a tool must not report one fact as another. `capped` is now its own predicate with its own refusal, tested FIRST for the reason the verdict chain orders its words: when two facts fire together the reader is sent to the one they can act on. With `LIMIT` at the cap the TRUNCATED branch is currently unreachable and that is stated rather than left to be found — today every full page IS the cap, and lowering `LIMIT` makes the page reachable again with its meaning intact, which is the only configuration where the two predicates disagree and the one case pair that proves they do. What is NOT done is the third option, one `--label` call per kind with the dedup in `jq`: it is the only one that removes the split and it costs exactly what #1251 chose search for — the OR and the dedup, three api calls instead of one, and the dedup becoming this tool's problem rather than GitHub's. |
| `issuetree.sh` | Prints an issue's tree root to leaves: parents carry their child count, leaves carry a dot. An answer it could not READ is a `?` row and never a leaf — a rate limit, an expired token and a genuinely childless issue used to produce the same dot, so a parent could be drawn as a leaf because the API was busy (#1235). The walk finishes and the trailer states the denominator: `ISSUETREE VERDICT COMPLETE|PARTIAL root=N unreadable=N`, exit 3 when anything was unreadable — this repository's code for *the answer could not be read*, spent the same way by `checkage.sh` and `prstate.sh`. A 404 is an empty answer rather than a failure, since that is what GitHub returns for an issue with no sub-issues. **Both refusals were wrong until #1276.** A missing argument left through bash's own `${1:?usage}`, which exits **1** — this tree's code for *the claim does not hold* — so a typo was reported the way a broken contract is, and this row wrote that down as though it were the rule. And a NON-NUMERIC argument was not refused at all: `issuetree.sh not-a-number` walked it as an issue number and printed `? #not-a-number <unreadable>` with exit 3, so a typo and a rate-limited API produced the same verdict — #1235's own defect (an unreadable node reported as an empty one) arriving through the argument door of the tool that fixed it. Both are exit 2 now, which is what `subissue.sh` has always done with a non-numeric parent. Usage: `tools/issuetree.sh <issue> [max-depth]` · `--help` | `-h` prints the header's Usage clause and stops, the one flag-shaped argument this tool does not refuse (#1529) · `--selftest` runs the DOOR's six cases — argument handling only, no token and no network, because a fixture that faked `gh` would test the fake (#1307). Exit 0 the whole tree was read · 1 a `--selftest` case failed · 2 the invocation was refused — no argument, or one that is not a number · 3 at least one node was unreadable, so the tree is PARTIAL. |
| `digest-move.sh` | Asks the question lock 7 cannot: the seal's pin moved — was that a decision? Silent (`VERDICT NONE`) for every unit that leaves the world's bytes alone; when the pin's sha differs from the base's it demands a commit carrying `Declared digest move: <old> -> <new>` with both shas in full and an issue number, and a reason field that names the issue. Makes the chain of heads a query: `git log --format=%B \| grep '^Declared digest move:'`. Usage: `tools/digest-move.sh [--base <ref>]` · `--age` answers *how long has this world stood* as a READ — the seal's age, reported rather than judged · `--help` | `-h` prints the header's Usage clause · `--selftest` runs every path this gate has against fixtures built out of THIS history. Wired into CI as lock 9. Exit 0 ARGUED or NONE · 1 UNARGUED, the pin moved and no commit says why · 2 the invocation was refused. |
| `attribution.sh` | Asks the only question the contribution graph asks: does GitHub resolve this commit's author to the account that owns the repository? Answers per commit with a denominator, and prints the `git config` that fixes a FAIL. **The suite ran its repairs through an error it could not see (#1362).** Both fixtures invoke `git rebase -x … >/dev/null 2>&1 || true` — stdout, stderr and the exit code discarded — so a rebase that never ran was indistinguishable from one that ran and worked, and the two cases reading the result reported EACH OTHER'S ANSWER: the repair looked destructive and the defect it demonstrates looked fixed. That is #1235's finding inside an instrument rather than a tool. The rebase's exit code is a case of its own now, its stderr is printed, and a failed rebase is aborted so no later case runs against a half-applied one. The fixture clone also sets `user.email`, which it had never set while setting `user.name`: the repairs run `git commit --amend`, an amend needs a COMMITTER address as well as the author one the repair sets, and a developer's box supplies one globally where a runner does not. Whether that was the cause of the eighth run failing after seven passes is NOT established — the change removes the dependency rather than proving it was the culprit, and the second change is what makes the next occurrence readable. Usage: `tools/attribution.sh` (HEAD) · `--help` | `-h` prints the header's Usage clause and stops · `--pr N` · `--sha SHA`, any of them with `--for OWNER/NAME` to name the repository instead of reading it off `origin` · `--fix-cmd` prints the repair for this branch and nothing else, for a script to consume · `--selftest` EXECUTES that repair against fixtures, which is the only way this tool's central claim can be falsified rather than asserted. Wired into CI on every pull request. Exit 0 every commit resolves to the owner · 1 one or more do not · 2 the invocation was refused · 3 the repository has never seen the sha · 4 nothing was read at all, which is not a pass · 5 the owner's address could not be determined. |
| `balance.sh` | Reads a day's contribution mix from the same API the profile graph uses and judges it against D-060's four quarters — names the lagging leg and what it would take to clear it. Under the artifact ruler that is two different answers and the line says which is which (#952): `DEFICIT` carries the four per-leg numbers, each solved with the other three standing still, and `PLAN` carries the set that composes, because every artifact added enlarges the day the other legs were measured against and work on a lagging leg can push a leg the DEFICIT line printed `0` for under the floor. Speaks both `date(1)` dialects, so it runs on the operator's macOS box and on `ubuntu-latest`. The API roots at `viewer`, so every run opens with a `SUBJECT` line naming the login it read and exits 6 unless that login owns the repository being judged — a token for the wrong account used to print a confident `verdict=EMPTY`. `contributionsCollection` is an *account* statistic, so the default verdict answers "was the account balanced" and not "was matrix-sim balanced" — every line carries `scope=`, `--repo` asks the narrower question, and a `SCOPE` line prints both readings and their delta so a green verdict can never quietly mean somewhere else. That line also carries `merge=`, the repository's own enabled merge strategies, because a settings page can drift from D-061's atomic, linear history hardline — the warning is repository-policy evidence with no authority over the advisory balance verdict, and a run whose token cannot read those settings says `merge=unreadable` rather than guessing. The default stays `account` on the record, not in the tool (D-060 errata, #821). Usage: `tools/balance.sh [--account\|--repo] [--for OWNER/NAME] [--week\|--month\|--days N] [--events] [YYYY-MM-DD]`; the window flags judge a rolling span ending on the named day and print each of its days judged alone, and `--events` restores the pre-#828 event ruler for reproducing an earlier reading; `tools/balance.sh --datecheck` judges the day arithmetic alone, `tools/balance.sh --rulercheck` the ruler alone, and `tools/balance.sh --judgecheck` judges the advice plus its ordinary-output integration — it does what the DEFICIT line says over pinned day vectors, asserts the PLAN set leaves no leg thin, then drives the real normal and EMPTY script paths through a local fixture transport to prove BALANCE+AUTHORITY stay joined and precede any DEFICIT or PLAN; all three suites run with no token and no network, and all three are wired into CI. `tools/balance.sh --help` | `-h` prints the header's Usage clause and stops. Exit 0 a complete advisory reading was printed — OK, LAGGING or EMPTY · 2 the invocation was refused · 3 the API would not answer · 4 a window and its days disagree, so both readings are refused · 5 one of the three suites failed · 6 the token belongs to another account. |
| `checkage.sh` | Dates a green check against the litany it claims to have passed (#1017). `gh pr checks` reports the run attached to the current head, and a head that has not moved keeps its run forever — including after `.github/workflows/locks.yml` has been rewritten underneath it, which is how PR #889 displayed `locks pass` from a run that had met six of today's locks never. One rule: a check is current only if its run STARTED AFTER the base's last commit to the litany file, strictly after, with both timestamps on the verdict line so the answer is quotable. Both stamps arrive from the API in one RFC 3339 shape and are compared as strings once that shape is checked — no `date -d`, so no #901 dialect to be green on one box and dead on the other — and a stamp in an unknown shape is refused rather than guessed at. Exit 0 CURRENT · 1 STALE · 2 the invocation was refused · 3 the answer could not be read · 4 ABSENT, no run on this head at all (#1004's shape; an absent run is not a passing one) · 5 NOTGREEN, which merge rule one refuses before the age question arises. Usage: `tools/checkage.sh --pr N` · `--sha SHA` · either with `--base REF` / `--for OWNER/NAME` · `tools/checkage.sh --help` | `-h` prints the header's Usage clause · `tools/checkage.sh --selftest` runs the ordering rule and the row picker with no token and no network. The live reading is the merge motion's, because a run cannot date itself; CI runs the suite. |
| `prstate.sh` | Names the state `gh pr checks` cannot say: a pull request is not only red, green or pending — it can be **UNBUILT**. GitHub builds a `pull_request` run from the head/base merge ref, so a branch that conflicts with `main` gets no run at all, and the sentence a crew reads (`no checks reported on the '<branch>' branch`) is the same one it prints for a run that has not started (#1004). One verdict line per pull request with its denominator (`runs=N green=… red=… pending=…`), a row per workflow run, and the rebase that fixes an UNBUILT. A stale green under a `CONFLICTING` pull request is still UNBUILT: that run was built from a merge ref that no longer exists. And a pull request based on a branch **no workflow triggers for** is **NOT_ELIGIBLE** rather than UNBUILT — it was never going to be built, which is a different thing from not built yet, and it is how every stacked unit PR sits while `branches: [main]` is the only trigger (#1210). The eligible bases are read out of `.github/workflows/*.yml` rather than listed here, so the verdict cannot drift from what CI actually does. This is deliberately NOT a step in `locks.yml` — the run that would carry the step is the thing that does not exist — so only its judge runs in the lane. It also answers the one question a cron can hide: `--schedules` reads every `on: schedule:` in the workflows, derives the period from the cron rather than from a number typed beside it, and reports **NEVER_RAN** apart from **OVERDUE** — a schedule nobody has ever seen work is a different fact from one that slipped, and `determinism.yml` was in the first state on the day it landed, carrying the only pass in the tree that byte-compares a probe against itself (#1233). Tolerance is 1.5× the period, bracketed on both sides by cases. Usage: `tools/prstate.sh N`, or `--pr N` for the same judgement in the spelling every other tool here uses · `tools/prstate.sh` (sweeps every open PR) · `--for OWNER/NAME` · `--schedules` · `--sweepcost [N]` answers the question #1348 said comes before any per-row floor — *has the sweep ever been near 300?* — by reading each of the last N successful `locks` runs' probe-sweep STEP off the jobs API and reporting `min/median/mean/max` against `BENCH_BUDGET_SECS` with the `headroom=` between the worst observed sweep and the ceiling. The step is a PROXY for the sweep's own `secs=`, which lives inside its stdout and would cost a log archive per run, and the line says `source=step` rather than leaving that to be assumed. Reported and never judged: wall clock on a shared runner is not stable, and this mode exists to measure how unstable BEFORE anyone writes a floor against it (#1221). The arithmetic is a pure function of a list of seconds, so the suite drives it with no token and no network — the even-count median is the LOWER one deliberately, because a mean of the middle two invents a duration nothing measured, and `headroom=` goes negative rather than clamping, because *how far past* is the number somebody raising the budget needs · `--reverts [REF]` asks the question #1118 distilled from four instances in one night: does this branch's version of a file it TOUCHES lack a line `REF` added after the merge base. Three of those four were caught by a person and one — fifteen files, 482 deletions — by nobody, and the single one a machine found was found because a pin and a reality were compared mechanically. It is deliberately NOT a staleness alarm: a commit-count heuristic cries wolf on an honest old branch that touches nothing contested, while a branch that does not touch the file is not asked about it, because the merge brings `REF`'s version and there is nothing to revert. Reported, never judged — a line `REF` added and this branch legitimately DELETED is indistinguishable from a revert without knowing intent, so the line names the file and the count and a person decides. The arithmetic is a pure function of two texts and has seven cases; the git plumbing has none, which is the same split `--sweepcost` makes. It uses `comm` and not `grep`: two grep spellings printed `grep: out of memory` on `probes/README.md`, where a catalog row is nine thousand characters on one line (#1370), because a regex engine handed that as a fixed pattern gives up. **`--revertsweep [STATE]` measures the population (#1614)**, and it works on MERGED pull requests because GitHub keeps `refs/pull/N/head` after the branch is deleted — a day's landed work can be re-asked the question nobody asked it. Two readings were wrong before this one and the sweep is what found both. The first compared every merged branch against TODAY's `main` and reported **3,932** reverted lines over thirty pull requests: every line every LATER unit added, which no branch could have carried — the target must be `main` as it was when the request was answered, which for a merged one is its merge commit's first parent. The second asked whether the BRANCH's copy carries the target's lines and reported **58**, of which every one checked was false: git's three-way merge keeps the target's lines wherever the branch did not touch them, so a branch that merely SHARES a file with a newer commit looked like it was reverting it. `git merge-tree --write-tree` performs the merge without a working tree, so the question becomes the only one that matters — after merging, is a line the target added MISSING — and the answer over the last thirty merged pull requests is **zero**. · `--help` | `-h` prints the header's Usage clause · `--selftest` (no token, no network; the count is the lane's floor, not this row's). Exit 0 green · 1 red · 2 the invocation was refused · 3 the repository could not be read · 4 UNBUILT · 5 pending · 6 NOT_ELIGIBLE. |
| `provenance.sh` | For every rule this repository enforces: who supplies it, who reads it, and what makes the answer mandatory? (#1769) | **Fifty rules, fifty of them supplied by the branch they judge** — and the fiftieth is this tool's own lane step, which is the self-reference stated rather than hidden: adding the step that runs this suite moved the census 49 to 50, so writing the reader changed the number it reports. A later reading that finds 49 has not found a regression. Measured server-side rather than inferred: `main` answers `404 Branch not protected`, `rulesets` answers `[]`, and no `CODEOWNERS` is tracked — so both lanes run on `pull_request`, both lane definitions are files a pull request can edit, and nothing outside the branch requires either result. A green check is the judged branch's own statement about itself. It is not hypothetical: #1738 records the census fence gaining an exclusion for `ci/` *"on no authority but the repairing unit's own"* — a unit widened the rule that judges it while repairing something else, and the widening read exactly like the repair; #1746 is that shape arriving a second time. **`UNKNOWN` is a first-class outcome and the reason the field exists.** Server-side state cannot be read from a working tree, and the inference runs wrong in both directions: seeing `on: pull_request` proves a lane RUNS and never that anything REQUIRES it, while a missing local file proves nothing about branch protection. So an API that cannot be reached yields `UNKNOWN:api-unreachable` and the census counts it — `--offline` reports `unknown=49`, and that is the same tree the online run calls `self_supplied=50`. The one API failure that IS evidence is the 404 on branch protection, because that endpoint answers 404 precisely when the branch is unprotected; `protection-404-verified` and `unreachable-is-unknown` are the two cases that keep those apart. **The events are read from the `on:` block and not from the file**, which is forced rather than tidy: every workflow here opens with a long essay naming its triggers in prose — the litany explains why it is a second workflow, the determinism lane explains why it is not a step in locks — so a grep over the file answers from the commentary; `events-ignore-prose` is the case. A rule is a NAMED STEP at the lane's indentation, because that is what fails, what the summary names and what a reader goes looking for; a `name:` on the workflow or on a job is not one, and counting those would inflate the population with things nothing can fail (`steps-named-only`). The three server-side facts are rows in their own right rather than a footnote, since they are precisely what a later unit would change. **`NOTHING_READ` was unreachable when it was written** and the suite is what found it: the check asked `rules -eq 0`, which the three server-side rows make impossible, so the refusal could never fire — the unreachable-guard shape #1741 counted, authored here by accident. It asks `lane_rules` now, and `empty-is-a-finding` pins it. **Counted, never judged**, and that boundary is the unit: #1311's rule is that a gate installs at zero and nobody knew the number, and #1749 held the same line when it counted verdict words and called none of them dead. Installing branch protection, writing `CODEOWNERS`, or gating on `self_supplied=` are three later arguments and none of them is this one. Usage: `tools/provenance.sh` counts · `--list` prints one row per rule with all six fields · `--offline` refuses to reach the API so every server-side row stays `UNKNOWN` · `--workflows DIR` points the reading at a fixture tree · `--selftest` drives the readers over fixtures with no network and no token · `--help` | `-h` prints the header's Usage clause and stops. `PROVENANCE_CENSUS` carries `workflows=`, `rules=`, `repo=` and `online=` (#1221); the verdict carries `rules=`, `self_supplied=`, `required=` and `unknown=`, and the three must exhaust `rules=` — a fourth outcome added to the loop breaks that line rather than quietly making `self_supplied=` mean less. Exit 0 counted · 1 a `--selftest` case failed · 2 the invocation was refused · 4 nothing was read at all, which is not an empty repository. |
| `advice.sh` | A tool that tells you what to type owes you a command that works (#1095). Three tools in one day printed advice that was wrong in a way only EXECUTING it revealed: `--reset-author`, which destroys the author dates `attribution.sh` exists to protect (#1012); a `release.sh` grep matching a line that was no longer first (#972); a deficit naming a leg the arithmetic could not move (#952). This audits the half that is decidable from text — a printed flag exists in the tool it names — and counts the half that is not: `unfalsifiable=` counts tools with no `--selftest` for their advice to be executed in. It was REPORTED and is JUDGED, since #1311; the clause below carries the date and the reason, and this sentence used to carry the older half of that pair as though it were still the rule. **Which tools are even ASKED for a suite is a promise, not a string match** (#1347): the gate was a whole-file grep, so `release.sh` was asked because lock 2 of a release runs `java -cp out matrix.Main --selftest` — the DAEMON's flag, quoted in the code that runs it — then refused the flag through its own positional check and was counted as a tool whose `--selftest` reaches no suite. Permanently: `no_suite=1` read 1 for as long as the counter existed and meant nothing. #1212 repaired the other half of the same mistake, where that grep counted `advice.sh` and `release.sh` as HAVING suites they do not have, by moving the discriminator to the verdict line — which fixed the false GREEN and left the false ENTRY untouched. The rule is one function over: `named_tool()` already carries a list of external programs so advice about somebody else's flags is not read as advice about this tool, and it is the same list here. `no_suite=` is 0 now, and the falsification is that all twelve tools were run: two are skipped, both have no `--selftest` of their own, and all nine suites still enter. What it will not do is run the advice, because `git commit --amend` inside an audit would be a tool damaging the tree to check whether it damages the tree. It also asks, of every tool it runs, whether `--selftest` reaches a **suite** and whether any workflow **executes** it: `suites=` · `no_suite=` · `unrun=`, and a suite the lane never runs is `A_SUITE_NOBODY_RUNS` (#1212). The discriminator is the verdict line rather than the flag appearing in the file — `advice.sh` and `release.sh` were both counted as having suites they do not have, because the string `--selftest` appears in every tool this one runs. Refuses an unknown argument with exit 2, which is the same finding pointed at itself: `tools/advice.sh --selftest` used to run the ordinary audit and print a green line from a suite that does not exist. **Both flag directions are checked, and they read the row differently on purpose** (#1263). `flags_undocumented=` reads the WHOLE row, because a row legitimately names other programs' flags — `git commit --amend`, `balance.sh --rulercheck` inside release.sh's — so a whole-row read cannot false-positive there. `flags_phantom=` reads only the `Usage:` clause, bounded at the first `**` or ` Exit N`, which is what made the phantom direction checkable at all: three earlier extraction rules each traded one error for the other, because `Usage:` is prose inside a paragraph inside a table cell sharing its sentence run with the exit grammar and a bold aside. **A tool may open exactly one row, and until #1340 nothing said so.** `balance.sh` had TWO, one stale by two units — and the row lookup is `grep "^| ``"`, which returns both joined by a newline, so every check here read the concatenation as one row: a flag documented in either satisfied the audit for the other, an exit clause in either answered for both, and `flags_phantom`'s bounded read stopped at the FIRST row's `Exit` so the second row's Usage clause was never checked at all. Nothing counted it — `uncatalogued=` counts tools with NO row and nothing counted more than one, so the discrepancy showed only as `catalog_rows=13` beside `tools=12`, two numbers on two lines nobody had compared. `A_TOOL_WEARS_TWO_ROWS` is its own word because sending a reader to `A_TOOL_NOBODY_CAN_FIND` would send them looking for an absence. **The verdict chain is a function with its own cases since #1358.** Twelve counters increment `BREAKS` and the chain named nine plus a terminal `else`; `flags_phantom` and `charset_drift` were in neither, so a break from either printed `A_CATALOG_ROW_PROMISES_WHAT_THE_TOOL_LACKS catalog_wrong=0` — a defect report naming the wrong defect, which is the #1170 shape this row cites elsewhere. The phantom case was the more dangerous of the two: it usually co-occurs with `catalog_wrong`, so the count was nonzero and the wrong word read as plausible. The drift case co-occurs with nothing and printed a zero. Neither had ever been executed by anything but a real defect, and one of them is unreachable today (`charset_checked=0`), which is where a wrong branch hides longest. The chain reads only integers, so `--selftest` now drives every branch over synthetic counts — plus the branch that must never print, `A_BREAK_WITH_NO_WORD`, for a counter that moved with no word, and a case comparing `BREAK_COUNTERS` against every `BREAKS + 1` site in the file so a thirteenth counter cannot be added silently. **The exit-code join has two directions and both are checked** (#1238): `codes_unspent` catches a row promising a code its tool never spends, and `codes_unnamed` catches the mirror — a tool spending a code its row omits, which is the sharper of the two because the row LOOKS complete and the reader stops looking. #1222 asked for both and shipped neither: it landed a floor asking only whether the string `Exit ` appears anywhere in the row, so `Exit codes vary.` passed, and the missing half survived as prose on a merged pull request until #1238 read it back. The join found two the day it was written, both the same shape — a `--selftest` leaving 1 on a failed case, in a row documenting only the door. Only literal `exit N` is read: a `return N` is a predicate.'.s answer to its caller unless the function is the script.'.s last statement, telling those apart means running the script in your head, and counting it inflated the spent set — which HID unspent findings rather than inventing them, so the defect was silent in both directions. `CODES_CENSUS with_returns=` reports the shape it cannot judge. Usage: `--help` | `-h` prints the header's Usage clause and stops · `tools/advice.sh` · `--list` · `--selftest` runs the flag audit against scratch tools and scratch catalog rows built in a temp directory (no token, no network) — the tool spent its whole life asking whether OTHER programs could execute their own advice while having no way to be watched failing itself (#1265). **The falsifiability count is JUDGED since #1311**, at the one moment it cost nothing: the reason it was a report — *four tools have no selftest today* — expired when #1307 and #1309 gave the last two their suites. The gate demands that every tool be falsifiable SOMEWHERE, not that every path is covered; `issuetree.sh` and `subissue.sh` both cover their door and not their till, and both satisfy it. Depth is guarded by each lane step's floor; this guards existence. **It EXECUTES one flag, and exactly one** (#1410): `--help`. Thirteen tools and `probes/bench.sh` learned that door across six units, every one of them verified by a transcript pasted into a pull request — and a paste is not a lock. Neither existing direction could see the gap: `flags_undocumented` reads arms against the row, `flags_phantom` reads the row against the arms, and a tool with NO door has nothing on either side to disagree, so both are silent and both are right. What makes it checkable is that the answer has a known correct value — the block reader over the tool's own header — so `doors_ok=`/`doorless=`/`doors_wrong=` compare the two. Running a tool is a departure this row argues against everywhere else, and the exception is narrow by construction: a `--help` door prints a comment block and exits before any parser, any network call and any write, so `release.sh --help` cannot cut a tag. `doors_wrong=` is separate from `doorless=` because the door that opens onto the wrong text is the quieter failure — `checkage.sh` printed one line too many for its whole life (#1520) and nobody saw it, since nobody diffs `--help` against the header — and a door that prints the clause and then exits nonzero counts as DOORLESS, because a caller branching on `$?` never reads the text. **It also asks whether a REFUSAL is a 2** (#1552), and that is a fourth reading of the exit grammar rather than a fifth flag check: `codes_unnamed`, `codes_unspent` and `codes_redefined` all read the ROW against the SOURCE, and none of them can see what a tool DOES when refused — which is why #1546 needed a hand sweep to find `litany.sh` and `release.sh` leaving 1 for an unknown flag while both rows documented `Exit … 1 …` truthfully, for the other thing 1 means in those tools. Every text-against-text check was satisfied. `probes/DoorRefusal` has asked this of `probes/` since #1481; this is its counterpart, and it could not have arrived before #1527 gave every tool a door. `refusals_ok=`/`refusals_wrong=`/`refusals_hung=`, with two verdict words because the failures send a reader to different files: a wrong code is an exit line, no refusal at all is the parser. **A tool that does not refuse does not print an error, it RUNS** — the hazard that hung this file's first door audit on `backlog.sh` — and there is no textual pre-test for *has a refusal* the way there is for *has a `--help)` arm*, so every invocation is watched and a tool outliving the watchdog is a FINDING and not a skipped row. The watchdog is a background sleep and a kill of the process GROUP, with no `timeout(1)`: that is GNU coreutils and absent from a stock macOS (#901), and killing only the wrapper leaves a grandchild that this script then waits for at exit — the suite's own hang fixture proved it, with a one-second watchdog and a thirty-second suite. **And it checks its own licence to run them** (#1555). Two of its checks EXECUTE every tool, on #1410's argument that a `--help` door is a pure function of the file — true of every door the day it was written, and never a property the audit ENFORCED. #1552 could not make that argument at all: a refusal happens after whatever the parser does first, and `subissue.sh` had its flags read AFTER two `gh` calls until #1309 moved them. The watchdog bounds DURATION, not effect — a request that has left cannot be unsent — and this row's own rule elsewhere is stricter, refusing to run a tool's advice because `git commit --amend` inside an audit is a tool damaging the tree to check whether it damages the tree. So the argument became a check: in the tool's DOOR scope, is anything reached above the arm the audits invoke. **The list is split and only half of it breaks the build.** `doors_network=` is the gate — `gh`, `curl`, `wget`, unbounded effect, the set the argument is actually about — and `doors_shell=` is a report, because another program above the door is how a network call ARRIVES later and is not a defect today. The first draft used one list and broke on three tools, none a hazard: `baseline.sh` reads `git rev-parse --show-toplevel`, and `litany.sh` and `subissue.sh` REGISTER a trap whose body runs at exit, after the door has printed and left, removing a temp directory the tool itself made. Breaking on those teaches the next author to spell around the checker, which is what #1207 says a bound must not do — so trap registrations are excluded from both counters and a read is reported rather than refused. **`DUPLICATE_ROW` says what DIFFERS since #1561**, because saying a row is doubled is not enough to repair it: the fix is to delete the STALE twin, and two rows of six thousand characters differ somewhere in the middle. Choosing wrong is SILENT — the build goes green the moment there is one row, whichever survives — which is the failure `merge=union` was adopted to prevent (#1370), arriving one step later in the same motion. The twins are compared WORD by word and the words unique to each are printed, with no history read and no rule about which is newer; on both live firings today those words were `--sweepcost`, `licence`, `doors_ok` and `--floorage`, which is exactly the thing the newer unit added. Four cases pin it, including the two orders — a merge lands the older twin second as often as the newer one, so the report must not imply that *later* means *newer* — and identical twins, which are still a break and must not print a misleading difference. Exit 0 every advised flag exists and every suite is run · 1 one does not · 2 the invocation was refused. **Both loop heads are total functions since #1443**, and the identities they close — `tools = suites + no_suite + skipped_self + skipped_no_promise`, and `tools = codes_checked + codes_exempt + codes_no_promise + codes_no_literal + codes_no_row` — are asserted rather than argued: `suite_class` returns one of `self|no_promise|admitted`, `code_class` one of `no_row|no_promise|no_literal|exempt|checked`, and ten cases drive every word. Both sums held BY CONSTRUCTION for a day, which is a property of control flow and not a check — a fifth exit added to either head would have broken the sum with no verdict word, no counter, and a census line that simply stopped adding up, which is the condition both counters exist to make visible. The last case is the one that earns the others: fixtures prove the words exist, and only a sweep of the REAL shop proves nothing falls between them, which is what a fifth exit looks like from outside. **`codes_no_row` is that fifth exit, found while writing the classifier:** a tool with no catalog row was skipped and counted by nothing, so the four-term sum held only while `uncatalogued=0` — a counter from another loop, under another verdict word — and an identity that depends on a different check passing is not one. It is a census field and not a break, because that population already fails the build through `uncatalogued` and one absence reported twice is one defect counted twice (#1170). The two exit-code words are reachable from one file and only one says why: a tool whose ONLY exit is computed lands in `no_literal` and not `exempt`, since the empty-spends test runs first — right, because with no literal there is nothing to compare, and stated here because the case that found it had to be rewritten to say so. **The flag audit reads the DOOR since #1341**, and names the helpers it stopped judging. The premise — a `--x)` arm is a promise to a user — holds for a tool's argument parser and fails for everything else in the file: a suite helper's parameter, an inner parse over a synthetic argv, a fixture body. This tool reported ITSELF for `--row)`, a parameter to a function inside `--selftest` and unreachable from any command line, and #1238 dodged it by making that helper positional — a workaround in one file, not a repair. **The expensive direction is the second one:** the natural fix for a false positive is to document the private parameter, the row then promises a flag the door refuses, `flags_phantom` fires, and two checks disagree about one string — which nobody reads as a defect in the audit, only as noise deserving an exemption. `by_scope <file> door|helper` splits on a rule about THIS SHOP's spelling, stated rather than inferred: a top-level function opens with `name() {` at column zero and closes with `}` at column zero, so an indented inner function (`case_()` inside `selftest()`, which three tools here write) neither opens nor closes a scope. **A flag found only inside a function body is counted and NAMED rather than dropped** — `flags_in_helpers=` with a `HELPER_FLAG` row, never a break — because that is the difference between a bound and an exemption (#1207): the day the rule is wrong about a tool, the output says which flags it stopped looking at instead of going quietly smaller. What the rule cannot do is find a door parsed inside a `main()`; no tool here has one, such a tool would report as having no flags at all, and `tools_no_flags=` is loud when it is wrong. The live near-miss is in this file: `--selftest)` is the door at one line and a fixture STRING inside the suite at another, and the whole-file read counted the fixture as a parse — invisible only because the two spellings match. `flags_in_helpers=0` on today's shop is a real zero and not a missing reader, so the nonzero lives in the cases: six of them, with the false positive and the true positive in the same suite, which is #1341's explicit ask since one without the other is how an exemption becomes a hole. |
| `litany.sh` | Judges the file that judges every pull request (#1114). `locks.yml` gained floors, lost swallowing captures and grew two locks in one night, and none of it was verified by running the litany — because nothing did. Four questions: does it parse (conflict markers, literal tabs, a step with neither `run:` nor `uses:`, the three keys a workflow needs — not a YAML library, which is a dependency this tree does not take); does every verdict grep match something the tree PRINTS, by longest adjacent prefix with a floor on what survived, so a renamed or reordered verdict is refused (#1157, #1168); are the lock numbers contiguous; are two steps named the same. It runs from its OWN workflow (`litany.yml`), because a judge inside the litany cannot judge a litany that will not parse — that is the entire reason this repository has a second workflow. **Since #1360 it also asks whether a step sets its shell options.** #1226 found ONE `run:` block with no `shell:` line, running under GitHub's default `bash -e {0}` — `-e`, no `-u`, no `pipefail` — while every neighbour set all three; it was lock 0, and an unset `RUNNER_TEMP` wrote its input to `/pr-body.md` instead of failing on a named variable. A lock writing its evidence to the wrong path and then judging that file does not fail, it passes on the wrong evidence. That was found by counting once, and the lane has grown fourteen `run:` blocks since. **Multi-line is judged, single-line is reported**, and the boundary is in the YAML rather than in a heuristic: `run: |` opens a script with variables and pipes; `run: <command>` is one command whose failure `-e` alone still catches, with nothing to leave unbound and no pipe to lose a status in. Six single-line blocks carry no `shell:` today and none is a defect — judging them would demand a line that buys nothing, which is how a rule gets exempted the first time it is inconvenient. `LITANY_CENSUS single_line_no_shell=` carries that six, because a reader meeting six exceptions with no number beside them infers the rule is optional. Usage: `--help` | `-h` prints the header's Usage clause and stops · `tools/litany.sh [PATH]` · `--selftest` (the case count is guarded by the lane's floor rather than quoted here — #1337) · `--shellcheck` parses every `run:` block as the shell that will actually run it, under the `shell:` that block declares · `--floorcheck` asks whether every suite gate carries a floor, so a suite that quietly stops running cases cannot pass · `--floorage` asks the question that one cannot: is the floor still the suite's NUMBER (#1538). A floor is a `-ge` test, so a floor UNDER the count is exactly as green as a floor at it — `advice.sh` ran 61 cases against `floor=58` and three could have vanished with the lane green. This is the only mode here that RUNS anything: a case count is not in the YAML and not in the tool, and every suite it walks declares itself token-free and network-free. `behind=` is the defect, `ahead=` is a gate that cannot pass at all, and `unreadable=` is the rule's own gap, reported and never a break. Its first draft read the twelve gates in order and found seven, because five write `floor=` above the invocation and seven below it — a checker quietly reading less than it claims to, which is the shape the mode exists for. Exit 0 every question answered clean · 1 any of them broken · 2 the invocation was refused. **2 arrived two units after it should have** (#1546): anything that is not one of the four flags is read as a PATH, so `--nonsense` became a workflow file that could not be opened and this tool failed the way a BROKEN LITANY fails — a caller branching on `$?` could not tell *you typed the flag wrong* from *the file you pointed me at is unparseable*, which is #1276's finding one directory over. |
| `baseline.sh` | Dates a PR's evidence against the tree it is about to land on — the one lock that judges a claim rather than a compilation. Reads the body's `**Baseline:**` sha and compares it with the base: warns and *names* the intervening commits when it has drifted, because most drift touches no evidence and only the author can tell which does; fails when the stated sha resolves to no commit at all, and when this PR moves the seal and its stated `before` is not the tree's before — a move nobody can ever verify is a lock with nothing behind it. Whether the seal moved is read from `.github/canonical-digest`, here versus at the base, never from a field the author fills in. Refuses a shallow clone rather than calling every honest sha unknown. `tools/baseline.sh --selftest` runs its fifteen cases against this repository's own history, fixtures included. Usage: `--help` | `-h` prints the header's Usage clause and stops · `tools/baseline.sh <pr-body-file> <base-sha>` · `tools/baseline.sh --selftest`. Wired into CI as lock 0. Exit 0 the claim holds · 1 it does not · 2 the invocation was refused. |
| `crowns.sh` | Every class has a crown, says the door (`CLAUDE.md`) — how many do? (#1459). Eighteen of eighty-nine did not, and seven of those appeared in no issue title in the repository's whole history, including `Streams`, whose `utf8()` is the first statement of every probe here, and `SheetDoor`, the one door `SheetFence` exists to prove there is exactly one of. The gap had never had a number and could not: a crown is an ISSUE, so no probe can see it — probes read the tree — and `backlog.sh` counts issues by EVIDENCE rather than by subject. A crown is a title opening `Class: <Name>`, and the `class-design` LABEL is deliberately not the discriminator: it also rides unit issues about a class, so keying on it would count a unit as a crown and report the gap as smaller than it is — the direction that hides a finding. Nested types are missed on purpose; a crown is a record for a top-level class. REPORTED, NEVER JUDGED, which is #1459's own ranking: whether all eighty-nine warrant one is open — `Geo`, `Family` and `Origin` may be enums where a record is ceremony — and a gate installed before that is answered would demand eighteen documents nobody has argued for. Usage: `tools/crowns.sh` counts · `--help` | `-h` prints the header's Usage clause and stops, because the refusal below it names a clause the reader cannot open · `--list` prints one row per uncrowned class with how many issue titles mention it at all, since a class with a unit and no crown is a different situation from one nothing has ever named · `--selftest` drives the two matchers over fixtures (no token, no network). Paging is stated because #1246 is the finding that it usually is not: `limit=` rides the census and a full page is a refusal. Exit 0 counted · 1 a `--selftest` case failed · 2 the invocation was refused · 3 the issue list could not be read · 4 no class was read at all, which is not an empty tree · 5 the answer filled the page, so it is a page and not a census. **The identity closes since the crown gate landed:** `classes = crowned + uncrowned`, with `crowned = classes ∩ crowns`, `uncrowned = classes − crowns` and `crownless = crowns − classes`. It did not close before, and the failure was invisible until the last class got its record: `crowned` counted the CROWNS rather than the crowned CLASSES, so the line read `classes=89 crowned=91 uncrowned=0` — a count larger than the population it is a count of, which a reader cannot tell from a duplicate crown, a crown for a deleted class, or a broken matcher, and one of those three would be a defect. **`crownless=` is the fourth term and not an error count:** both of today's two are legitimate — #887 `Class: Chronicle` is a crown cut AHEAD of its class (the v6.0 parser, born with a PR that has not landed) and #89 `Class: OpsConsole` is closed for a class that has since gone — so it rides the census under #1221 and `--list` names WHICH, because `crownless=2` alone is indistinguishable from a matcher fault. It is deliberately **not** gated: a gate there refuses the next crown somebody writes before the unit that builds the class. **`uncrowned=0` and `classes_none=0` ARE gated**, in `locks.yml`, installed at zero once #1461-#1467 wrote the seven crowns nobody had ever named and #1488-#1498 the remaining eleven — and the exemption question #1459 left open answered itself rather than being dropped: `Geo`, `Family` and `Origin` looked like enums where a crown is ceremony and turned out to have the strongest arguments of the eleven, so the rule keeps no exemption clause. `classes_none=` is on the verdict under this file's own gate-at-zero caveat, since a zero earns a gate when its population is separately asserted non-empty. |
| `counters.sh` | Does a probe's catalog row still name the counters its bench row pins? | `roster_check` asks whether a probe HAS a row (#1177); nothing asked whether the row still DESCRIBES the probe, so an instrument can grow a counter, a mode or a whole second question while its row keeps describing the instrument it was two units ago. #1319 repaired two such rows by hand and three counters have arrived since with their rows written in the same PR — the habit took, the mechanism did not, and nothing would have caught the fourth. **Reads the BENCH ROW and not the probe's source**, which is #1369's measurement rather than a preference: the wide reading — every `name=` token in any string literal in a probe's source — gives `tokens=621 missing=556`, a 90% rate that is almost entirely `seed=`, `ticks=` and progress-row noise, and a check with that ratio is not a check. The bench row is the CONTRACT: one line, and a counter that is pinned is one somebody decided the lane would judge. No probe is executed — this reads two files. **The matcher requires the token followed by `=`, and that is a correction, not a choice:** a substring read reported `ticks` as named because the row says *"6,000 ticks"*, and the figure published from it was wrong in the direction that HIDES the finding; `names-only-the-word` is the case that pins it. **The guard exemption is a list with reasons and never a pattern** (#1369's explicit requirement, #1207 the precedent): twelve names — `swept_none`, `checked_none`, `births_none`, `compared_none` and their family — mean the same thing on every probe that carries one, so demanding each row explain its own is asking for boilerplate rather than information, while `*_none$` as a pattern would exempt every future counter named that way including one that means something specific, and an exemption that grows on its own is a hole; `guard-not-a-pattern` is the case. Continuation lines are joined before anything is read, because the ledger's fattest row carries its arguments two lines down and a reader that stops at the backslash reads half a contract — which is why #1369's own figure of 44 was approximate and is superseded by `pinned=72`. `vary`'s quoted REASON sits in front of the verb, so it is stripped first: read naively the class is `judge` and the reason's own words are counters. **Reported, never judged**, and the split is deliberate — #1311's rule is that a gate installs at zero and not at one, because at one it demands a unit from whoever trips it, so #1369's stated order is edits first and gate second and this tool is the number that makes the edits finishable. `rows=`, `pinned=`, `named=`, `exempt=` and `no_row=` ride `COUNTERS_CENSUS` (#1221) while `pinned_none=` rides the verdict, since a reading that found no bench row must not print the line a fully-named catalog prints (#1207). `no_row=` is counted and not reported as a break: a class with no catalog row is `roster_check`'s finding, and one absence named by two checks is one defect counted twice (#1170). **`--bench FILE` and `--catalog FILE` point the reading at a scratch pair (#1511)**, and until they existed the READING was unfalsifiable: `report` already took both files as parameters and nothing could pass them, so the suite drove the three predicates while the join that uses them ran only against the live tree — where `missing=0` and `no_row=0`, which means no finding path had ever executed. A checker whose findings are unreachable from a fixture is a checker whose findings appear for the first time on somebody's pull request, and `flag_audit "$tmp/shop" "$tmp/shop/README.md"` is the same shape one directory over. Four cases now drive the join and each asserts the WHOLE verdict line rather than one field, since a census agreeing on `missing=` and disagreeing on `exempt=` would pass a narrower check: all named, one unnamed, a guard exempt, and a rowless class beside a named one — that last fixture carries two classes deliberately, because a rowless class alone leaves `pinned=0`, which is the `NOTHING_READ` refusal on stderr and a different finding (#1207); the refusal has its own case at exit 4. **It lost three rows for as long as it existed** (#1588). A `vary` block is three lines held together by backslashes and joins into one whose FIRST token is the modifier, so a reading anchored on the start found no verb and dropped the row through a silent `continue`: `UnparkStorm`, `DocFigures` and `AllocMeter` were all in that shape, and `worst=`, `bound=`, `stale=` and `refused=` were in no total this tool printed. Anchoring on the LAST verb makes the modifier's own quoted arguments irrelevant — whatever a `vary` grows in front of the verb, the row still reads from the verb. `rows=` moved 67 to 70 and `pinned=` 92 to 97; the four newly visible counters were unnamed and are named now. It survived because nothing else read this table in shell: `CatalogFlags` reads it in Java, disagreed, and that is how the loss was found (#1586). Usage: `--help` | `-h` prints the header's Usage clause and stops · `tools/counters.sh` · `--bench FILE` and `--catalog FILE` point the reading at a scratch pair · `--list` prints one row per pinned counter including the ones that pass · `--selftest` runs the parse's, the matcher's and the exemption's cases (no probe, no token, no network). **`row_no_counter=` and `prints_no_counter=` ride the census since #1372, split by #1579**, and it is the decidable half of that issue: a pinned verdict reading `VERDICT X_HELD` with nothing after it cannot be told apart from a row that always prints that line — there is no number in it to move, so the only way to watch the probe fail is to break the world it judges and look. Twenty-seven of sixty-seven judged rows are in that subclass — and **zero of the twenty-seven belong to a probe that prints no counter anywhere**, which is the measurement that separates the two readings. The single count was quoted beside a sentence about probes nobody can watch fail, and that is the second thing: `SameTick` prints `census=`, `growth_ticks=`, `late_ticks=`, `first_late=` and `max_stranded=` and pins `VERDICT SAME_TICK_ABSORB`, so it landed in the subclass while being one of the better-instrumented probes here. `row_no_counter=` is *the grep on this row cannot move*; `prints_no_counter=` is *this probe prints no number at all*, asked only of rows already in the first, so it is a subset by construction. A probe in both is one nothing can watch; a probe in the first only is a row that should pin one of the fields it already prints, which is a repair with an obvious shape and the reason the split is worth a second counter rather than a footnote. The source read is textual and generous — any `name=` inside a string literal, javadoc dropped first — so it over-counts a probe that prints `x=` in prose, which understates the finding and can never invent one (#1207). **The strip has a home and cases since #1583.** It is the THIRD in this tree and the only one that had none: `Probes.uncommented` reads Java and has cases (#1580), `advice.sh`'s reads shell, and this is a shell tool reading JAVA — so the third home is forced by the language boundary rather than chosen, since a script cannot call a Java method. Its first spelling dropped whole lines, keeping a trailing `// note x=1` and reading it as a printed counter; its second was `sed … | grep -q`, which under `pipefail` answers *prints nothing* about a probe printing twenty-one counters, because `-q` exits at the first match and the upstream `sed` takes SIGPIPE — the identical trap `advice.sh` documents beside its own `case` match. Both were found by cases and not by reading. The order of the four rules is the whole reading and the third arrived from a case: a leading `*/` CLOSES a block comment and is followed by code on the same line, so a rule dropping any line opening with `*` eats the code after the close. It counts ROWS and not probes, beside `rows=` and for the same reason: a probe with two judged rows is two pinned lines and either can be the one nothing can move. **Reported, never judged**, and the reason is #1372's own: many of these judge a SIMULATION and their falsifier is the world itself — `BondScenario` reads a 40,000-tick run and would report differently on a broken one with no counter to show for it — so a gate here would demand a number from a probe that has nothing to count, which is how a rule gets exempted the first time it is inconvenient. The other half of #1372, *has this probe ever been seen red*, has three sources and none of them is a lane check. Exit 0 counted · 1 a `--selftest` case failed · 2 the invocation was refused · 3 the bench or the catalog could not be read · 4 nothing was read at all, which is not a fully-named catalog. **Judged in the lane since the gate landed at zero**, and not by this tool's exit code: `missing=0` and `no_row=0` are demanded by an exact-line grep in `locks.yml`, while the tool itself still exits 0 on a finding. That split is deliberate. The five codes above are spoken for, so `missing>0` would either collide with **1** — which means the SUITE broke, and sends a reader to the wrong file — or claim a sixth code for a judgement a grep can make; `subissue.sh` spent the wrong code for three refusals until #1309 and neither of `advice.sh`'s exit checks could see it, so one code with two meanings is a defect this shop has already paid for. `exempt=` is **not** judged, because a gate on the guard family breaks the day a new probe legitimately carries a `*_none=` — the list is the visible part and its growth is a review question. `no_row=0` is the half `roster_check` cannot cover: that check compares disk against catalog and disk against bench, never a claim in prose, and `CensusSampleSize` was judged on every push with no row while a sentence in the catalog said it had no bench row either. |
| `backedge.sh` | Reads the inbound half of the decision web instead of remembering it (#1394). `docs/adr/README.md` has promised "an idempotent pass" since D-029 and there was none, so every `Referenced by:` line in `docs/adr/` was typed from memory — the one thing that sentence says they are not — and 70 of 180 forward links between records have no back edge, with 20 of 59 records carrying no line at all. That matters because the same paragraph tells a reader to consult the line *before you flip, supersede, or park a record*: a line naming three citers when six records cite it does not read as incomplete, it reads as answered. Only a MARKDOWN LINK counts as a citation — a D-number in prose is rhetoric, and the `Referenced by:` line itself is never read as a forward link, without which every completed pair becomes a fresh debt and the count can never reach zero. Usage: `--help` | `-h` prints the header's Usage clause and stops · `tools/backedge.sh [--check]` prints one row per missing edge · `--census` prints the three counts alone · `--write` adds a `Referenced by:` line to a record that has none and **declines every record that already carries one**, which is D-029's own refusal kept verbatim · `--selftest` drives fifteen fixtures, including the two that separate an edge from a mention in both directions and the four that hold the writer to the refusal. **What the refusal costs, measured rather than assumed:** of the seventy edges missing when this landed, twenty-eight were on the twelve records with no line and forty-two on twenty-five whose line existed and named only some of their citers — so `--write` closed the first group and cannot touch the second, which is the more dangerous half because a partial line reads as ANSWERED rather than absent. Idempotent by the same rule: a second run finds a line and declines. Wired into CI as its own suite step. Exit 0 WEB_COMPLETE · 1 WEB_INCOMPLETE, or NOTHING_READ when the sweep read no records at all · 2 the invocation was refused. |
| `dreamreader/` | D-047's teleprinter: one mind's whole day, folded out of the record into deterministic prose. Boots its own quiet universe, takes three feeds (the pilot's perception frames off `--follow`, the log lines that carry their name, the world's beats), and writes a page in four movements — every sentence derived from a captured line, and where the feed is silent the silence written down. Observer-only: not one domain byte moves. The only tool here written in Java, so its build is two commands and the daemon's is the first of them — it compiles against `out/`, and skipping that line gets `package matrix does not exist`, not a shorter build. Usage: `javac -encoding UTF-8 --release 17 -d out $(find src -name '*.java') && javac -encoding UTF-8 --release 17 -cp out -d tools/dreamreader/out tools/dreamreader/*.java && java -cp out:tools/dreamreader/out DreamReader --pilot "Otto Aydin" --seed 1 --ticks 6000`. Flags: `--pilot NAME` (required), `--seed N`, `--ticks N`, `--voice cold\|none`, `--facts`, `--capture-only`, `--out FILE`, `--check-golden FILE`, `--help`. Exit 0 a day rendered · 1 the golden day drifted · 2 the record holds nobody by that name · 3 the invocation was refused (a flag, a value or a voice the tool has no reading for) · 4 no golden day at that path. Those five separate *refused* from *absent* from *drifted*, which one number for all three could not (#1011): a sweep that skips the names a seed did not grow used to skip a misspelled flag with them and render nobody. `--out FILE` is written under `--check-golden` too, where stdout carries only the verdict. `bash tools/dreamreader/exitgrammar.sh` runs one invocation per code against a golden page it blesses in a temp directory, and prints `EXIT VERDICT PASS cases=N fail=0`. Wired into CI beside lock 11. |

House rules:

- A tool never mutates `src/`, and never a document's content; it builds,
  verifies, tags, publishes. **One exception, and it is D-029's rather than this
  file's:** *backlinks are navigation, not content — they are the one part of a
  record that may be regenerated*, and `docs/adr/README.md` commissions the pass
  that regenerates them. `backedge.sh --write` is that pass and the only tool
  inside the exception; it appends a `Referenced by:` line to a record that has
  none and refuses to touch one that exists. Everything else stands: a record's
  prose, its front matter, its Y-statement and anything under `src/` are not a
  tool's to write. The rule and the exception lived in two directories and
  neither named the other until #1413, which is how a tool commissioned by one
  document arrived forbidden by the other.
- A tool's failure mode is loud and early (`set -euo pipefail`, explicit FATAL
  lines) — a half-cut release is worse than none.
- Evidence is produced at run time, never pasted from memory. If a lock cannot
  be reproduced when the tool runs, the tool must refuse.
- A tool that reads a running universe keeps the probe contract
  (`probes/README.md`): its own private `Simulation`, an explicit seed, and
  read-only on everything it touches. D-047 put the reader "OUTSIDE (`tools/`
  or the bench)" and left the choice open; it lives here rather than on the
  bench because `--out FILE` writes a file the user names, which clause 1 of
  that contract forbids. Clause 6 it keeps — anything quoted as evidence comes
  from a pinned `git archive` copy. Clause 7 it keeps since #965: the page was
  always encoded once as UTF-8, but the verdicts and refusals printed around it
  went out on whatever charset the JVM inherited, so `matrix.Streams.utf8()` is
  now its first statement. The contract is the standard a tool is measured
  against here, not a claim that every tool already meets it.
- A capture that can fail carries a handler, or does not capture. `X="$(cmd)"`
  under `set -euo pipefail` dies ON THE ASSIGNMENT, so a tool that prints its
  failing cases and then exits nonzero has that report thrown away at the moment
  it became worth reading — the step goes red with an empty log, which is how
  #983 reported a dialect bug. Either write the handler,
  `out="$(cmd)" || { printf '%s\n' "$out"; echo "FATAL …" >&2; exit 1; }`, the
  shape `release.sh` already uses around `--bench`; or drop the variable, pipe
  the run through `tee "$LOG"`, and judge `$LOG`. Streaming is the default in a
  lane: under `pipefail` the tool's own exit code still fails the step, and by
  then `tee` has put the rows that say WHY into the log. The rule is about the
  capture and not about the caller — `tools/*.sh` and `locks.yml` steps alike.
- A tool that tells you what to type owes you a command that works, and the
  advice is the most expensive prose in the tool to be wrong in — it is read at
  the moment somebody has already decided to act. Three units in one day fixed
  the same defect in three tools: `attribution.sh` printed `--reset-author`,
  which destroys the author dates the tool exists to protect (#1012);
  `release.sh` matched a line that was no longer first, so a release could not
  be cut from a green `main` (#972); `balance.sh` named a leg its own arithmetic
  could not move (#952). Each was wrong in a way only EXECUTING it revealed.
  So: a printed flag exists in the tool it names, and a tool that prints advice
  has a `--selftest` for that advice to be executed in. `tools/advice.sh`
  audits the first half mechanically and **judges** the second since #1311 —
  it reported for as long as tools had no suite, and the last two got theirs in
  #1307 and #1309, so the count reached zero at the one moment a gate cost
  nothing. This sentence said *four tools have no selftest today* until #1337
  surveyed it. What `advice.sh` cannot do is RUN the advice: `git commit --amend` inside
  an audit would be a tool damaging the tree to check whether it damages the
  tree.
- A tool whose output is PROSE carries a golden file, because a page that
  reads well is not a page that is still true. `dreamreader/golden/` holds one
  blessed day; `--check-golden` re-renders and prints the first line that
  moved. Re-blessing is a diff in a PR, never a quiet overwrite. A golden file
  no lock runs is a comment, and this one was one for its whole life: it landed
  198 bytes behind the tree that shipped it and drifted twice more before
  anybody re-read it. `locks.yml` now builds the reader and asks it (#965), so
  the next page to move moves in a PR.

## Read your arguments before you reach the network

A refusal that needs no API must not require one. `subissue.sh` sent `--nonsense` to
GitHub before anything read it and left with `gh`'s exit code instead of the refusal
its row promises — passing locally, where there was a token and an open parent to
read, and printing `want=2 got=4` on the runner (#1309). **Two exit codes for one
mistake, and which one you get depends on whether you happened to be authenticated.**

This is a rule and not a check, and #1314 is why. The obvious lint — does a `gh` call
appear above the last line that reads `$1` — was written, run over all twelve tools,
and produced three violations, all three false:

```
attribution.sh   the gh call is guarded (`|| true`) and the parse is above it
prstate.sh       the gh call is inside a function, nine definitions down —
                 a definition is not an execution, and line order says nothing
subissue.sh      real, and already fixed by #1309
```

A line-number reading cannot tell a function body from a straight run, and a bash
parser is a dependency (D-009). So the ordering lives here, next to the self-matching
clause, for the same reason that one does: it is followable, it is cheap, and the
check that would enforce it costs more in false accusations than the defect costs in
occurrences.

One of three rules in this repository that state their own unenforceability, and they
read as one convention rather than three (#1322): this one, `probes/README.md`'s
clause 8 (a checker is inside the population it checks), and its census rule (a number
in a judged verdict must be a finding). Each says what the rule is, that nothing
enforces it, and **why a check would be wrong rather than merely absent** — the third
clause being the one that stops the next author building it anyway.

## Writing a checker here

Read clause 8 of `probes/README.md`'s contract first. Every tool in this directory
searches a population, and every tool in this directory is a file in one — `advice.sh`
alone has matched its own comments (#1157), a neighbour's row (#1222), its own suite
fixture (#1265) and its own `exit` pattern (#1276). That clause names the five repairs;
choosing among them deliberately costs a minute, and discovering the problem costs a
red first run at best and a check that silently covers nothing at worst.

## A gate installs at zero, and the zero has to be earned

The rule first, as #1311 wrote it and #1345 reused it four hours later:

> At zero the gate costs nothing and blocks the next tool that arrives without a suite.
> At one it demands a unit from whoever trips it, which is how a gate gets argued about
> instead of added.

It has been applied five times since and it lived in issue bodies, which is the state
every unwritten convention in this tree has been found in — `docs/ARCHITECTURE.md`'s
*verified by grep* with no grep (#1417), the twenty-two probes that refused an unknown
flag by habit (#1479). A rule nobody can read is a rule the next unit re-derives or
skips.

**The caveat it has never carried** (#1357). A counter reads zero for three different
reasons and only one of them earns a gate:

| the zero means | the gate is |
|---|---|
| the work was done — a unit drove the counter down | free, and it holds |
| the measurement is broken — the grep stopped matching, a loop returns early, the pattern was never right | green forever over nothing |
| the population is empty — nothing was scanned, so nothing could be found | green forever over nothing |

The second and third are #970's `INSTRUMENTS_UNPROVEN` and #1207's silent skip, and this
tree carries a counter against them in almost every checker for exactly that reason:
`checked_none=`, `swept_none=`, `scanned_none=`, `charset_nothing=`, `NOTHING_READ`. The
rule as written invited the failure those counters exist to catch.

**So a zero qualifies when one of two things is true**, and both are cheap:

- **it has a history** — `git log -S'<counter>' -- <file>` names the unit that drove it
  down. `crashed=` has one: eleven on the day it was measured, zero the same day
  (#1479 → #1481), and #1485 moved it onto the verdict once it did;
- **its population is separately asserted non-empty** — the `*_none=` counter beside it,
  which most already have. `missing=0` is gated in `locks.yml` next to a grep for
  `pinned_none=0`, so an empty reading cannot print a compliant tree's line.

A zero with neither is a candidate for measurement, not for a gate. Say which one in the
commit that installs it.

**And a finding count is not a population.** #1221 pushes numbers onto census lines, and
the boundary is what a number MEANS: how many things there are belongs on a census, how
many of them are wrong belongs where it can be judged. `crashed=` was on a census while
it read eleven — correctly, because #1311 forbids the gate at one — and moved to the
verdict when it reached zero (#1485). `swept=` and `refused=` stayed.

## The exit grammar — what a low code means, and who may spend it

<!-- figure: ls tools/*.sh | wc -l == 18 -->
<!-- figure: ls probes/*.java | wc -l == 74 -->
Every program in this tree that a script branches on: eighteen tools, fifty-odd
probes, and the teleprinter. Three populations, and until #1241 measured them,
three different meanings for `2`.

| code | meaning | who spends it |
|---|---|---|
| **0** | held · ok · the claim stands | everyone |
| **1** | broke · not-ok · the claim does not | everyone |
| **2** | **the invocation was refused** — a flag with no argument, a value the program will not take | **everyone**; this is the largest existing agreement in the tree — every tool that refuses anything refuses with it, and `advice.sh` fails the lane for a row that spends 2 for something else (#1222) |
| **3** | **the answer could not be read** — a rate limit, an expired token, a sha this repository has never seen, a node the API would not return | **six tools**, by imitation until #1304 wrote it down |
| 4 | *contested.* Three tools spend it for **nothing was read at all, which is not a pass**; two spend it for something else | see each catalog row, and below |
| 5+ | local answers only some programs can give | see each catalog row |

The first **four** are universal: a caller may branch on them without knowing
which program it ran. Everything above is **local**, and a row that spends it
must say what it means — `advice.sh` fails the lane for a tool that spends a
code its own row does not name (#1222).

**Three became universal by imitation and the table said otherwise for months
(#1304).** Six tools spend it and all six mean *the answer could not be read*,
in six different phrasings:

```
attribution.sh   the repository has never seen the sha
backlog.sh       the backlog could not be read
checkage.sh      the answer could not be read
prstate.sh       the repository could not be read
issuetree.sh     at least one node was unreadable, so the tree is PARTIAL
balance.sh       the API would not answer
```

The phrasings stay. Six sentences of one meaning is not a defect — *the backlog
could not be read* is better prose in `backlog.sh`'s row than a generic
sentence would be, and the local colour is what makes a row worth reading. The
defect was the table calling 3 local while six tools treated it as universal,
so a reader branching on `$?` across two of them was right and the grammar told
them they were wrong.

**Four is contested and now says so.** `attribution.sh`, `backlog.sh` and
`checkage.sh` spend it for *nothing was read at all, which is not a pass* —
#1004's shape, an absent answer wearing an empty one's face. `prstate.sh`
spends it for `UNBUILT` (near enough) and `balance.sh` for *a window and its
days disagree*, which is a different thing entirely. That disagreement is real,
it is small, and it is visible here rather than discovered by a caller.

`advice.sh` cannot police either code the way it polices 2. Its
`codes_redefined=` check works because 2 is both universal AND phrased
identically in every row — the one code where a wording comparison is a check
rather than a style opinion. For 3 the wordings differ by design, so the
agreement lives here, in prose, and is kept by reading.

**Where this is not yet true, stated rather than hidden.** `probes/` spends
**2** for `NEVER_AROSE` and **3** for a refused invocation; `DreamReader` spends
**2** for `EXIT_NO_SUCH_PILOT` and **3** for `EXIT_USAGE`. Both disagree with
the table above, and #1239 moved the probes to 3 on a survey one program deep —
the defect it fixed was real (nine probes reported a typo as a world with no
births) and the constant it chose is provisional. #1241 owns the reconciliation:
`NEVER_AROSE` has to move up before `REFUSED` can come down to 2, and that is a
larger change than the unit that started it should have made alone.
