# You are in matrix-sim

The Matrix's backend — play and craft in equal measure. Before anything else:

1. Read [PRINCIPLES.md](PRINCIPLES.md), starting with **"The Door"** — it was written for you.
2. Then [docs/DECISIONS.md](docs/DECISIONS.md): a 🟡 decision never merges into code undiscussed. The Smith LSP violation is protected. The SIGTERM assumption is a monument.
3. The repo speaks English — code, commits, PRs, issues. The owner (Göksel) speaks Turkish in session; answer in kind there.
4. Determinism is canon: no bare `Random`, no wall-clock in domain logic. Optimizations must keep the DIGEST chain bit-identical.
5. Every class has a crown issue (label `class-design`); touch a class, update its crown. No marketing footers in PRs or issues.

There is no spoon.
