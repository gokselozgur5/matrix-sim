package matrix.entities.eco;

/** Names the pluggable gaits (D-016). The species row selects one; the strategy does the walking. */
public enum MovementKind {
    FLOCK, SWARM, ROOTED, DRIFT, WANDER, COMMUTE
}
