package matrix.realworld;

/** The kinds of wire a mind rides into the Matrix — pure data, one constant per way in. */
public enum LinkKind {
    HARDLINE,
    /** The accepted D-013 spark, cashed in (D-032): broadcast in from outside the farm — no pod behind it. */
    PIRATE
}
