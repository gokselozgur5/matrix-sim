# probes/ — the skeptic's bench, in permanent form

Four adversarial passes produced a drawer of throwaway diagnostic programs; each ran
once and died with its scratchpad. This directory is where they live instead. A probe
is not a test and not a feature: it is an instrument you point at a running universe
when one of the three D-020 instruments shows a symptom the others cannot explain.

**If this file conflicts, do not take a side** — the catalog rows below are one
line each, so two units editing two different rows collide across both, and
taking one whole side silently reverts the other's row. `.gitattributes` sets
`merge=union` here: the merge completes with both versions present and the
duplicate is the thing to repair, not a row to choose between (#1370).

## The probe contract

1. **Read-only, with one carve-out that is now stated rather than inferred.** A probe
   never mutates the repo tree, never writes files, and never calls anything on a
   shared `Simulation` that could queue a `WorldEvent`. It prints.

   The carve-out: a probe MAY mutate **its own private universe** when the mutation is
   *the thing being measured*, and it says so in its javadoc. `SystemFatigue` calls
   `world.bumpVersion()` because the fact under test is that the sheet's fatigue axis
   moves when the world reloads — there is no other way to ask that question, and the
   universe it bumps is one it built itself (clause 2). It was the first mutation any
   probe in this tree performed, and it was inside the letter of a clause written when
   none did; the rule people were actually following had stopped being the rule on the
   page (#1261).

   The boundary does not move: never a shared `Simulation`, never a queued
   `WorldEvent`, never the repo tree. A probe that writes a file is still a probe that
   lies to the next reader — clause 5's `--without-probes` check binds itself by this
   same sentence. And *convenient* is not *measured*: a mutation that saves the probe a
   setup step, rather than being the fact under test, is refused by this clause and not
   licensed by `SystemFatigue`'s precedent.

   Not counted by anything, deliberately. A grep for `.set` across `probes/` is mostly
   false positives, and a parser is a dependency (D-009) — so this clause is social,
   which is stated here rather than left to look mechanical.
2. **Own universe.** A probe constructs and ticks its **own private** `Simulation`
   (quiet sink, explicit seed). Determinism does the rest: the universe it dissects is
   byte-identical to the one that showed the symptom.
3. **Reflection allowed.** Probes may open private fields. The daemon's encapsulation
   protects the domain from the domain — not the coroner from the corpse.
4. **Greppable verdicts.** Evidence lines are stable, prefixed, one-per-fact
   (`DUP …`, `t=… link#… …`), so a probe's output can be diffed across runs and quoted
   in a PR without prose. "Diffed across runs" is a promise the bench keeps rather
   than assumes: `probes/bench.sh --twice` runs every probe a second time and
   byte-compares, and a probe that reaches for wall-clock, a default locale, a heap
   address or an unordered iteration fails the sweep on the line that moved. The
   second run is taken under `LC_ALL=C`, because the locale clause was the one this
   pass could not see while both runs stood in the same shell (#836).

   **A NARROWED pass runs three times, not two** (#1355). #1302 put a wall-clock
   `secs=` on `DocFigures`, the lane ran `--twice-changed DocFigures`, and it PASSED —
   then the weekly full pass found `secs=5` against `secs=4` six days later. The
   narrowing selected the right probe and the comparison was right; one comparison is
   simply not enough for a field whose drift is PROBABILISTIC, and wall clock at
   one-second resolution is bimodal — two runs 400 ms apart print the same integer
   most of the time. The third run is bounded by the narrowing itself, which is the
   probes one pull request touched; `--twice` over the whole bench stays at two,
   because there the extra run is fifty of them and the whole-tree question belongs to
   the weekly pass. `STABLE <cls> runs=` says which it was, because a reader cannot
   otherwise tell how much the word is worth. **It turns a coin flip into a better one
   and does not make the answer certain** — the guarantee this pass gives is about
   bytes, and a field that moves on a clock has no guarantee to give.


   **Both branches of the comparison, since #1567.** The third run landed on the plain
   branch first and stopped there, which left the narrowing's strongest protection off
   the rows with the strongest prior: a probe wearing `vary` is one KNOWN to have a
   field that moves on a clock, and that is evidence it is the population most likely
   to grow a second one — `DocFigures` is the probe #1329 was opened for and it is
   exempt. The cut-and-compare is applied to the new pair rather than to a merged
   three-way read: the first run is the reference and each later run is judged against
   it, so a drift on run three reports the way a drift on run two does. `--cut`'s rule
   is decided rather than copied — it asks whether the exemption pattern still reaches
   its subject, a third run that cuts fewer lines is the same finding, and the message
   says which run cut them.

   That hostile second run **is** the charset comparison for this directory, and it is
   worth saying because a sibling check was thought to be missing it. `advice.sh`
   reports `charset_checked=0` and #1207 read that as a guard covering nothing —
   correctly, for TOOLS, which print instrument lines in ASCII by convention. The
   population that does have the defect is this one, and it has been compared under a
   hostile locale since #836, weekly in `determinism.yml`. Two halves of one question,
   in two directories, neither written where the other could be found (#1207).

   **A pair, not a duplicate** (#1299). This half compares one probe's whole output
   between two PROCESSES, the second hostile — strictly stronger on the locale axis,
   and it catches heap addresses, unordered iteration and wall-clock with it. The other
   half compares one tool's `--selftest` stdout between two LOCALES, one process each,
   and it is the only thing in this tree that runs a TOOL twice and compares. Folding
   them together would lose that: no other check gives a tool a second run at all.
5. **Outside the build.** Nothing under `probes/` is compiled into the daemon.
   `src/` must build and `--selftest` must pass with this directory deleted. "With this
   directory deleted" is a promise the bench keeps rather than assumes:
   `probes/bench.sh --without-probes` extracts `git archive HEAD` into a throwaway tree,
   removes `probes/` from **that** copy, builds `src/` and selftests there, and prints
   `VERDICT BENCH_STANDS_ALONE` or `VERDICT BENCH_ENTANGLED`. It never deletes anything
   in the tree you are standing in — clause 1 binds the check as hard as it binds a probe.
   The lane has run it since #1291; before that the clause was kept by whoever
   remembered to type the command.

   **What it guards is not what this clause used to advertise (#1293).** The coupling
   named above — a `src/` class depending on something in `probes/` — has no syntax:
   probes are in the DEFAULT package, every file in `src/` is in `matrix.*`, and Java
   provides no import for that direction. Nobody needs to spend a unit re-guarding it.

   The failure the checker really catches is the archive/working-tree gap. `git archive
   HEAD` cannot see a `src/` file that exists on one developer's disk and in nobody
   else's clone, so a committed class needing an uncommitted one prints `src_builds=no
   VERDICT BENCH_ENTANGLED`. That is a real defect class, this repository would meet it
   on an ordinary bad day, and nothing else in the lane looks for it — `compile src`
   builds the working tree, which is the copy that has the file. `BENCH_ENTANGLED`
   therefore reads as *the archived tree does not stand*, which is broader and truer
   than the coupling it was named for.
6. **Pinned to a SHA.** A probe run that is *evidence* — anything quoted in a PR,
   a verdict, an ADR errata or a skeptic round — is taken from a `git archive <sha>`
   copy, never from a shared working tree that may move under it. This is Ag9's
   closing sentence and clause (3) of D-030's errata, which records why: it was
   adopted the day a tree moved mid-verification, and the round survived only
   because the skeptic had pinned. `tools/` rides the same rule.
7. **Owns its bytes.** A probe's first statement is `matrix.Streams.utf8()`. D-020's
   line grammar is a byte contract, and a JVM takes its charset from the environment:
   with no locale exported, JDK 17 resolves it to `ANSI_X3.4-1968` and every em dash
   the probe printed becomes `?`. The pin is scanned for in CI, so a probe that
   forgets it fails the lane rather than the next reader.
8. **A checker is inside the population it checks.** Every instrument here searches a
   set of files, and every instrument here is a file. The ones that have found
   themselves, oldest first — this list is the record, and adding to it is how the next
   one gets written down:

   - `litany.sh` matched its own ghost verdict (#1144)
   - `advice.sh` matched its own comments (#1157)
   - `advice.sh` matched a neighbouring row through an unanchored grep (#1222)
   - `advice.sh`'s suite matched its own fixture — `= "--pr"` is what the extractor
     greps for (#1265)
   - `advice.sh` matched its own `exit` pattern, in a comment (#1276)
   - `DocFigures` matched its own catalog row, which explains the marker syntax and
     therefore *is* a marker (#1283)
   - `SheetDump --stolen` matched the line searching for the fence, so a probe about
     *exactly one fence* reported `branches=2` over a tree with one (#660)

   The last one landed six hours after this clause was written, by an author who had
   just read it and applied repair (1) to fix it. **No count is quoted here** — the
   clause carried "six" and went stale the same day, which is the shape #1279, #1284,
   #1330 and #1332 each cost a unit (#1335). A list of names with issue numbers ages
   honestly; a number beside it does not.

   Every one was caught on the check's first run, which is why the shape kept getting
   fixed locally and never written down: it announces itself loudly, and the next
   author meets it fresh. The five repairs, **in the order to reach for them** — they
   are not equivalent, and the list used to imply they were (#1296):

   1. **assemble the fixture at runtime** so the literal never appears in the file.
      Cannot fail quietly: the string is absent or the fixture does not exist.
   2. **anchor the search** so a neighbour cannot answer for you (`^| \`name\``).
      Cannot fail quietly either: the anchor matches or it does not.
   3. **strip comments before counting** — a tool discussing `--foo` is not parsing it.
      Narrow failure: a string literal containing `//`.
   4. **escape the syntax in prose** when the document is the population. Holds until
      somebody writes the marker a second way.
   5. **exclude yourself explicitly** — LAST RESORT, and narrowed to the check that
      needs it rather than the loop. This is the only repair whose failure is silent
      and unbounded: `advice.sh`'s whole-loop `continue` exempted it from the census it
      publishes, nothing went red, and the tool reported *two tools cannot execute
      their own advice* while being an uncounted third (#1265).

   The ordering matters because (5) fits every shape. A reader choosing quickly reaches
   for the bullet that always applies, which is precisely the one that fails without
   saying so.

   Nothing enforces this clause and nothing can: a check that detected self-matching
   would have to know which file it is, which is the same problem one level up. One of
   three rules here that say so — see the census rule below and `tools/README.md`'s
   ordering rule, which state it in the same form for the same reason (#1322).

## Building and running

**Pinned — the form for anything you will quote.** The SHA goes in the PR next to
the output, so a reader can reproduce the exact universe you saw:

```sh
SHA=$(git rev-parse HEAD)                     # or a tag: $(git rev-parse v3.0.0^{commit})
WORK=$(mktemp -d)
git archive "$SHA" | tar -x -C "$WORK"
cd "$WORK"
javac -encoding UTF-8 --release 17 -d out $(find src -name '*.java')
javac -encoding UTF-8 --release 17 -cp out -d probes/out probes/*.java
java -cp out:probes/out <Probe> [args]
```

Building inside the pinned copy also keeps clause 1 honest: `probes/out` is written
in the throwaway tree, not in the tree you are working in.

**Unpinned — casual local poking only.** From the repo root, daemon already built
to `out/`, and with the understanding that the tree can move under you between the
compile and the run:

```sh
javac -encoding UTF-8 --release 17 -cp out -d probes/out probes/*.java
java -cp out:probes/out <Probe> [args]
```

Compile the whole directory, not one file: most of the probes here call the shared
`Probes` reflection helper, and `javac … probes/<Probe>.java` alone fails on them
with `cannot find symbol: variable Probes`. How many is a question with a command
rather than an answer with a date:

```sh
ls probes/*.java | wc -l                            # every file here
grep -al 'static void main' probes/*.java | wc -l   # the ones you can run; the
                                                    # difference is the Probes and
                                                    # LineGrammar helpers, which have
                                                    # no main and are not probes
grep -al 'Probes\.'         probes/*.java | wc -l   # how many call the shared accessors
```

**No count is quoted here, and the third attempt at that is why.** The
figures in this paragraph are all in the past tense, dated, and attached to
the issue that measured them — that is what keeps them from rotting, and it
took three tries to get right:

| attempt | figures | how they were phrased | outcome |
|---|---|---|---|
| original | `39` / `37` / `17` | in code comments | rotted, found by #1192 |
| #1192 | `45` / `43` / `26` | *today the same three commands print* | rotted, found by #1279 |
| #1279 | `50` / `48` / `42` | *Today the same commands print* | rotted before the PR merged, #1284 |

The last row is not a joke at #1279's expense; it is the finding. That unit
diagnosed the defect exactly — *the phrasing is the defect, the numbers were
only its symptom* — and then wrote the phrasing back in, in the sentence
introducing the ban, sixteen lines above the ban itself. Three repairs, all
of them to the numbers, and the tense survived all three.

A figure in the past tense with a date cannot rot: *by the time #1279 read
them the commands printed 50, 48 and 42* is true forever. A figure in the
present tense is a claim about now, and nothing re-runs it. That is the whole
rule, and this paragraph is the fourth attempt at obeying it.

The same file did it in prose too. The sentence introducing the block kept its
pair — *seventeen of the thirty-seven* — through #1192's whole repair, so the
paragraph announcing that the numbers were removed was the paragraph still
carrying them, and every figure in it was wrong including the one asserting
that wrong figures had been taken out (#1279). A document describing its own
repair, un-repaired, in the file that teaches the practice.

#1283 owns the mechanical version of this — comparing a prose figure against
the command printed beside it — and would have caught every row of the table
above. Until it lands, the rule is the tense: the commands are the answer, and
a number written near them is history or it is a bug.

`grep -a` is kept out of habit rather than necessity now. `SheetDump.java` used to
carry a raw NUL byte inside a string literal, which made a BSD grep call the whole
file binary and undercount both figures by one — and, more expensively, made lock
8's charset guard skip that file entirely (#1039). The byte is an escape now, so a
bare `grep` agrees; the flag stays because the next probe to need a control
character should not silently change what these commands count.

## The sweep

One command runs every probe and prints one verdict:

```sh
probes/bench.sh            # 6,000 ticks each, compile included
probes/bench.sh --list     # the contract table, run nothing
probes/bench.sh --twice    # the sweep, plus the determinism pass below
probes/bench.sh --without-probes   # clause 5: does src/ still stand alone?
```

The contract table lives in that script, one row per invocation — `judge
<Class> '<exact line>'` for an instrument that verdicts, `run <Class>` for one
that only reports, either of them prefixed by `vary '<reason>'` when the row's
output may legitimately move. A judged probe is judged by exact-line grep
(`grep -qxF`), so `=0` can never match `=01` and a missing verdict fails the
sweep; a reporting probe fails only by crashing or exiting nonzero.

**A number in a judged verdict must be a number whose CHANGE IS A FINDING.**
A number that merely describes how much was looked at goes on a census line
beside it (#1221). Exact-line grep is what makes this a rule rather than
taste: every count a verdict carries becomes a lane pin, and a pin that must
be hand-edited to go green is a number people learn to edit — after enough
edits, nobody reads it. That is #884's mechanism, recorded for the seal and
running quietly in five verdict lines.

The test, on the two rows that show the difference:

| line | pinned number | is its change a finding? |
|---|---|---|
| `LEDGER_SWEEP … clean=53 broken=7 at 4,7,8,…` | the seeds that break | **yes** — a seed starting or stopping is the whole claim |
| `SEAL_HYGIENE … checked=25` | how many ids were pinned | **no** — pinning a 26th turned the lane red for doing what the probe wants |

`LeaveContract` learned it twice in one afternoon (`judged=35 → 36 → 37`, once
for its own row landing and once for a sibling's), and `SealHygiene` was
repaired by #1221. The emptiness of a population IS a finding and stays on the
verdict — `checked_none=`, `scanned_none=`, `swept_none=` — because a run that
looked at nothing must not be able to report that nothing moved. The count
goes beside it: `SEAL_CENSUS`, `FENCE_CENSUS`, `ROSTER_DOCS`, `FIGURE_CENSUS`,
`LEAVE_CENSUS`.

**Nothing enforces this rule and nothing can** (#1322). A check would have to
know which numbers are findings — which is the judgement the rule exists to
convey, so a checker able to make it would not need the rule. What a lane
*can* see is the symptom, one incident late: a verdict row edited in a PR that
changed nothing about what the verdict claims. That is a code review, and this
paragraph is what a reviewer reads before writing the comment.

Third rule in this repository to state its own unenforceability, and they now
read as one convention: clause 8 above (a checker inside its own population),
`tools/README.md`'s ordering rule (arguments before the network, with the
three false positives its failed lint produced), and this one. The form is
what the rule is, that nothing enforces it, and **why a check would be wrong
rather than merely absent** — the third clause being the one that stops the
next author building it anyway.

**A judged probe also owes an honest exit code, and until #1093 only the grep
was ever enforced.** The bench reads the verdict line, so nineteen probes could
print a failing verdict and exit 0 and no lane cared — while every hand-run
invocation, every `$?` in a script, and `bench.sh --twice` (whose `settle` reads
the second run's code before comparing bytes) were told the contract held.
`Probes.leave(verdict, held)` is the one place that owes both: it prints the
line the bench greps and leaves with 0 or 1 to match. A **reporting** probe must
not call it — a `run` row fails on a nonzero exit, so adopting an exit code
there changes what the row means.

**Three codes, and the third one is named rather than positional.**
`Probes.leave(verdict, Probes.Outcome.NEVER_AROSE)` is how a probe says the run
never reached the situation it judges — no births, no liberations, no firing.
Silence is not testimony, and a probe that printed a passing verdict there would
be certifying a question nobody asked.

    HELD         0
    BROKE        1
    NEVER_AROSE  2
    REFUSED      3

It is an enum because the shape it replaced was two adjacent booleans:
`leave(v, false, true)` meant *broke* and `leave(v, true, false)` meant *never
arose*, one transposition apart, same types, compiling identically. The entire
purpose of the third code is that a sweep can tell those two apart — so an
argument order that could swap them silently swapped the green sweep with the
red one (#1204).

**A refused invocation is 3, and that number is borrowed rather than invented.**
A flag with no argument, a scale the config will not accept, a roster too wide
to print — nothing about the world was judged, because the probe never got as
far as looking. `DreamReader` has spent 3 on `EXIT_USAGE` since #1011, and 3 was
chosen to match it.

**That match was with the smaller incumbent, and it moved the collision instead
of closing it (#1219).** This paragraph used to end *the two families of program
in this tree agree where they overlap: a script branching on `$?` across
`probes/` and `tools/` reads one meaning per value*. That was false when it was
written. `DreamReader` is one program carrying a five-code grammar of its own;
`tools/README.md`'s universal table is eleven shell tools, and it calls 2 —
**the invocation was refused** — the largest existing agreement in the tree. So:

<!-- grammar-boundary: 2 3 -->

    code   probes/          tools/
    0      HELD             held · ok · the claim stands          agree
    1      BROKE            broke · not-ok                        agree
    2      NEVER_AROSE      the invocation was refused            SPLIT
    3      REFUSED          the answer could not be read          SPLIT

**Nothing may branch on `$?` across the two families above 1.** Inside a family
the value is exact; across them, only 0 and 1 carry. `bench.sh` reads probe
codes and reads no tool's; the lane reads tool codes and reads no probe's; the
split is survivable today because no caller spans both, and that is a property
of the callers rather than of the grammar.

The marker above is the mechanism, not decoration. `ExitGrammar` measures which
codes the two families spend differently and requires every one of them to be in
that list — and requires every code in the list to still be split, so a boundary
cannot outlive the collision it was drawn for. A collision nobody wrote down is
red; a collision written down is a boundary. Unifying them is the other repair
and it is a decision about the bench's contract — whether `NEVER_AROSE` gives up
2 — which is #1219's own framing and the Architect's call, not a probe's.

It was **2** in nine probes — the word for NEVER_AROSE — so a refused invocation
reported as a world with no births (#1239); and **1** in `BirthInputs`, which
said so at the call site while declining to add a fourth code, because that was
a decision about the bench's contract rather than a repair. This is that
decision. A probe with nothing to print reaches `System.exit` directly through
`Probes.Outcome.REFUSED.code()`, so the number still has one home.

**#1239 fixed nine and left six (#1345).** The count was taken by grep and the
grep found the probes that spelled the refusal one way. Six others reached
`System.exit(2)` from an argument-parsing branch — a missing `--wing` family, an
unknown selector, a scale the config refuses, a block list nobody gave,
`NeutralDiff`'s function literally named `refuse()` — and every one of them was
read by the bench as `UNEXERCISED`: *its scenario never arose in this run*. Not
a failure. A row that could not run printed the same word as a world with no
births, and the sweep stayed green.

Two more spent **3** for *a sweep did not finish* and *a universe failed*, which
are breaks wearing the refusal's number; they leave with `BROKE` now. **No probe
writes a digit any more, and `ExitGrammar` fails the sweep if one does** — a
literal is not searchable, and this file has spent three units finding numbers
that had no producer.

**A known break is declared, not deleted and not tolerated.** `known <Class>
'<verdict>' '<#issue>' [args]` is the third verb, and it exists because #1093's
honest exit codes made "expected broken" unsayable: `judge` fails a row whose
probe exits nonzero, so a defect the tree already knows about could only be
watched by deleting the row or by dropping the exit code. Both lose something.
A `known` row requires the nonzero exit **and** matches the verdict exactly, so
a probe that starts *passing* is as red as one that starts failing differently
— that is what makes it a lock rather than a mute button. The issue number is
in the row; when it lands, the row becomes a `judge`.

**A property row runs at two seeds; an arc row runs at one (#1094).** The
distinction is what the row CLAIMS. *"The contract held"*, *"breaches = 0"*,
*"the audit is clean"* are claims about every world, and one universe cannot
support them — three units in one night found defects the canonical seed cannot
see, and the second seed added here immediately found a fourth (#1155: D-021's
perception clause holds at seed 42 and at no other seed tried). *"The film's
beats are 1299, 1525, …"*, *"movers = 19"*, *"the subject is Nadia Petrov"* are
measurements of the canonical arc, and asserting them at another seed would be
asserting a different measurement, not a stronger one. Seed 7 is the tree's
second canonical universe; nine property rows carry it, measured at 6.4 s.

**Which printed field belongs in the pinned line (#1584).** `row_no_counter=27`
is #1372's measurement and `prints_no_counter=0` is its companion: none of the
twenty-seven is a blind probe. Every one of them prints numbers and pins a
word, so the row goes green on a suite that ran nothing — the shape #1615,
#1623 and #1633 each found separately, in the eight units before this rule was
written. The repair is one rule applied twenty-seven times rather than
twenty-seven arguments, and this is the rule:

- **The field the verdict word is ABOUT — pin it.** `late_ticks=0` under
  `SAME_TICK_ABSORB`, `anomalies=0` under `POD_OPTIONAL_HELD`. It is what the
  word claims, said in a number, and it is the only field whose movement means
  the claim changed.
- **A count of the SUITE — pin it, exactly.** `scenarios=`, `cases=`,
  `checks=`, `beats=`: how many things the probe put a question to. This is the
  half the twenty-seven are missing, and it is the half that catches the
  emptiness — `anomalies=0` over zero scenarios is the literal sentence
  *nothing disagreed with me* (#1207, #1373). Exact and not a floor, because a
  floor cannot see a swap (#1615), and a suite's members are named shapes, so
  an exchange is a real change of subject.
- **A count of the WORLD — leave it on its own line.** `census=`, `swept=`,
  `open_streaming=`, `checked=`: a population the run measured rather than a
  question it asked. Pinning one reddens the sweep on every lawful digest move,
  which is #1221's argument and the reason `BENCH_COST` is a census.
- **A sentinel or a restatement — leave it.** `first_late=-1` means *never*,
  which is what the verdict word already said; a second copy of one claim is
  not a second lock.
- **A `*_none=` guard — it is already there,** by name and by exemption
  (#1369), and it is this rule's older half: it keeps a reading that found
  nothing from printing the line a passing reading prints.

The distinction that carries the weight is **suite versus world**. Both are
counts and only one is a contract: the suite is what the AUTHOR decided the
probe would ask, so it moves when somebody edits this repository; the world is
what the run found, so it moves when the seal does. A row may pin the first and
must not pin the second.

`PodOptional` is the first row repaired under it — `scenarios=8 anomalies=0`
rides `POD_OPTIONAL_HELD`, the census line that carried them is gone rather
than duplicated (#1624), and the rule had been applied once before it was
published as advice. Twenty-six rows remain, one unit each.

**The lane has a budget of its own (#1115).** The summary line carries
`secs=<measured> budget=<ceiling> WITHIN|OVER`, because `--bench` judges the
*daemon* against D-027's table and nothing judged the sweep that runs it — so
every unit adding an instrument made the lane longer for every unit after it,
unbounded by construction. The ceiling is `BENCH_BUDGET_SECS`, 300 s by default
against a measured 147 s, and it is deliberately loose: wall clock on shared
hardware moves 20% between runs, and `OVER` **prints without failing**. The
thing being prevented is a lane growing tenfold over a season, which a human
reads; a bound that reddens under load is a bound that gets switched off.

**And a ceiling on the sum could not say which row spent it (#1216).** The
trailer reported one number for a two-minute sweep; learning *where it went*
meant re-running and reading 1,400 lines, which is the work a trailer exists to
save. The line above it now answers:

    BENCH_COST slowest=UnparkStorm:18,LedgerMirror:17,BondScenario:8 rows_timed=67 classes_timed=49

Three properties of that line, each a decision rather than a detail:

- **Above the trailer, not on it.** #1216 asked for the trailer; the census rule
  (#1221) says a number whose change is not a finding may not ride a judged
  line, and wall clock is that number exactly — the lane's own history is
  `min=296 max=710` for the same work. One line above is adjacent enough to
  answer the question, and keeps a timing off the line `PROBE SWEEP FLOOR`
  greps.
- **Per class, summed.** Rows outnumber classes: `SheetFence` runs twice with
  different flags. The question being asked is what a probe costs the sweep, and
  two rows of one class are one answer to it.
- **Measured in `execute`, not at the verbs.** A row that fails never reaches
  its verb's `PASS … secs=`, so attribution written there would report where the
  time went on a green day and lose the answer on the day somebody needs it.
  `--twice` folds its second runs in for the same reason: on that invocation the
  second run *is* half the cost.

What it still cannot say is whether a row is too expensive. `slowest=` catches
one fat row; fifty rows each growing a second move the total the same distance
and appear nowhere. That is #1348, and it is a decision about whether cost gets
a rule at all rather than a gap in this one.

Adding a
probe is a one-row change here, beside the probe — one row per probe, and one
row per mode where a probe verdicts in more than one. `SheetBench` is the only
one that does today, and its `--avalanche` row is `run` rather than `judge` on
purpose: that mode prints its measurements and its verdict on the same line,
so judging it by exact line would pin `mean_bitflip` and `max_axis_corr` into
the runner beside the bound the probe already prints and already checks. Its
exit code is its verdict instead, which is what `run` reads.

One row reads a committed file. `CensusBeatDrift` judges today's beat ticks against
`probes/beatdrift.baseline` — two rows, one per standard seed, verbatim as the probe
printed them at the tree their own `sha=` names. Moving the pin is those rows
re-measured in the commit that moves the film, so `git log -p -- probes/beatdrift.baseline`
is the whole move history, the way `.github/canonical-digest` holds the seal's. Its
judged line carries the set it judged (`compared=16/16`) and the tolerance it judged
against (`band=200`), because a probe that read a baseline naming none of the beats
would otherwise print the same clean line as one that compared all sixteen pairs.

## The bench refereeing itself

The probes referee the daemon. `--twice` is what referees the probes: every row
runs a second time at the same seed and budget, the two outputs are byte-compared,
and the first line that moved is printed with its number.

```sh
probes/bench.sh --no-build --twice-changed SheetFence 200
```
```
STABLE SheetFence
STABLE SheetFence
BENCH determinism probes=62 stable=2 drift=0 exempt=0 unchecked=60 VERDICT INSTRUMENTS_NARROWED
```

`SheetFence` appears twice because the pass is per INVOCATION and that class
holds two rows — the fence and `--crossings`. `stable=2` counts invocations,
not classes, which is the same denominator the sweep itself uses.

The command is quoted beside the output for #1130's reason: a sample nobody
re-runs is a sample that rots, and this one did. Until #1277 the block above
read

```
BENCH determinism probes=17 stable=15 drift=1 exempt=1 VERDICT INSTRUMENTS_DRIFTED
```

— four fields against the program's five. `unchecked=` arrived with #970 and
the document never followed, so the line a reader was shown as *what this looks
like* was a line no run had produced since. That is worse than a stale count:
a count is data, and this is GRAMMAR. A reader writing a grep from it gets a
pattern that matches nothing, and the lane's grep works only because it was
written from the program instead.

The other two rows, in the shapes the pass prints them:

```
DRIFT NameCensus line=37 a="NANO 3276938399359740" b="NANO 3276941531734784"
EXEMPT AllocMeter reason="prints its own instrument noise: steady_max is a cold …"
```

The digest leash proves the *world* is deterministic and says nothing about the
instruments pointed at it — and a drifting instrument is worse than none, because
it manufactures mysteries the world never had and the next skeptic spends a round
chasing a phantom that lives in the coroner, not the corpse. The sweep prints two
verdicts because they are two facts: `BENCH_GREEN` can sit directly above
`INSTRUMENTS_DRIFTED`, and the run still exits nonzero.

**Four words, because a narrowed pass is not an incomplete one (#1247).**

| verdict | what happened |
|---|---|
| `INSTRUMENTS_STABLE` | every instrument was compared, none moved |
| `INSTRUMENTS_DRIFTED` | something moved — outranks everything below it |
| `INSTRUMENTS_NARROWED` | `--twice-changed` selected a subset, on purpose, and here is what it covered |
| `INSTRUMENTS_UNPROVEN` | the pass was supposed to reach every instrument and did not |

`--twice-changed` exists to narrow the pass to what a pull request touched
(#1185), so a large `unchecked` is what a CORRECT narrowed run looks like — and
every correct narrowed run used to print `INSTRUMENTS_UNPROVEN`. That word is
#970's, coined for *a green report about work that did not occur*; spending it
on the ordinary case wears it out, which is #884's mechanism (a lane number
people learn to edit) applied to a verdict people learn to ignore. The
per-PR lane read `drift=0` instead of the verdict for exactly that reason —
a lane cannot demand a word that fires on success — and it reads the verdict
now that there is one it can ask for.

The denominator stays on all four, as #970 insisted: `stable + drift + exempt
+ unchecked = probes`, whichever word is printed.

A row that legitimately moves declares itself with `vary` and states why, rather
than being skipped in silence. Exactly one does today: `AllocMeter` reads the JDK's
thread-allocation counter, which counts the JIT compiling as well as the daemon
allocating. Its headline barely moves — the steady window is a median of 24
repeats and printed `367` on eleven of thirteen back-to-back runs at seed 42,
`407` and `415` on the other two (#817) — but the same line deliberately
carries `steady_max`, the cold first repeat, which is a different number every
time (1,952 to 7,899 across those thirteen). That field is the
evidence and it is why the row is still exempt: an instrument that publishes its
own noise cannot pass a byte-compare, and hiding the noise to pass one would be
the disease, not the cure.

`vary` prefixes a verb rather than replacing one, because moving and being judged
are different questions. `AllocMeter` is the row that asks them separately: its
byte counts move with the JIT, so it is exempt from the determinism pass, while
`VERDICT ALLOC_IN_BUDGET` is fixed for as long as the budget holds, so the sweep
judges it (#906).

## Catalog

| Probe | Question it answers | Case it solved |
|---|---|---|
| `NameCensus` | Are grown pilot names unique at a seed? | seed 42: 196 humans, 154 distinct — namesakes are real |
| `LinkTrace` | How did one pilot's link state evolve, tick by tick? | the Nadia Petrov double-dark mystery **as of the `v3.0.0` tag** (worn 1717, freed by The One 1846, worn 2477) — on current `main` that trace is gone: her link never changes in 6,000 ticks, so run this one pinned (`git archive v3.0.0`) if you want the case the field manual narrates. A bare run now closes with `VERDICT STILL_LINK` and the count of links that *did* move in the same window, so an era mismatch reads as a verdict instead of a malfunction |
| `LinkAudit` | What is the end-state of every NeuralLink after N ticks? | ghost-link triage: open/closed × alive/dead × present/absent. **`ghosts=` and `audited_none=` ride the verdict since #1638** and the four populations beside them do not: `ghosts=` is the field `CLEAN` is chosen by — `all[4] == 0` is the branch — and it was printed on a neighbouring line and pinned nowhere, which is #1584's first clause. `open_streaming=`, `worn_by_smith=`, `closed_clean=` and `closed_dead=` stay off the pinned line because they are the world's own numbers and `open_streaming=190` moves with every lawful digest move (#1221). `audited_none=` is the vacuity guard and it was REACHABLE, unlike the one #1636 went looking for in `SameTick`: `ghosts=` is a count over the link book, an empty book gives `ghosts=0`, and no `NEVER_AROSE` branch sat under it — so a clean audit of nothing left at exit 0 printing the line a clean audit prints (#970, #1207). Falsified both ways: a planted ghost gives `GHOSTS_FOUND ghosts=1`, an emptied book gives `audited_none=1` and a nonzero exit |
| `ChainDump` | What is the DIGEST chain of a run, as plain lines? | out-of-band replay diffing between two boxes — and since #518 the same diff at scale, which is the only cross-PROCESS determinism proof the dial has: `ChainDump [ticks] [seed] [scale]`, two runs at `2000 42 11` byte-identical, trailer `CHAIN … scale=11 entities=5266`. `--selftest` double-runs inside one process, where anything derived from the dial once is shared by both runs and cancels; two processes do not share it. The scale rides the daemon's own gate (`Config.scaleRefusal`), so 0, 101 and 217 die with the daemon's sentence and its exit 2, and scale 1 appends nothing so every chain this probe has already published keeps its bytes |
| `LedgerMirror` | Does every ledger delta equal the open-link residue mirror? | the ghost-HARDLINE class of bug, made permanently detectable (`LEDGER_ANOMALIES=0`, seeds 42, 7 & 9 — seed 9 joined when #863 modelled the clean exit, which the mirror used to report as an anomaly and explain away in a comment). `compared_none=` rides the verdict since #1429 and `compared=` the MIRROR census: the comparison is not per tick — a delta of zero is skipped, 602 comparisons in 6,000 ticks — so the tick budget was never the population and could not stand in for it **The sweep row's four counters, named because #1356's reader found them unnamed.** `seeds=0..59` and `ticks=6000` are the sweep's EXTENT — sixty universes at the standard budget, and they ride the pinned line rather than a census because a sweep judged over a different span is a different claim wearing the same verdict. `clean=53` and `broken=7` are #1090's whole finding: seven of the first sixty universes report an anomaly and only seed 42 was ever run, so the split is the measurement and not a status. A reader who greps `broken=` used to land on a bench line and nothing else. |
| `SealHygiene` | Are the numbers the seal borrows from the JLS still the numbers it borrowed? | #837: `String.hashCode` sits inside `World.digestEntity`, so a species id is canonical text and not a caption — renaming `"black cat"` moved the chain from `421d7263…` to `ec0a1b61…` in total silence. Ids and both door thresholds pinned; needs no ticks, no seed and no world, so it is the one probe that cannot be flaky. **How many** is on `SEAL_CENSUS` rather than the verdict since #1221: `checked=25` was a lane pin, so adding a twenty-sixth id turned the sweep red for doing exactly what this probe wants done. `checked_none=` stays on the verdict, because a run that pinned nothing must not be able to report that nothing moved — the emptiness is a finding, the count is a description. `breaks=` is the judged counter — a borrowed JLS number that moved — and `checked_none=` is the guard beside it since #1221: `checked=25` was once on the verdict and a twenty-sixth id turned the lane red for doing exactly what this probe wants done, so the COUNT moved to `SEAL_CENSUS` and only the emptiness stayed judged |
| `LeaveContract` | Does every probe the bench JUDGES reach its exit through `Probes.leave`? | the contract `probes/README.md` has stated since #1093 — a judged probe owes a greppable verdict line AND an honest exit code — checked for the first time. Eight judged probes had no `System.exit` anywhere the day this landed (`BondScenario`, `CapSentinel`, `CensusSampleSize`, `DocLint`, `DoorPressure`, `LedgerMirror`, `PodOptional`, `SealHygiene`): each printed its failing verdict and fell off the end of `main`, which is exit 0, so `SEAL_HYGIENE_BROKEN` would have left with the code that means the seal is clean — #1091's defect, eight times over, four months after it was fixed. The bench table is READ rather than a list being kept here, because a list would be a second copy of the bench's own contract. Its verdict pins the CONTRACT — `no_code=0 by_hand=3 undeclared=0` — while `LEAVE_CENSUS` carries the population unpinned, after the first draft put `judged=` in the verdict and the exact-line row went red twice in one afternoon for reasons unrelated to the check. Two facts are separated rather than merged: a probe with no exit call at all is red (`no_code=`), a probe that calls `System.exit` itself is honest and merely counted (`by_hand=`), since calling a style a lie is exactly the confusion this probe exists to end. **`by_hand=` is a number with a reason attached since #1218**: `Probes.leave`'s javadoc has said since #1093 that the contract has one place to be read and one place to change, and that sentence was false in twelve files — #1204 demonstrated the cost when the three-valued exit became an enum and the change reached only the probes already using the helper. `DistrictCensus` and `DistrictNeutral` are converted; the three left are not convertible and each says why in a `LEAVE_BY_HAND` comment beside the exit it explains — a FATAL on stderr with no verdict to print, a verdict already emitted by a reporting helper, and a probe whose word and code are deliberately separable. `undeclared=` IS the break: a count that only ever goes down by accident is not a lock. It is the one reading in this file that looks at COMMENTS on purpose, which is the mirror of `spendsBeyondRefusal` reading through the strip (#1531) — a declaration is prose, and prose is where it belongs. **The marker must OPEN a comment line since #1605**: #1218 found it with a whole-file `contains`, so a probe satisfied the demand by naming the marker anywhere — in a javadoc about this mechanism, in prose explaining why it needs none, in a string. That is the self-matching shape this file's neighbours have met five times and that #1531 found in THIS file, reintroduced eleven units later by the unit that added the demand. It cannot be found through the strip, because the marker is deliberately in a comment; the reading is the mirror, and the bound is a SHAPE rather than a proximity rule with a number nobody can justify. **A declaration is not a justification** — `// LEAVE_BY_HAND: because` passes any rule here. What it buys is that a fourth by-hand exit costs an author a sentence, which is what `digest-move.sh` buys for the seal: not correctness, but a deliberate act with a name on it. Blind by construction to a probe that routes through the helper on one branch and returns on another. **The reader reads through the comment strip since #1531**, and it had to: `spendsBeyondRefusal` read the raw file, so a javadoc sentence containing the words `System.exit` made `CensusBeatDrift` — which had no verdict exit code at all and printed `DRIFT_FLAGGED` at exit 0 — read as a probe that spends its own, and the `Probes.leave` test one line above had the quieter half of the same blindness, skipping any probe that merely MENTIONS the helper in a comment (`KnownFixture` did). `--selfcheck` gives the matcher its own fixtures for the first time: `cases=` is how many of them ran and `failed=` how many disagreed with the classification they were written to pin, two of the five going red against the pre-#1531 raw read. **Its cases are its own since #1619**: thirty-one that belonged to `Probes` moved to `HelperSuite` with #1615, and their history followed them here with #1619 — a catalog row describing code in another file is the second-copy shape #1192 spent a unit deleting, and a reader grepping this file for `benchRows` was landing on the probe that no longer contains it. **`LEAVE_MEMBERS` carries the sets since #1550**, because `by_hand=5` was 5 before #1531 and 5 after and the five were not the same: `CensusBeatDrift` left the population and `KnownFixture` entered it, and the pinned row matched throughout. The row is right to match — it pins the contract and not the census — but every argument for pinning a number is that a change in it is a finding, and a SWAP is a change that produces no signal. The set is sorted and joined so two sweeps of one tree are byte-identical, and it is UNPINNED: a member list in an exact-line grep is a list every unit edits (#1192, #884) |
| `HelperSuite` | Do the shared helpers in `Probes` behave as their javadocs say? | thirty-one cases that were homeless for five units. `Probes` is the one class every probe on the classpath can reach — `uncommented`, `joined`, `benchRows`, `verbShaped`, `BENCH_VERB_WORDS` — and it has no `main` and no bench row, so its cases went where a harness already existed: `LeaveContract --selfcheck`, which #1580 called a compromise rather than a preference and #1592 doubled down on. Three units later that suite carried forty-one cases across **six** subjects under one verdict word about exit codes, and its lock floor guarded their SUM — forty could vanish from one subject and be replaced by forty in another with no gate moving, `cases=` being how many ran and `failed=` how many disagreed with the behaviour they were written to pin, which is #1443's shape: a number that cannot distinguish the population it is about. The alternative was a `main` on `Probes`, and that changes what a shared helper class IS — every probe on the classpath gains an entry point it must not use. A probe whose SUBJECT is the helpers costs one row on a bench of seventy-eight and leaves `Probes` a helper. Nothing moved but the home: the cases are the ones #1580, #1592, #1594, #1596, #1598, #1600 and #1602 wrote, and the two verdicts now have floors that move independently — `LEAVE SELFCHECK cases=10` is the exit-code classifier and the `LEAVE_BY_HAND` marker, `HELPER SELFCHECK cases=31` is everything about `Probes`. **What this does not buy:** a subject-level count, only a class-level one. `HELPER SELFCHECK` still sums five helpers, and the same swap is possible inside it — smaller by six-fifths and the same shape. The rule that keeps it honest is one verdict per subject, and this class has one subject only because `Probes` is one class; **Since #1620 the verdict carries FIVE numbers instead of one** — `strip=5 joined=4 rows=13 shapes=6 verbs=3` — because #1615 moved the summed-count problem from six subjects to five rather than removing it. `cases=` and `subjects=` moved to `HELPER_CENSUS`, since a total the five already determine is a sixth pinned thing to edit (#884, #1221). Falsified by moving one loop from `shapes` to `verbs`: the old row printed `cases=31` unchanged, the new one prints `verbs=9` with `shapes=` gone. `failed=` is still a sum and is meant to be — a nonzero there is red whatever its subject, and every case prints its own name one line up. No world, no tick, no seed — every case is a function of text the probe writes into a temp directory itself, and nothing reads `bench.sh` or this catalog, because a case that reads the live tree fails for reasons that are not about the helper **The cases and their history came from `LeaveContract` (#1615, #1619).** Five since #1580 belong to `Probes.uncommented` rather than to the exit-code reader they were written under: the strip makes four checkers correct and had no cases of its own, its evidence being four callers whose suites exercise it INCIDENTALLY. **The property asserted is the LINE COUNT**, which was asserted nowhere — #1512's whole subject was that a regex deleting `(?s)/*.*?*/` deletes the newlines inside a block comment too and joins the line before to the line after, and `LatticeFence` reads field declarations one line at a time, so the joined result is a member declaration nobody wrote. A collapsing strip fails `strip-block-spans-lines` at `lines=2/3` and passes everything else. One case records that the strip is a HEURISTIC and not a lexer: a `//` inside a string literal is truncated, no probe writes one today, and the case exists so the next reader meets the limitation as a fact rather than discovering it. **Ten more since #1592, and they belong to `Probes` rather than to the exit-code reader**: `joined` (#1574) and `benchRows` (#1590) are shared by every probe on the classpath and had no cases, their evidence being four numbers across three probes that did not move — real regression evidence that pins no RULE. An unsorted `joined` fails `joined-sorts` and nothing else; a `benchRows` that swallows the row after a `vary` block — the shell reader's mistake, which cost three rows for a year (#1588) — fails `bench-vary-then-two` and nothing else. They lived under `LeaveContract` for the reason the strip's did — `Probes` has no `main` and no bench row, a compromise #1580 stated rather than preferred — until #1615 gave them a probe of their own. `benchRows` joins backslash continuations since #1594, because `counters.sh` always did and this read line by line: a row split BEFORE its quote reads as a `judge` with no verdict, which `judged()` calls a `run` row — and #1590's lane step would then report two readers disagreeing on a pull request whose author was writing a long line, a correct check naming the wrong cause (#1170). The order is the trap: joining first makes a `vary` block one line whose first token is the modifier, so the prefix is STRIPPED rather than the line skipped, and the strip is anchored on the LAST verb — the first draft was lazy and stopped at the word `run` inside a `vary` reason, producing a row whose class was the word *the*, which is #1588 arriving in Java through the fix for the Java version of the same bug — a mistake found by a census line and not by a case, because the fixture reason was the word `why` and contained no verb. #1596 added the case that would have caught it: a `vary` reason saying *two of its markers run the daemon* reads as `run/the/` against the lazy spelling, and one saying *the row this judge decorates* as `judge/decorates/`. A dangling backslash on the last line is pinned as DROPPED rather than repaired — the file is malformed, the reader cannot invent the missing continuation, and a case saying so beats a StringBuilder deciding by accident. **One pattern and one verb list since #1598**: there were two — one anchored at the start to read a row, one finding the last verb to strip a joined `vary` prefix — and nothing asserted they held the same verbs, so adding a fourth to one and not the other passed every case while either mangling a class or losing a row. `BENCH_VERBS` is the list, in one place, used twice in one pattern. **The skip is bounded by what a row LOOKS like** rather than by the verb alone: a decorated row whose verdict says `VERDICT X run Beta` has two verbs on the joined line and the greedy skip took the second, reading the row as `run/Beta/` — the lookahead demands verb, class and quote, which verdict text does not supply. That case is in the suite, and without it this reading would have shipped with the bug it was written to remove.  |
| `VacuousGuard` | Can a judged row pass over an EMPTY population? | a verdict with no denominator cannot tell *the contract held over everything* from *the contract held over nothing*, and this tree has invented a counter for that five separate times — `checked_none=`, `swept_none=`, `scanned_none=`, `door_missing=`, `stale_none=` — each after a probe printed a passing line over an empty set (#970, #1207). #1373 counted twenty-five judged rows with no such guard. **That count was true when it was taken and had no producer to re-run (#1082)**, which is the finding this probe answers rather than the number itself: it missed a SECOND guard the tree uses just as often, an `Outcome.NEVER_AROSE` on the empty path, which makes the population print a DIFFERENT word so the pinned row goes red with no number on the line at all — `SameTick`'s `NO_LIBERATIONS`, `BondScenario`'s `NOT_DEMONSTRATED`, `ClauseAftermath`'s `NO_FIRING`, all three on #1373's list of the dangerous group and all three guarded. The population is the bench's own `judge`/`known` rows read as text, never a list kept beside them, for `LeaveContract`'s reason: the second copy is the one that goes stale (#1192). **One entry per PROBE and every row read** — a first-row-wins rule reads whichever mode sits higher in the table, and `NeutralDiff`'s selfcheck row is above its real one, so the first draft called a guarded probe unguarded. **Comments are stripped before the constant is looked for** (#1531), and that is not a precaution: the `grep -c NEVER_AROSE` used to write #1540's own measurement counted a javadoc sentence in `NeutralDiff` and put a probe in the guarded column that has no guard — the identical comment-blind mistake, in the measurement, on the day it was fixed in `LeaveContract`. `judged=` is the population, `by_field=` and `by_word=` the two guards, `unguarded=` the answer; the first three ride `VACUOUS_CENSUS` unpinned because a census inside an exact-line row goes red for reasons unrelated to the check and then becomes a number people edit until the lane is quiet (#1221, #884). **`unguarded=` is pinned and REPORTED, `no_source=` is JUDGED**: converting twenty-seven probes is twenty-seven judgements about what each denominator IS, and a gate demanding them in the unit that lands the reader is a gate that gets exempted in the unit after it — `unfalsifiable=`'s path, reported by #1095 and judged by #1311 once its population had been worked down. A judged row naming a class with no file is the one condition under which the count means nothing, so that is the break. **What it cannot see:** the `NEVER_AROSE` test is textual and generous — it asks whether the constant is reachable in the FILE, not on the empty path, so a probe spending it for an unrelated reason reads as guarded. That error understates the problem and can never invent one, which is the safe direction for a census and the wrong one for a gate, and it is the thing to fix before `unguarded=` becomes a break. **`VACUOUS_MEMBERS` carries the set since #1550**, for the reason `LeaveContract` learned one row up: `unguarded=` is pinned, so a probe gaining a guard while another arrives needing one is a green row over a different population, and a count with no names cannot show a swap. Sorted, joined, and unpinned. **The word test searches for an ASSEMBLED string since #1607**, and it had to: `contains("Outcome.NEVER_AROSE")` is the string on its own line, so this probe counted ITSELF as guarded by the string it uses to count guards — a checker exempting itself, the sixth instance of one shape here after `advice.sh`'s five, `LeaveContract` reading `System.exit` inside a comment (#1531) and `LEAVE_BY_HAND` satisfied by prose about `LEAVE_BY_HAND` (#1605). Qualifying the name does not fix it, because the qualified literal is still the literal; the strip does not, because the string is CODE. Only assembling does, which is what `advice.sh` reaches for when it builds `--pr` from `$dash`. The probe is NOT excluded from its own population instead — `LeaveContract` states why: a check that excluded itself would report a number nobody could reproduce by counting the file. **And the probe that measures vacuous guards should not be one**: breaking the self-match put it into its own `unguarded=` correctly, since its verdict carried `unguarded=` and nothing else, and `unguarded=0` is not the statement *the read opened a table*. `judged_none=` is that statement. **The word guard asks about the EMPTY PATH since #1609**, which is the open question its crown named on the day it landed: the test was *is the constant anywhere in this file*, and six probes were counted as guarded by a rule that cannot tell an empty-population guard from any other use of the constant. It now looks BACK from the constant for a condition mentioning emptiness — `== 0`, `isEmpty()`, `< 1`, and `> 0` because an `else` after it IS the empty branch for a count. `AllocMeter` moved to `unguarded=`, which is exactly the case #1609 predicted the old rule was always going to get wrong: its `NEVER_AROSE` is `ranAt != 1`, a refused configuration rather than an empty population. The window is six lines and that is a number and therefore a weakness — wide enough for `BondScenario`'s five, narrow enough that an unrelated condition does not reach, and it was four until four called `BondScenario` unguarded. It still cannot decide whether a branch is REACHABLE or whether the condition is the RIGHT one; `if (census == 1)` reads as a guard, and that error over-counts guards and under-counts `unguarded=`, the safe side for a census and the wrong one for a gate. **`--selfcheck` makes the window arguable since #1611**: the reader shipped with a FITTED number — set to four, run, `BondScenario` reported unguarded, its gap measured at five, set to six — and no way to change it except against a live sweep. `cases=` is how many ran, `failed=` how many disagreed, and `window=` is the number they ran with, so the row carries the constant it was judged under. The width is falsifiable in BOTH directions: seven breaks `window-just-outside`, four breaks `guard-else-after-positive`. One case is the false positive the loose `> 0` invites — an unrelated `retries > 0` inside the window makes a refused-configuration guard read as an empty-population one, which is what would have happened to `AllocMeter` had its condition been written `ranAt > 0` |
| `CatalogFlags` | Does a probe's catalog row name every flag the probe parses? | `probes/README.md` is read against `probes/` in exactly two directions and both are about EXISTENCE — `roster_check` asks whether every probe has a row and every row a probe (#1177), `counters.sh` whether a row names the counters its bench row pins (#1356). **Nothing asked whether the row DESCRIBES the probe**, and one directory over `advice.sh` asks that six ways. Twenty flags were found parsed by a probe and named by no row the day this landed, across twenty-six probes with a door: `CensusBeatDrift` parses `--band`, `--baseline`, `--baseline-file` and `--sha` and its row names none of them. **One direction only, and the other is refused with a reason.** `advice.sh` checks both, and its phantom direction (`flags_phantom`) is decidable only because a tool's row carries a bounded `Usage:` clause; these rows have none — they are three columns of prose that legitimately name other programs' flags, `bash probes/bench.sh --twice` and `git rev-parse --show-toplevel` among them — so a whole-row read would report every one as a phantom. That direction needs a clause the rows do not have, and inventing one is a change to the catalog rather than a check of it. **The door is Java, so the reading is SHAPED rather than greedy**: a quoted long option counts only where it is compared — a `case` arm, `.equals("--x")`, `startsWith("--x")`, and the reversed `"--x".equals(…)` that `NeutralDiff` writes and the first three miss. That is not hypothetical caution: a greedy read of `DocLint` collects `--is-shallow-repository` and `--verify`, which are arguments it hands to `git`, and reports both as undocumented flags of the probe — the exact false accusation `advice.sh` spent #1341 removing from its own reading. Comments are stripped first (#1531), because a flag discussed in a javadoc is not a door. `probes=` is the bench table's population, `checked=` those with a door and a row, `no_flags=` those parsing no long option at all — counted and never judged, the shape `advice.sh` prints `NO_FLAGS` for (#1207) — and `no_row=`/`no_source=` ARE the break, because either means the read walked past a probe it could not see. `undocumented=` is pinned and REPORTED: twenty row edits is twenty judgements about what each row should say, which is `unfalsifiable=`'s path — reported by #1095, judged by #1311 once its population had been worked down. **What it cannot see:** the shaped read is four spellings, not a parse. A fifth — a `Set.contains(arg)` door, a table lookup — reads as no flag at all and the probe reports clean. That direction understates and can never invent a finding, which is the safe one for a census and the wrong one for a gate. `--selfcheck` runs the reading over fixtures in a temp directory (#1576) — four door spellings because a four-branch alternation has four ways to be wrong, plus the false positive that cost `advice.sh` a unit (an argument handed to `git` is not this probe's door, #1341) and a flag named only in a comment (#1531). It found a defect in the OTHER half while being written: the row join was a plain `contains`, so a row saying `--prefix` read as naming `--pr` and a flag the row never mentions counted as documented — the substring trap `advice.sh` solved by padding both sides with spaces, here in the direction that HIDES a finding rather than inventing one, which is why nothing noticed. `cases=` is how many ran and `failed=` how many disagreed with the classification they were written to pin; one of the twelve goes red against the pre-#1576 `contains`. **Both no-counter populations moved here in #1586** and the census carries them: `row_no_counter=` is *the grep on this row cannot move* — a pinned verdict reading `VERDICT X_HELD` with nothing after it cannot be told apart from a row that always prints that line — and `prints_no_counter=` is *this probe prints no number at all*, asked only of rows already in the first so it is a subset by construction. They were in `counters.sh`, which meant a SHELL tool read Java and carried the tree's third comment strip; that strip is deleted, because this class already reads sources through `Probes.uncommented`, which carries state across a block comment and has cases (#1580). **The move corrected the count.** The shell reading said twenty-seven and the answer is twenty-eight: its row joiner collapsed `AllocMeter`'s two judged rows into one, and an independent grep over `bench.sh` confirms twenty-eight. It counts ROWS and not probes for the reason `AllocMeter` demonstrates. **`bench_rows=` is the cross-language number since #1590**: `counters.sh` prints its own `rows=` from a shell walk of the same table, the two disagreed by three with nothing comparing them, and the lane compares the two integers now — so the next divergence is a red build rather than an accident somebody notices while writing a second reader. All three Java readers of the table take `Probes.benchRows` since that unit. **`verb_shaped=` and `unread_verbs=` read the FILE rather than a reader (#1600)**, which is the direction nothing had: every other check here reads a reader, and if `bench.sh` grows a fourth verb both readers ignore every row that uses it and then agree perfectly about the rows they can see — two readers with one blind spot agree, so #1590's comparison cannot help. Seventy-seven lines are shaped like a row and seventy are read; the seven are `vary`, a modifier that decorates a row rather than being one, which is why the gap is reported as WORDS and not as a number. `unread_verbs=` IS the break, unlike the two no-counter populations beside it: a row nothing reads is not a style preference. **The known set is DERIVED since #1602**: it was a `Set.of` copying the reader's three words, three units after #1598 collapsed two verb lists into one and in a unit whose own subject was that the checks read the readers. Add a fifth verb to the reader and not to the copy, and this reports the READER'S OWN verb as unread — a false accusation about the file, the class of defect `advice.sh` spent #1341 removing. They are not the same list and merging them would be wrong: this one is the reader's plus `vary`, because the modifier is known to the FILE and not to the reader, which is exactly why the two counts differ by seven; the three copies they replaced agreed by DESCENT, having been written from one template, which is weaker evidence than three readers agreeing. `checked_none=` is the guard against the population being empty, and it is here for a reason worth recording: without it this probe was the twenty-eighth row `VacuousGuard` reports as unable to tell a full population from an empty one — a checker of catalogs joining the population of rows that cannot say whether they read anything. **`CATALOG_FLAGS_MEMBERS` carries the set since #1572**, for the reason its two siblings do: `undocumented=` is pinned, so a flag documented while another arrives undocumented is a green row over a different twenty. The per-finding `UNDOCUMENTED_FLAG` rows are not enough — they come out in bench-table order, so moving a row in `bench.sh` reorders them and a diff of two sweeps reports a reordering as a change. Sorted, joined, and unpinned. **`no_counter_declared=` and `no_counter_phantom=` split the backlog from the floor (#1636).** `row_no_counter=` counted two things under one name — a row that SHOULD pin one of the fields its probe already prints, and a row whose verdict word IS its whole contract — so the number could never reach zero and therefore could never be gated, which is a second reason #1372 was right to refuse. A row declares with `# NO_COUNTER <Class>: <reason>` beside it in `bench.sh`, `row_no_counter=` stays the whole subclass, and the difference is the part somebody can finish. **The declaration is read from the RAW file and not through the strip**, deliberately: it lives in a comment on purpose, which is the mirror of `LeaveContract`'s `LEAVE_BY_HAND` reading (#1218, #1605) and of `spendsBeyondRefusal` (#1531). The pattern is anchored at the start of a comment line and requires a reason after the colon, and both edges have cases — without the anchor this class's own javadoc about the marker declares every class it names, which is the self-matching shape #1605 found in `LeaveContract` eleven units after the unit that introduced it; without the reason clause a declaration is a required field become `n/a` (#1246). **A declaration is not a justification** — `# NO_COUNTER X: because` passes — and what it buys is what `vary` buys for the determinism pass: a deliberate act with a name on it. **`no_counter_phantom=` IS the break** and rides the verdict while both populations beside it are only counted: a row repaired later leaves its declaration behind, and a stale exemption is one nobody re-reads. It is `flags_phantom`'s refused direction, decidable here for the reason it is not decidable there — the declaration NAMES its class, so the join is exact — and it is the only half of this mechanism a gate can install AT ZERO (#1311), since the declared set starts empty. `SameTick` is the first declaration and #1636 carries its measurement. The `DOOR` pattern also lost a fourth alternative, `switch ("--x")`, which was UNREACHABLE: a switch takes the variable, not the literal, so nothing here could ever have matched it, and an unreachable branch is where a wrong one hides longest (#1358) |
| `KnownFixture` | Does the bench's third verb still work? | a break that will never be fixed, so `known` stays proven. The verb had **zero rows and zero tests** (#1231): it worked once for `LedgerMirror`'s broken seeds and retired when that row became a judge again — leaving the verb the tree reaches for at its worst moment (the defect is real, the fix is not ready, and the choice is between deleting the row and dropping the exit code) defined, unused and unobserved. This probe prints one line and exits 1. No seed, no tick, no `Simulation` — a fixture that touched the daemon could break for a second reason and stop being a fixture. `--heal` makes it exit 0, which is how the inversion a `known` row rests on gets falsified: a probe that starts PASSING must fail the sweep with *make this a judge row*, and that branch is the one most likely to break under a refactor of `execute`/`ROW_RC` (twice this week: #1093, #1138). Its issue is itself and will be closed forever, which is stated rather than hidden — every OTHER `known` row must cite an issue that is genuinely open **`by_design=yes` and `issue=1231` are the third verb's own markers**, named here after #1356's reader found them unnamed, and named in the ROW rather than added to `counters.sh`'s exemption list — which was the choice #1472 put on the record. `by_design=` is what makes the inversion legible on the line itself: the row is BROKEN and that is the pass condition, so a reader scanning a red sweep needs the word before they go looking for a defect. `issue=` is row-specific by design — every other `known` row must cite a genuinely open issue, and this one cites itself forever — so exempting the pair by name would have exempted a marker whose whole value is that it differs per row, which is how an exemption becomes a hole. |
| `HuntBound` | Does the running world obey the displacement law the gait table declares? | #825: `HUNT_DISP_BOUND_CM` had 74 cm of headroom and no reader — crossing it multiplies the linear far-mover term 428x while the digest, the selftest and the hunt referee all report that nothing happened. `Config.huntBoundLine()` is the tight check and runs in `--selftest`; this probe is the half a table cannot prove about itself, measuring what each gait actually spends (sparrow: 566 declared, 566 measured) and reporting the ledger occupancy nobody could see (peak 2, mean 0.147 at seed 42). `movers=` and. `breaks=` are both pinned, and only one is a defect count: `breaks=` is the displacement law violated, while `movers=` is the population — the gaits that actually spent displacement — so a run that measured nobody would show a moved `movers=` rather than a quiet zero |
| `OneTrace` | Does the One's death close his link the same tick? | the finale's contract after the v3 fix round: died=4284, closed=4284, `CONTRACT_HELD` `births_none=` rides the verdict since #1423: `lives` is empty when no One was born, so the loop that checks the contract does not run and `ok` stays true — the probe reported the contract held in a universe with no One, reachable by a short tick budget and no fixture at all |
| `CapSentinel` | Do awakened minds (present + wrapped) ever exceed the cap? | the treaty-restore cap breach, made permanently detectable (`CAP_BREACHES=0`, seeds 42 & 7). `samples_none=` rides the verdict since #1427, and `samples=` the census: `breaches` counts what went WRONG, which is zero both when the cap held everywhere and when no tick was examined — `CapSentinel 0 42` reported the cap intact over a universe it never ticked |
| `ArcBeats` | Does the film play, in order? | the D-036 DoD as a machine verdict (`BEATS_IN_ORDER` at 42 & 7) — and its own first run caught a wrong needle and a wrong beat order, which is what instruments are for. `beats_none=` rides the verdict since #1421: the needles are a hand-written list of eight, and an empty one leaves `ok` untouched — the film played in order having compared nothing |
| `AllocMeter` | What does the hot path allocate, really? | retired D-027's never-measured "allocation-free" row, then guarded the row that replaced it: steady bytes/tick and GC collections judged against the record's own 32 KB and 5, `VERDICT ALLOC_IN_BUDGET` (#906); `--selfcheck` runs the comparison's four cases with no universe, because at these figures the breach branch is otherwise unreachable. Then it caught itself: the 1,963-8,537 B/tick it used to report was one cold sample of a window C2 was still compiling into, and twenty-four repeats on fresh simulations converge to **365-367 B/tick at seed 42, 423-425 at seed 7, 0 GCs per arc** — the bound has ~90x of room, not 4x (#817). The old headline still prints, as `steady_max` **The B/tick figures in this row are per-JVM constants, and the row said so nowhere until #1477.** Re-measured, same class files, three runs at seed 42 and two at seed 7 on each build: `steady_bytes_per_tick` is **351 · 351 · 351 at seed 42 and 406-409 at seed 7 on Temurin 17.0.20**, and **290 · 290 · 290 at seed 42 and 348 · 348 at seed 7 on OpenJDK 25.0.1** — a 17% and a 15% gap, with `full_run_mb` moving 5 → 4 and 4 → 3 the same way. **That is the opposite shape from noise**: noise is what `cascade_bytes_per_tick` does inside one build (631/596/628 at seed 42 on 17), and this is three identical readings on one JVM and three different identical readings on another. So the *365-367* and *423-425* ranges above were a Temurin 17 measurement stated as a property of the code, and they are also stale on their own JVM by roughly sixteen bytes a tick. **The bound is what survives the difference:** `steady_budget=32768` clears every one of those numbers by ~80-110x, so `ALLOC_BUDGET`'s comparison is true on both builds while any raw median is true on one — which is why the budget line and not the median is the thing worth pinning, and why the sweep's declared exemption now names the JVM its figure belongs to (the standard `SealHygiene`'s pinned hashes set). The determinism lane runs the whole sweep on 17 and 25 since #1477 for exactly this class of drift; all sixty-three judged rows are green on both, so nothing in the lane was ever wrong — one sentence about a reported number was. |
| `SeedAtlas` | Across the multiverse, how common is the film? | 20-seed census: 17 FULL_ARC, 2 TREATY, 0 WAR, 1 QUIET (Smith can lose), 0 emergency reloads in the wild. The verdict is a seed list and not only a count — TREATY is 4 and 12, QUIET is 1 — because the row this replaces read 16 and 4 with no TREATY column at all, which folded two universes that reached peace in with the one that never went to war and lost the distinction the five verdicts exist to draw. Read at `5afa0e1` with `java -cp out:probes/out SeedAtlas 1 20 6000`, whose `ATLAS` trailer carries all five totals |
| `CensusBlocks` | Is a contiguous block of seeds a random sample of the multiverse? | **no** — the same 400 universes cut contiguously disagree at φ=5.01 (`FULL_ARC` p=0.0018) and dealt interleaved do not (p=0.72): adjacency carries information, so contiguous blocks are retired as a sampling method (census entry 5). `--selfcheck` reproduces entry 3's published z=3.10/−3.54 with no universes at all |
| `CensusSampleSize` | How many seeds does a claim need, per class of claim? | the census's cheapest mistake is to run one sweep and let every sentence in the write-up borrow its sample size. **A fraction, a median and a RANKING are three statistics with three appetites, and the third is ravenous:** an ordering of four clauses read off twenty runs reproduces the true order **18.6%** of the time, and gets merely the top category right 64.7% — at n=200 those are 87.2% and 97.8%. The gap between the two columns is why a ranking is the most expensive sentence a census can print, and it is measured rather than derived, because no closed form is worth trusting at these counts: a seeded Monte Carlo over multinomial draws (`trials=20000 seed=42`, D-010 — reproducible or it is an anecdote). The fraction law is `n = (1.96/h)^2 · p(1-p)`, reported at the worst case `p=0.5` and at any observed `p`, and the two differ by nearly half the sample (±5%: 385 worst case, 217 at p=0.17). The median law is `n = (2.4565 · sd / h)^2` and it is stated **with its assumption in the LAW line itself** — normal shape, so a heavy tail makes it optimistic, and a census that has only ever measured a range does not know its own shape. `ZERO_RULE` is the one a reader most often needs and least often knows: `k=0` in `n` bounds a rate at `3/n`, so a hundred quiet runs bound a branch at 3% and *a dead branch is only ever bounded, never proven* — the sentence is printed rather than left in a comment because it is the answer to "we never saw it, so it cannot happen". Priced BEFORE a sweep is booked, which is the whole point: a table's claims are sized in advance instead of defended afterwards. Judged on every push (`VERDICT SAMPLE_LAWS_PRICED`) and it takes no universe — pure arithmetic plus one seeded Monte Carlo, so the row costs the lane about nothing. **The verdict carries no number and that is deliberate rather than an omission:** every figure here is a function of the flags, so pinning one would pin the defaults and a reader changing `--p` would be reading a red row as a defect. `--p`, `--sd`, `--probs`, `--trials` and `--seed` all move the answer; #1479 is the finding that an unknown flag is swallowed by an empty `default` arm, so a typo prices the defaults and prints them confidently |
| `DrawMeter` | What does the rng stream spend, and where? | boot 1,728 · steady 374/tick · cascade 491 · negotiation freeze **exactly 0** — the held breath, instrumented; windows derive from the run's own transitions (`BOUNDS`), so a QUIET universe reports no cascade. Every figure but the freeze is a reading at one seed and says nothing about the next: seed 7 spends 343 steady and 514 in cascade off the same derived windows, so the unqualified `~505` this row used to carry could not have been right for both canonical universes and is 491 at this one. Read at `5afa0e1` with `java -cp out:probes/out DrawMeter 6000 42` and `java -cp out:probes/out DrawMeter 6000 7` |
| `PirateSever` | Does the wire's third ending hold — flatline, close, nothing to flush? | unit #110's DoD as a machine verdict: pirate sever + podless death with no NPE, hardline flush unchanged, a clean exit stays unkillable (`CONTRACT_HELD`). `checks_none=` rides the verdict since #1425 and `checks=`/`scenarios=` the census, both derived from the calls that make them: `scenarios=4` had been a literal, and `anomalies == 0` was the whole verdict, so a version asserting nothing would have reported the third ending held |
| `BoardScope` | Whose bodies are on the fleet's boards after a hull goes down? | #918's measurement, pinned. `Zion.pirateBoard` is the fourth walk over the fleet and the only one that does not guard `MissionState.LOST` — launch, recall and `zionLine` all do — and a sunk hull's board never empties, because `BroadcastRig.beginSession` holds the only line that takes a closed wire off a board and a LOST hull never begins another session. So the roster of every ship that has ever sunk stays in the trace metric's exclusion set until the process exits: at seed 42 with `--sink-at 4600`, `stale=3` at tick 6,000, 1,400 ticks after the Nebuchadnezzar went down. It measures the SET and not the arithmetic — the stale members are inert because `MetricsCollector.traceSuffix` keeps only `p.alive && world.isPresent(p)`, and that filter is the ONLY thing standing between a stale roster and the baseline. So the verdict pins the relationship rather than demanding zero: `lost_present=0` is the invariant the filter silently relies on, and the day it stops holding — the SmithCopy unwind path #918 argues from the code and no seed has walked, where `Remove` is id-keyed and an infected pirate's removal matches nothing — this row goes red instead of a baseline moving quietly. `stale=` rides `BOARD_CENSUS` because its size describes how many wires this seed happened to cut (#1221). `stale_none=` rides the verdict beside `lost_present=`, and the pair is the design: `lost_present=0` is the invariant the trace filter silently relies on, while `stale_none=` refuses a run that found no stale members at all — a board census over nothing must not print the line that means the scope is honest |
| `PodOptional` | Does crown #50's `pod` 0..1 ruling survive contact with all four endings? | the guard that had no keeper (#813/#849): a free-born mind and a racked one driven through the Kid's door, the treaty's door, a rig death and an unclean cut — the racked side must name its rack unit, the free-born side must say there is none, and the "untouched" cut must leave `Pod.occupied()` true. `podless=0` at every seed is REPORTED, not judged, so #121 does not break it. Re-introducing #813's bare deref turns it red on the line that lied (`POD_OPTIONAL_HELD`). **`scenarios=` and `anomalies=` ride the verdict since #1584** and the census line that carried them is gone: the row pinned one word, so deleting all eight scenarios left `anomalies=0` and the lane green — this is the first row repaired under the field rule stated above, and `scenarios=8` is exact rather than a floor because each of the eight is a named ending and a swap is a change of subject |
| `DoorPressure` | Can the inward door's two refusals be reached at all, by the door the root wired? | #886: `DoorPolicy` refuses on a full rack and on a starved budget, and the canonical arc walks neither — `DOORCLAIM` measures it (seed 42, 6,000 ticks: somebody is ashore on **2** ticks, the rack has 8 free units on both, and the budget never sells below 5 against a floor of 2). Three universes off one amnesty and two sink orders: `healthy` grants exactly `REINSERTION_QUOTA` and then says *quota spent*, `rack_full` and `starved` break one half of I-1 each and must say *no slot*. Every mutation of either comparison in `substrateSeats` passes every lock in the lane, the canonical digest included, and turns this one red (`DOOR_PRESSURE_HELD`) — the count that stood here said *seven* and the lane has been thirteen for some time, which weakened the row's own claim rather than overstating it |
| `FateAtlas` | Which births is the Kid band even willing to let out, and for how long is that answer true? | first the monoculture, enumerated over the 400 growable names: 11 needed 6 spikes, 197 needed 7, 192 needed 8, none ever landed more than 7, and exactly one cleared its own bar — `Otto Aydin` (threshold 161, window 474 = tick 4740). Then #764 keyed fate to the birth event and the domain became the births a span of universes grows: `admitted=5 distinct=5 top="Hugo Novak"x1` of 3,920 across seeds 1..20 at 600 windows, `VERDICT BAND_OPEN admitted=5`, and the same bar at `KID_BASE=144` admits 0 of 3,920 — the retune was forced, and this table is what forced it. `--sweep` is the second half (#843): the admitted count is a reading at one budget and nowhere else — 5, 161, 1238, 3813, 3920 at 600/1,200/2,400/6,000/20,000 windows, `VERDICT ELIGIBILITY_DRIFTS 5..3920 of 3920`. #843 expected #764 to make that line read FLAT and it does not: re-keying the die moves who is admitted, never the accumulator's slope, so the flat verdict is still owed to whatever rules #373's axis |
| `ConfirmationSweep` | Do D-001, D-011, D-021 and D-025's *Confirmation* clauses still describe this tree? | four scripted clauses that were prose since 2026-08-10, mechanized in one own-universe pass (`CONFIRMATIONS_HELD`): 413 originals restored, 6 minds out the door alive with null links, 60 frames at max_gap 100, 8 collections through notice→grace→ending. Its own first run printed `restored=409/413` — the delete broadcast and the treaty's door share tick 4329, and four restored originals walked out of the world again before the probe looked |
| `DistrictNeutral` | Does naming the city cost the world a die roll? | the D-048 catalog is the same six quarters at seeds 42, 7, 1 and 55, and the same again built out of a stream burned 1,000 draws down (`DISTRICTS_DRAW_NOTHING`) — #536's claim, kept |
| `DistrictCensus` | Do the city's names and its people's names still obey one law? | the join `NameCensus` and `DistrictNeutral` each leave to the other — one counts people and never reads the map, the other reads the map and never counts people. At seed 42: six quarters, three of them namesakes of a living citizen, five family names shared with 196 humans, all REPORTED, because a namesake is the naming law working rather than failing. What is judged is the part that can be wrong — two quarters wearing one name, a pool entry with a space in it (which would make every `SURNAME` line quietly wrong instead of loudly absent), and a name on either bank not drawn from `NamePool`. `off_pool=0` on both banks is what #842's one-home refactor claims, checked from outside instead of read off the source. Deliberately blind to a RENAME: change `District`'s `SALT_FIRST` and four quarters become different people with the verdict still green, which is #944's pin and not this probe's (`VERDICT CITY_CENSUSED`) |
| `BondBook` | What holds the bond book's ceiling, and does the book ever let go? | #852's own diagnosis, refuted and replaced: the book fills at t=1419 and the slots are not squatters — `evictable=0/64`, every other slot WOVEN and exempt by law. Also the `RETURN`/`STRAND` band that set `BOND_FORGET_WINDOWS`: with the clock off, returns of 160 and 208 windows apart still weave while the one real desertion runs to 479, so the symmetric 12 would have eaten two real bonds |
| `SameTick` | Is a liberation queued in tick T in the census before tick T ends? | #830: the root door promised it for six hundred commits and nothing checked — `ZION` prints every hundred ticks, so a one-tick slip reads identically on the line #187 offered as proof, and the census is outside the digest chain. The falsifiable form is `RealWorld.pendingLiberations` empty at every tick boundary (`SAME_TICK_ABSORB`, seeds 42/7 at the treaty t=4329/3747, seed 1 at the Kid's door t=4739). Armed against the refactor the door used to invite: hoist the drain ahead of the treaty block and it reads `first_late=4329 max_stranded=6 VERDICT LATE_ABSORB` |
| `BondScenario` | Does D-013's one exception hold both halves of its ruling — the death unwritten, and the edge never paying twice? | #377: the unwriting is checked per `(tick, name)` pair, so a saved mind's own flatline line on the saving tick is a break and a LATER one is not (the clause is one payment, not immunity). The refusal is not reachable in a canonical arc — at 6,000 ticks the clause fires and is never asked twice — so the row scripts 40,000 ticks under 60 daemons deployed through the ops console's own `agent` command, and only after the first miracle: 24 firings, 11 `refused — the edge is spent`, 10 stand-downs, 0 written anyway (`ONCE_PER_EDGE_HELD`). Letting `observeDeath` fall through its own exception branch turns it red on the five lines that lied (`VERDICT UNWRITING_BROKEN`) |
| `ClauseAftermath` | What does the most expensive line on the retail list actually BUY the mind it saves? | #1018: one tick. `ROOM_303_DEPOSIT` is 4,000 and #377 unwrites the death without moving the body, so the saved mind stands up inside the contact radius of the daemon that just killed it, still red, and D-002's 90/10 is re-rolled next tick. Every firing is followed by OBJECT IDENTITY — 196 minds wear 154 names at seed 42, so a name match across 40,000 ticks proves nothing — and gets one of four fates: `recaptured` (the 90), `rekilled` (the 10, written), `resaved` (the 10, and a second woven edge paid), `uncaught` (still prey when the budget ended). Seed 42 reads `saved=18 recaptured=16 rekilled=0 resaved=2 uncaught=0 median_delay=1`; seed 7 reaches the other branch unscripted at `saved=17 recaptured=10 rekilled=7 median_delay=1`. Thirty-five firings across two universes, not one delay above 1. The residual has a POSITIVE definition — same brain, same wire, same body, alive, red, held by the world — so the verdict is not the tautology `saved == sum`: a firing that leaves the hunt by some other door (walked out, worn by a Smith copy, re-jacked) counts `unaccounted` and turns the row red until that door is named (`AFTERMATH_ACCOUNTED`) |
| `CensusBeatDrift` | Is the film's timing drifting, merge by merge? | the eight D-036 beats at seeds 42 and 7, pinned in `probes/beatdrift.baseline` and judged against a declared 200-tick band (`DRIFT_WITHIN_BAND compared=16/16 band=200`). `ArcBeats` gates the ORDER and throws the ticks away, so #222's cascade — seed 7's overflow, flatline, peace, reboot and door all sliding +492 together, second birth +420 — passed the lane green |
| `LineLint` | Do the instrument lines still speak the grammar D-020 fixed? | the eight families as a runtime registry (`LineGrammar`) plus their validator: 360 instrument lines at seed 42, `families=6`, `VERDICT GRAMMAR_HELD` — an appended column passes, a renamed or moved one names itself. Six of eight is what a bare run reaches, which is what this row's closing clause always said while its own headline said seven. The two it does not reach are `PERF`, which the quiet sink never asks for, and `BIRTH`, which prints only where a chronos recorder is attached; both arrive together under `--chronos`, one `PERF` line and two `BIRTH` lines for 363 and `families=8`. Read at `5afa0e1` with `java -cp out:probes/out LineLint 6000 42`, and the chronos half with `java -cp out matrix.Main --headless --ticks 6000 --seed 42 --chronos rec.jsonl > chronos.out` then `java -cp out:probes/out LineLint --stdin < chronos.out` |
| `BirthInputs` | Can a reader holding nothing but the recording state the birth event the die was keyed to? | #847: it could not. The record carried tick, name and family; the derivation reads five facts, and the rack unit and the growth ordinal were on no line of the file. Its own scanner — a hundred lines that have never seen a `Simulation` — extracts the five off a recorded universe (`births=2 complete=2 short=0`, `BIRTH_INPUTS_COMPLETE`), and `--file` points the same reader at a recording on disk: a pre-#847 one prints `SHORT line=5 missing=rack,id` and `BIRTH_INPUTS_SHORT` |
| `UnparkStorm` | How big is one déjà vu, and what does the tick it lands on cost? | #522: at x11 the worst single-tick re-materialisation is 834 minds (seed 42, tick 4103) and 758 (seed 7, tick 4889), against a stated S6 bound of 1,000 — one region's fold, set below two. The wall figure is the surprise: across eleven runs at two seeds no unpark tick reached the quiet p99, while every run's FOLD cleared it and five cleared the quiet maximum — tick 4102 folds those 834 minds at 15.3-42.8x the quiet median against a storm tick at 0.61-3.75x. Parking's bill arrives when the crowd leaves, not when it comes back (`VERDICT UNPARK_STORM_BOUNDED`). The judged number is the mind count, which is a function of the seed; the wall numbers are reported beside their own noise floor and never judged, per AllocMeter's #916 note. `--selfcheck` drives all four verdicts with no universe, because two of them need a city that has not been grown yet. **No row in `bench.sh`** for the x11 sweep — a 6,000-tick run there is minutes of walk over ~5,260 entities, which is a laboratory's wall clock and not a lane's. The judged row it DOES carry pins two counters this catalog had never named, because `counters.sh` could not see the row at all until #1588: `worst=` is the slowest unpark observed in the sweep and `bound=` is the ceiling it is judged against, so the pair is the measurement and its standard on one line — a verdict naming only the word would leave a reader unable to say how close the run came |
| `HullRoster` | Is the hull-naming rule total, and is it still the film? | #806's array crash, made permanently detectable without a universe: 3,000 ordinals, 3,000 distinct names, all thirteen generation marks reached, and the boot three pinned as literals — nothing else in this repository notices a renamed hull, because no `matrix.zion` state reaches the digest walk (`VERDICT ROSTER_TOTAL`) |
| `FleetLines` | Does a laydown line tell the truth about the fleet it just joined? | the other half of `HullRoster`: what a hull is CALLED was pinned over 3,000 ordinals and what the line announcing it CLAIMS was pinned by nothing, so one defect reached `main` three times — #806 called the third keel "a second hull", #948 narrated a loss into a fleet that had lost none, #1056 left that same lie in the ordinal-1 arm at the SHIPPED `FLEET_MAX = 2`. Every round was found by a human reading a log. Five arms pinned as literals and 5,999 walked, no universe: the head must name the laydown ordinal, the clause must come from a declared vocabulary of four, it must say *replaces what it lost* if and only if a hull was lost, and a clause that names a board count may only appear where the census can man that count. Its domain is honest about reachability — `replacing` is `laydown > afloat()` and ordinal 0 is an empty fleet, so `(0, true)` is not walked. Restoring #1056's arm turns it red three ways on the one line that lied (`VERDICT FLEET_LINES_TRUE`). **`anomalies=` and `swept_none=` ride the verdict since #1643** and `lines=`/`ordinals=` stay on the census line: the first is what the sweep FOUND and moves with the seal (#1221), the second is the argument handed back and is not a measurement. `anomalies=` is the field the word is chosen by, and it sat unpinned while the row greped a word. **The guard closes the widest such gap in the tree** — this probe reads twelve thousand lines, `anomalies=` counts over them, and `lines=0` gave `anomalies=0` and the clean word at exit 0, so *everything is fine* and *I opened nothing* printed the same sentence (#970, #1207). Falsified both ways: a planted anomaly gives `FLEET_LINES_LIE anomalies=1`, an emptied sweep gives `swept_none=1` and exit 1 |
<!-- figure: java -cp out:probes/out SheetBench --avalanche | head -1 == AVALANCHE names=400 axes=16 mean_bitflip=0.5001 max_axis_corr=0.0969 pair=HUMAN.disbelief~integrity corr_bound=0.15 VERDICT PASS -->
| `SheetBench` | What does the character kernel derive, and is its mixer worth believing? | the two questions the parked kernel's review left as homework, answered with numbers instead of assurance: strict avalanche on the finalizer reads `mean_bitflip=0.5001` over 400 names × 16 axes × 32 bits, and cross-axis Pearson tops out at `0.0969` on `HUMAN.disbelief~integrity` under a stated `0.15` bound, printed beside the measurement because a verdict without its threshold is a claim. `--discipline` is the other half: a HUMAN asked for `replication` throws and the message names the vocabulary it does have (D-042). Building it also caught a byte bug in the bench itself — this box's JVM reports `file.encoding=ANSI_X3.4-1968`, so an em dash in a verdict line was silently becoming `?` and a line quoted in a PR would not have been the line another box prints, which is why this probe sets its own UTF-8 stream and why #836 exists for the rest. The kernel is imported by nothing in the domain, so this bench is the only place its numbers exist. `--boot-version` (#1270) is the one mode here that builds a universe, and the exception is the point rather than a compromise: every other mode is identity in, fate out — no seed, no draw, no `Simulation` — so the one question that cannot be answered without a world is the one place a world is constructed. What it guards is a single row. Since #1260 the Architect's `versionFatigue` is read from the world's counter, and this bench has no world, so it names `BOOT_VERSION = 6` while `SheetDump` asks `world.version()`; move `World`'s initializer and the two censuses disagree by exactly that row, both green, neither complaining. Falsified by doing it: `bench=6 world=7`, exit 1. **`--avalanche-selfcheck` gives both bounds a falsification row (#1092).** `BITFLIP_TOLERANCE` had exactly one reader — the line it bounds — and nothing in the tree would notice a wrong value: set it to `0.5` and the mixer must be perfect, with the sweep going red for a reason no line explains; set it to `0.4` and everything passes forever. The value is correct today and nothing established that it was, which is #1002's argument about the beat band one instrument over — *a declared convention, not a measured tolerance*. Neither bound can be DERIVED, so the cheaper half of #1092 is the one taken: `cases=` synthetic pairs each bound must refuse, `failed=` those it did not, and both constants ride the verdict — `bitflip_tolerance=` and `corr_bound=` — so a moved bound is a moved line. The cases are written as offsets from the constants, not as literals, so tightening a bound moves them with it. **The boundary is deliberately not on the line:** `0.5 + 0.01` is `0.51000000000000000888…` in binary doubles, so `|x - 0.5| <= 0.01` is FALSE at what a reader would call the edge — the first draft asserted the boundary passes and went red on two cases, which is not a defect in the bound but the bound applied to a number that cannot be written exactly. The cases sit at `± tolerance × 0.99` and `± tolerance × 1.01`. The measurement and the suite share one predicate, so the bound watched refusing a synthetic figure is the bound applied to the farm. `bound=` on the AVALANCHE line is `corr_bound=` now: it was `CORR_BOUND`'s alone and read as global, a tolerance fifteen times looser than the one actually applied to `mean_bitflip`. **The legs line is falsified too since #1556**, and it needed to be: #959 added it so a reader could see WHICH conjunct failed, and at real figures both legs pass — so the FAIL text on each leg had been printed by no run and no case in this repository. That is #1092's own argument applied one layer up, to the thing a person reads at the moment being told the wrong leg costs the most, and it is the shape `advice.sh` met in #1358 where two counters had no branch and the chain printed a defect report naming the wrong defect. Four combinations — both pass, bitflip only, corr only, both — with the two leg words asserted SEPARATELY from the verdict, because a line that gets the verdict right and the legs swapped satisfies any check that only asks whether the run passed, and swapping two `?:` arms in one format call is exactly the edit nobody re-reads. **The comparison has one home since #1563.** `Math.abs(mean_bitflip - 0.5) <= BITFLIP_TOLERANCE` lived in three places — the measurement, the predicate the suite drives, and the legs formatter — two of them arriving on one day, each for a good reason, which is the shape #1512 met with three comment strips written an hour apart and #1192 met with a probe list kept beside the bench table. The obvious fix is wrong: passing two booleans into the formatter would make the four leg cases assert FORMATTING and never the judgement, so a caller handing over the wrong pair would satisfy every one of them. An `AvalancheVerdict` record derives both legs from the measurements exactly once and everything downstream reads it — the suite drives `of` and asserts a judgement, the formatter takes the record and the numbers it prints, and `--avalanche` spends one derivation on two lines and an exit code. Both printed lines are byte-identical to what they printed before |
| `NeutralDiff` | Did the control group move, and at which link? | the permanent-NEUTRAL ruling's measuring stick (#212/#537): the NEUTRAL lane's chain against the sealed baseline, byte-equal link by link, `NEUTRALDIFF 60/60 byte-equal VERDICT PASS`. Its first real case was the seal on the open lane branch — `--seal` at `unit/336-neutral-forever`'s committed fixture names tick 500 as the first unequal link and 4/60 links equal, so that branch's rebase has to reseal or land a lane that is red on arrival. #528's fixture is not in `main` yet, so the bare `NeutralDiff 6000` prints `seal_missing` and exits **2** rather than passing vacuously: a referee with no fixture is red, and a length mismatch is reported only after the links the two chains share, because the moved link is the one that says where the world changed |
| `DocLint` | Do the documents still say what the tree says? | the one probe pointed at the repository instead of at a universe. Six questions: the ADR front matter, the `DECISIONS.md` emoji and the `ROADMAP.md` gate cell agree per D-number; the same three name the same PHASE, the roadmap's section heading being the one D-039 schedules units against (#957: seven Season Three decisions read v6.0 in the index and v6.5/v7.0/v7.5 in the roadmap); no accepted record still claims, unlabelled, to be awaiting a verdict; every record carries a `### Confirmation`; a missing D-number is explained; and README's pinned `main` beat column equals a live `ArcBeats.measure` — one scan, two readers, no second copy of the needles. All three hand-repairs it replaces were invisible to every other lock: #907's five decisions carried two statuses for a day, #903's four beats were 420 ticks stale, #957's seven decisions each named two phases at once. `--selfcheck` breaks a canon of its own seventeen ways and demands that each break move exactly one counter, so `DOCS_TRUE` is a verdict that has been seen to say no (`VERDICT DOCS_TRUE`). **`gaps=` rides the verdict since #1627**, and `unannotated_gaps=` was not enough. A hole with no explanation in the index has always been red, because `unannotatedGaps == 0` is one of the eight conjuncts in `docsTrue()` — so the check caught CARELESSNESS. A hole WITH an explanation has always been green, and writing that sentence is exactly what a person creating the gap would do, so it never caught HABIT. `docs/DECISIONS.md`'s claim is about habit: the D-055–D-057 gap is *a numbering artifact, not three lost decisions*, and a fourth annotated hole would leave that sentence saying `three` while the artifact became a practice. This is #1623's argument a second time — a gap is a deliberate act, a number claimed in a commit subject whose record is never written, so the pin costs an author one more line rather than teaching them to edit a number that drifts on its own (#1221 is about the second kind). Falsified both ways: move `docs/adr/D-058-*.md` out and the verdict reads `gaps=4`, write a `D-056` record and it reads `gaps=2`. The `DOCS_DRIFT` above the census carries no field and stays that way — it is the parse-failed path, where there is no census to report and a number printed there would be an invention. **And the SET rather than the count since #1629**, which is the objection #1628's own review raised against it: `gaps=3` says three numbers have no record and nothing on that line says which three. A world where D-055 gains a record and D-062 is claimed and never written prints `gaps=3` and a green row — #1550's swap, in the unit whose whole argument was about counts. Falsified with every other counter held equal, which #1628's transcripts could not do: give D-056 a record, retire D-058's, and edit the index row to follow, and the probe prints `records=59 index_rows=59 gaps=3 unannotated_gaps=0` and `VERDICT DOCS_TRUE gaps=D-055,D-057,D-058` — identical everywhere but the set, red on the row. It is PINNED rather than parked on a census, and that departs from `LEAVE_MEMBERS` and `VACUOUS_MEMBERS` on purpose: those are populations of probe names that change whenever anybody writes a probe, and a member list inside an exact-line grep is a list every unit edits (#1192, #884). This one is three numbers that are never supposed to change again, since a D-number is a citation key and reusing one makes an old citation ambiguous — a frozen list is precisely the list that belongs in a row. The count stays on the census, derivable and in one place, which is the redundancy #1624 kept and named. **The set got cases with #1631**, and until then its only evidence was a hand-made transcript in a pull request. `--selfcheck` asks `moved()` which COUNTER a break moved, and the set is not a counter — it is the field #1629 added because a count cannot see a swap — so it needed its own assertion on the exact string. Three cases: the fixture as it stands (`D-003`), the SWAP that #1629 could only demonstrate by editing three files in the live tree (D-002's record becomes D-003's, index row and annotation follow, and `gaps=1 DOCS_TRUE` before and after — byte-identical to a clean run under the pre-#1629 verdict), and `none`, the empty spelling that is unreachable in this repository and always will be, since there is no world with zero gaps in a tree that has three (#1550 is why the branch exists, #1207 is why an unrun branch matters). The record IDs in the fixture canon became mutable to make the swap expressible at all — a literal record list can add a gap and cannot exchange one. Both cases are shown to FAIL, which is the harness's actual demand: hard-coding the gap id breaks `gap-set-swap` alone, and dropping the empty spelling prints `got=gaps:` and breaks `gap-set-none` alone. **The suite got a floor with #1633, and it is an exact count rather than a floor.** The row grepped one word — `SELFCHECK VERDICT DOCLINT_FALSIFIABLE` — so deleting every case in the file printed `cases=0 broken=0` and that word, and the lane was green. Third instance of one shape in eight units (#1615's summed floor, #1623's deleted figure marker) and the worst place for it: a suite that exists to prove a checker can say no is a suite whose emptiness is indistinguishable from its success, and `broken=0` over zero cases is the literal sentence *nothing disagreed with me* (#1207, #1373). `cases=` and `broken=` moved ONTO the verdict and the census line that carried them is gone rather than duplicated, which is the redundancy #1624 kept and named. Exact rather than a floor, because a floor cannot see a swap — three cases leaving while three arrive is #1615's finding — and every case here is a named break, so an exchange is a real change of subject. Falsified both ways: delete `gap-set-baseline` and the verdict reads `cases=36`, add one and it reads `cases=38`, and the row is red for each |
| `ExitGrammar` | Does `$?` mean one thing per value, and where it does not, does the tree say so? | #1219, deferred by four units and then answered in the smaller half. Three populations here exit with codes a script branches on — the probes, the twelve tools, the teleprinter — and #1241 made 2 universal for *the invocation was refused*, in a table whose own opening sentence names all three. The probes spend 2 for `NEVER_AROSE` and have since #1204. **Both documents asserted the collision was gone:** this page said *the two families of program in this tree agree where they overlap*, `Probes.Outcome.REFUSED` said it in the same words, and both were written when `REFUSED` took 3 to match `DreamReader`'s `EXIT_USAGE`. `DreamReader` is ONE program carrying a five-code grammar of its own; the shell tools are eleven, and their agreement on 2 is the largest in the tree. Matching the smaller incumbent moved the collision rather than closing it — 2 is NEVER_AROSE here and a refusal there, 3 is a refusal here and *the answer could not be read* there — so two values now carry two meanings each and two documents said one. What this judges is narrow on purpose: the grammar block on this page matches `Probes.Outcome` exactly in BOTH directions, and every code the two families spend differently appears in the `<!-- grammar-boundary: … -->` marker. Both directions again, because a declared code that has stopped colliding is a rule protecting nothing, and that is how a boundary outlives the collision it was drawn for — #1337's finding, one file over. Unifying the grammars is the other repair and it is a DECISION about the bench's contract, whether `NEVER_AROSE` gives up 2, which #1219 frames as the Architect's and not a probe's. `literals=` is JUDGED since #1345, at the one moment a gate costs nothing (#1311's argument, one directory over). It was a census while the fourteen call sites writing a digit instead of `Outcome.code()` were an unread population — telling a refusal from a never-arose means reading each site, and a checker that guesses is what this directory refuses. #1345 read them: six were REFUSALS spending 2, which in this family means NEVER_AROSE, so `bench.sh` counted a refused invocation as a world with nothing to judge and did not fail the row; two more spent 3 for a sweep that did not finish, a break wearing the refusal's number; six were honest breaks written as `1`. At zero the gate stops the next probe that writes a digit; at one it demands a unit from whoever trips it, which is how a gate gets argued about instead of added. `System.exit(held ? 0 : 1)` is a COMPUTED code and stays out: the two-valued form is the honest spelling for a probe whose whole answer is pass-or-fail. `--probes-doc FILE` is the falsifier: pointed at an empty file it reports four drifts and two undeclared collisions. `doc_drift=`, `undeclared=` and `checked_none=` are the three judged counters beside `literals=`: the grammar block and `Probes.Outcome` must match in BOTH directions (`doc_drift=`), every code the two families spend differently must appear in the boundary marker (`undeclared=`), and a run that read no code at all must not report that nothing drifted (`checked_none=`) **Comments are stripped before the literal scan since #1503**, and until they were this was the only checker in `probes/` reading exit spellings without doing so — the oldest bug in the shop, one directory over, where `advice.sh` has matched its own comments (#1157), a neighbour's catalog row (#1222), its own suite fixture (#1265) and its own `exit` pattern (#1276). It cost a build: a javadoc sentence explaining what a reading cannot see — *or spelled `System.exit(3)` as a literal* — was reported as `GRAMMAR_LITERAL … leaves with the digit 3` in a file whose only exit is `Probes.Outcome.REFUSED.code()`. **The pressure was on the prose, which is the wrong end:** this checker's subject is where the exit grammar LIVES, so the files most likely to discuss exit codes are the files most likely to be about it, and the check got least usable exactly where it was most relevant while the workaround available to an author was to write a worse sentence. Both directions are driven — a comment naming `System.exit(7)` gives `literals=0` and a real one in a method gives `literals=1` with the file named — and the strip takes block comments before line comments, because a `//` inside a block comment belongs to the block and stripping lines first orphans its opener. Line comments go from the marker to the end of the line rather than the whole line, since a real exit can share a line with a trailing comment; the reverse case, a `//` inside a string literal, does not occur here and would only ever HIDE an exit, which under-reports rather than inventing a finding. |
| `DocsRoster` | Does every document in `docs/` get named by another one? | the fourth catalog's reader (#1190). Three got one in a single day, each after a person found the gap by hand: the bench table (#1162 — a probe on nobody's push), `probes/README.md` (#1177 — five probes with a bench row and no catalog row), `tools/README.md` (#1189 — two tools in the lane and in no document). `docs/` had none, and `DocLint`'s seven questions do not include *is this file discoverable at all*. The answer today is that all sixty-four are named, which means what `SheetFence`'s first green run meant: a directory is tidy the way an empty room is tidy. D-058 made `docs/spec/` a sanctioned document kind, and a sanctioned kind nothing links to is exactly the shape that grows an orphan. NAMED, not linked — this tree has no single index for `docs/` and inventing one is a decision rather than a probe's business, so the question is whether some OTHER document mentions the file's name. A file naming itself does not count, and that is load-bearing: every ADR record carries its own D-number, so a self-match would make every record its own reference and the count would read 0 forever by construction. An orphan is RED rather than tolerated, unlike `bare=` (#1149) and `unfalsifiable=` (#1160) — both of those exist because a real debt was on the books when the check landed, and here the first run found none, so there is nothing to tolerate and a counter that tolerates nothing is a verdict wearing a softer word. `ROSTER_DOCS files=` carries the population off the verdict line (#1221); `scanned_none=` keeps an empty scan red. `orphans=` is the judged counter — a document in `docs/` that no other document names — and it is RED rather than tolerated, unlike `bare=` and `unfalsifiable=`, because the first run found none and a counter that tolerates nothing is a verdict wearing a softer word |
| `SheetFence` | Is the sheets-cached-nowhere law still true, and is there still exactly one door? | three sentences of #350's law, given a reader for the first time (#656): no wing holds a `Sheet` field, `matrix.character` imports nothing of the domain but `core`, and nothing in the layer keeps a derived sheet. All of it was already true the day this landed — `stored=0 foreign_imports=0 cached=0 door_missing=0` — which is the exact shape this tree has now caught five times elsewhere (#1203, #1212, #1210, #1233, #1243): a law that is true by luck reads identically to a law that is enforced, until the first commit that breaks it. It is the STATIC half of the pair; `SheetDump`'s `cached=` is the dynamic half, walking the live heap from the composition root, and neither subsumes the other — a sheet stashed in a `static` map that no reachable field points at is invisible to the walk and plain to the grep, while a sheet parked at runtime in an `Object` field is plain to the walk and invisible to the grep. That sentence was a claim before #1256 and a fact after it: the third counter read `SheetDoor.java` alone, so the very static map it names as the grep's own territory would have been missed by BOTH halves — the walk cannot reach a static from the composition root, and the grep was not looking at the file. It now reads every file in the layer and asks whether a field's declared type mentions `Sheet` at all, because `Map<String, Sheet>` keeps a sheet exactly as `Sheet` does. Comments are stripped before every count, because `SheetDoor`'s javadoc says `private final Sheet` out loud while explaining why no such field may exist — the self-matching shape `advice.sh` was bitten by four separate times. `--crossings` (#1255) gives `SheetDoor.crossing` the reader it landed without: #656's third clause — *the sheet crosses the jack as a hash, never a reference* — shipped as one definition and zero call sites, which is dead code wearing a javadoc, and an unfalsified fold is a wrong constant or a values-order bug waiting for the first real crossing. Four pinned numbers, one per family, SYSTEM crossed through its own door at a named version because its fatigue axis is read rather than folded — and the label carries `@6` for exactly that reason, since a SYSTEM crossing with no version in its name is a number nobody can reproduce. The first draft pinned four invented values and went red on all four, which is the correct way round: the numbers come from the fold, and the fold is now the thing that cannot move quietly. **`adapters=` and `impure_adapters=`** arrived with #658 and are the reason that unit's *world_reads=0* is a check rather than a claim: a wing that answers for a sheet must reach `SheetDoor` and read nothing world-shaped — no world, no tick, no pod, no link, no pill. Three wings answer today (`Human`, `Program`, `MachineCity`) and the census counts them. The reading is SHALLOW on purpose: three lines after a `Sheet sheet()` signature, asking whether the door appears and whether anything world-shaped does. A deeper reading needs a parser (D-009) and a longer window starts matching the next method — so the rule is also a rule about shape, *a wing's `sheet()` is one expression*, and a longer one is refused. Falsified both ways against a scratch wing: `SheetDoor.at(name + pod, …)` reads the slot, `Sheets.derive(name, …)` bypasses the door, and both print `impure_adapters=1`. `--wings` and `--layer` point it at a scratch tree, which is how the counters were seen to go red before the row was added. The domain is a DENY-list since #1258 — everything under `src/` except the layer — because the allow-list of four directories it shipped with did not include `src/matrix/core`, which is the package `SheetDump`'s own javadoc reaches for as THE example of the defect ("1 the moment a `Sheet` is parked on `World`"). `entities/eco` and `entities/behavior` were covered by luck rather than design, since `Files.walk` recurses. `FENCE_CENSUS swept=` carries the population on its own line and `swept_none=` rides the verdict: a scan that read zero files must be red, and the COUNT must not be in an exact-line row, or the lane goes red on the day somebody adds a domain class and the number becomes a thing people edit until the lane is quiet (#1221, learned twice in one afternoon by `LeaveContract`). `checked=`/`drifted=` are the `--crossings` mode (#1255): four pinned folds, one per family, and a drift is a wrong constant or a values-order bug in a door that had shipped with zero call sites |
| `DocFigures` | Does a figure written in a document still match the command written beside it? | #1082's gap, closed for the figures that opt in (#1283). Three documents here have carried a number that was true when typed and false when read — `LedgerMirror`'s javadoc (#1130), this file's build block twice over (#1192, #1279, #1284) and the determinism sample (#1277) — and every one was found by a person reading. A figure that wants checking declares itself in a comment the rendered page does not show: `&lt;!-- figure: <command> == <expected> --&gt;` — written with escapes HERE because the probe reads this file, found its own catalog row on the first run, and reported the word `<command>` as a program not on its whitelist — one of clause 8's instances, listed there rather than counted here (#1335). The command runs, its last line is compared, and a mismatch is red. THE DOCUMENT OPTS IN, and that is the whole design: a checker guessing which fenced block produces which nearby number would report phantoms, which is worse than reporting nothing, so an unmarked paragraph is prose this probe has no opinion about. The first two markers found a live one — `tools/README.md` said *eleven tools* against twelve on disk. What may run is a whitelist of first words (`ls`, `find`, `grep`, `wc`, `git`, `cat`, `sort`, `head`, `tail`, `sed`, `awk`, `basename`, `java`) and everything else is REFUSED and counted, never skipped — a silent skip is how a checker covers nothing while printing green (#1207, #970). The whitelist is not a sandbox: the text goes to `sh -c` in full, pipes and all, because these figures are pipelines by nature, and that is safe only because the documents read are this repository's own. Pointing `--docs` at a tree you did not write is running that tree's shell, which is stated here rather than discovered. **`FIGURE_CENSUS … secs=`** arrived with #1302, and it exists because two of the markers run the daemon — a 4,500-tick and a 20,000-tick simulation, added by #1300 — which took this probe from about a second to five with nothing but the sweep's total to show it. This is the probe most likely to grow expensive markers, since a slow figure is exactly the kind a reader most wants pinned, and the bench has no per-probe budget. On the census and never the verdict (#1221): a timing on an exact-line row goes red on a slow box, which teaches people to edit the number. **WHAT THE SURVEY COVERS, AND WHAT IT DOES NOT** (#1342): #1337 read every spelled-out quantity in `probes/README.md`, `tools/README.md`, `probes/bench.sh` and `README.md`, and the file list was itself an unguarded claim — nothing said why those four or what was left out, and a survey that does not state its own boundary reads afterwards as EVERYTHING WAS CHECKED. `.github/workflows/locks.yml` was outside it and carried three more, one of them the fourth copy of the sentence #1337 had just corrected. It is still outside, and now on purpose: a `# WHY:` block is an ARGUMENT for a step, and marking arguments is how every integer in the tree becomes a claim — which is precisely the rule #1337 declined to make. The repair there is the same one made here, in prose: a figure whose producer is the very line below it does not want a marker, it wants deleting. What this probe reads is `--docs`, the whitelist of documents named on that flag, and nothing else. **Five documents since #1625, and two of them had never been read at all.** `checked=` being pinned says nothing about figures that were never written, and a probe reading only the documents that already carry markers cannot notice a document that carries none — `PRINCIPLES.md`, `ROADMAP.md`, `docs/ARCHITECTURE.md` and `docs/DECISIONS.md` were all outside the population. `docs/DECISIONS.md` is the sharpest countable pair in the tree: a table of fifty-nine decisions beside fifty-nine `docs/adr/D-*.md` records, never once checked, and it HOLDS — which makes this the #1203 case rather than a repair, a law true by luck reading identically to a law enforced until the first commit that breaks it. `PRINCIPLES.md` pins its three numbered families (`A`, `Dev`, `Ag`), since a principle renumbered out of sequence is how the D-055–D-057 gap happened one file over. `ROADMAP.md` and `docs/ARCHITECTURE.md` stay OUT: #1342 drew that boundary and it holds — a marker is for a number whose PRODUCER is a command, and those two are prose about intentions. **`checked=` MOVED TO THE VERDICT WITH #1623**, because opting out was free and silent: delete the marker on `tools/README.md:191` and the probe prints `checked=4` on the census and `stale=0 refused=0 checked_none=0` on the verdict, which the exact-line row matches — a figure stops being checked and nothing says so. That is the opt-in design paying its own cost, and it is #1207 and #1373 under a third name. #1302's argument for keeping `secs=` off the verdict does not transfer: a timing moves on a slow box for a reason nobody chose, while a marker count moves only when a person edits a figure comment in a diff — the `LEAVE_BY_HAND` case, where the pin buys not correctness but one more line an author must write with their name on it. Falsified in BOTH directions, since a floor that only catches deletions is a floor: removing a marker prints `checked=4` and adding one prints `checked=6`, and the row is red for each. `docs=` stays on the census — `--docs` takes a list, so it is an argument of the RUN and not a property of the tree, and a caller asking a smaller question would go red for having asked it. `checked_none=` is now derivable from `checked=` in this row and is KEPT, because the verdict must still be honest when the probe is run by hand with no row to grep. `stale=` is how many marked figures disagree with the command beside them and `refused=` how many could not be run at all — two different failures, kept apart because a figure whose producer errors is not a figure that has drifted, and both were unnamed here until #1588 made the row visible to `counters.sh`. |
| `SheetDump` | What did every soul in this universe derive, and does the same seed derive it twice? | the census, printed: 681 rows at seed 42 boot — 10 cast, 196 humans, 473 programs, the Matrix, the Machine City — with `--cast`, `--wing <family>`, `--system` and `--all` so a reader can ask for one wing instead of the city. Two laws ride the exit code rather than the prose: every mode renders the census TWICE from two universes and byte-compares before a line is printed (an identity hash smuggled into a derivation prints `DRIFT line=3` and exits 1), and `cached=` is a heap walk from the composition root through `matrix.*` fields, arrays, collections and maps, so the sheets-cached-nowhere law of #350 is a number and not a promise — 0 today because the domain imports nothing from `matrix.character`, and 1 the moment a `Sheet` is parked on `World`. Its own first run found the empty wing — `--wing MACHINE` printed `souls=0`, because the machine side is singletons and statics and a sheet derives from an identity string none of them carries — and #1010 closed it the way the SYSTEM row was always closed: the city answers for itself, `SHEET the Machine City [MACHINE] power=10 precision=10 relentlessness=1`, one row minted with the name spelled once and forever, since respelling a name re-rolls the sheet attached to it. Residents (sentinels, harvesters, as catalog data under D-015) append to that wing when a verdict grows one; they do not move this row. **`--stolen` is the fence** (#660): a `SmithCopy` wears a victim's face, is a `MatrixEntity` and never a `Program`, so the PROGRAM wing's loop never sees one and every copy in the city derives nothing. At seed 42, 4,000 ticks: `copies=407 resolved_through_fence=288 unanswerable=119`. ONE fence is the entire design — two would be a second bridge and D-013 permits one — so `branches=` is counted by reading this probe's own source rather than asserted, and the needle is assembled at runtime because the first draft matched twice: the fence, and the line searching for it (clause 8's seventh instance, repair 1). The 119 are `Avatar` victims: the avatar is a mind's body in the dream and the mind answers on the real side (#658), so they are counted as unanswerable rather than guessed at — guessing would be inventing a fifth family. The Decorator itself is untouched, which is D-001's whole point: a `sheet()` on the copy would be a second answer for one identity, and deleting the Smith must snap the original back type-guaranteed. `checked=`/`of=` are the `--catalog` mode: four wings compared against the catalog, and the pair rather than a bare count because *four checked* means nothing without *of how many*. `branches=`/`unresolved=` are `--stolen`: ONE fence is the whole design, so `branches=` is counted by reading this probe own source rather than asserted, and `unresolved=` is the 119 Avatar victims counted rather than guessed at |
| `SystemFatigue` | Does the Matrix's fatigue axis read the counter, or invent a number? | the one axis in the whole grammar that is READ rather than derived, pinned (#661). Before it, the mixer folded `"the Matrix"` and produced `versionFatigue=1`, so the census reported a fresh install of a world on its sixth version — a number that was stable, reproducible, byte-defined and wrong, which is the failure mode a derivation cannot see: a derived value is correct by construction about the string it was handed and says nothing at all about the world. Three claims ride the exit code. `boot=6` is the counter, read at a universe the probe builds itself. `reboot=7` is the sheets-cached-nowhere law (#656) having an observable consequence rather than only a grep — bump the version, ask again, and the answer moves on the same page because there is nothing in between to go stale. `v99=10 v0=1` pins the BAND, which is the claim most worth pinning: an unbounded counter meets a 1..10 stat somewhere, and `Math.floorMod` — the banding every other axis uses — would make v11 read 1, reporting a system on its eleventh reload as brand new. `derived_intact=true` is the guard on the other side: `stability`, `tolerance` and `authority` still come from the name, so an override that reached past its own index is red. The reboot runs on the probe's own universe (clause 2) and queues nothing. `bypass_refused=true` arrived with #1260: #661 gave the SYSTEM row a correct answer without taking the wrong one away, so `Sheets.derive("the Matrix", SYSTEM)` stayed public and stayed wrong, and this probe called it deliberately — which made the wrong call a documented technique. `Sheets.derive` now refuses SYSTEM and names the door in the message; `Sheets.fold` is the package-private machinery underneath, with exactly one caller. The intact-axes control became a SECOND CROSSING OF THE SAME DOOR at a different version, which is the better test regardless: the old form compared the door against the bypass it was meant to replace, so it would have gone green on the day somebody made the bypass authoritative |
| `TwoWorlds` | Are two universes in one JVM actually two universes? | #1147's confirmation, and its SHAPE is the argument: "it did not throw" would have passed on main for seed 42, which produced a different chain at link 1 with no exception at all. Each seed runs alone, then all of them run concurrently, and the chains must be equal LINK FOR LINK (`WORLDS_INDEPENDENT`, four seeds, 2,000 ticks). The defect it guards was two singletons holding their neighbour scratch buffer as an instance field — one list per process wearing the syntax of one list per object. `diverged=` is the judged counter — two chains that must be equal link for link — while `ticks=` and `worlds=` are the shape of the experiment, pinned because a run of two worlds proves less than a run of four and the row would not otherwise say which was run |
| `SharedState` | What does one universe share with the next in the same JVM? | the other half of `TwoWorlds`: that one measures behaviour at four seeds, this one reads the tree, so a share seed 42 never exercises is still named. Three shapes — a mutable static, a final reference to a mutable object (`static final int[]` is a constant in name only), an instance field on a singleton. Twelve sites, all safe, all declared `read-only` / `process-wide` / `per-thread` in the comment above them (#1148): the rule is not "no sharing", it is that a share SAYS SO where the next person reads it. `undeclared=` is the judged counter: a share that does not SAY SO. Twelve declared sites keep it at zero, and the number is the rule rather than the population — a thirteenth safe share moves nothing, a thirteenth silent one moves this |
| `LatticeFence` | Is the dependency direction still the direction, and does anything check? | `docs/ARCHITECTURE.md` states three structural laws and names the instrument that keeps them — *"Dependency direction is law, **verified by grep**: `entities` imports nothing from `realworld` …; `World` holds no real-world objects; nothing depends on `Main`"* — and there was no grep. Nothing in `probes/`, `tools/` or the workflows read any of the three, and all three were true, which is the whole reason this exists: `SheetFence`'s shape, a law that is true by luck reading exactly like a law that is enforced, found five times elsewhere (#1203, #1212, #1210, #1233, #1243). This is the sixth and the widest — the other five guarded one wing or one field, and these three sentences are the shape of the whole domain: the thesis this repository demonstrates, *the mind is never uploaded*, IS the `entities`/`realworld` split, and nothing would have noticed the import that ends it. **Clause 2 is shallow and says so:** *`World` holds no real-world objects* is not an import question — a field typed `Object`, or a collection of a shared supertype, holds one without naming one — so what is read is each field's DECLARED TYPE, which catches every way this law has ever been broken here and does not pretend to catch a laundered one; a deeper answer needs a parser and D-009 refuses the dependency. Comments are stripped before every count, because `src/matrix/package-info.java` says `{@link matrix.Main}` out loud while explaining what `Main` is for — documentation, not a dependency, and a checker finding its own subject is what bit `SheetFence` four times. `swept=` and `world_fields=` ride `LATTICE_CENSUS` (#1221) while `swept_none=` rides the verdict, since a reading that opened no file must not print a tree's clean line. `--root DIR` points the whole reading at a scratch tree, which is how all three clauses were seen to go red — and the empty tree with them — before this row was added. `entities_reach=`, `world_holds=` and `main_depended=` are the three clauses, one counter each, so a red row names which sentence broke rather than only that one did |
| `DoorRefusal` | Does every probe that parses a long option refuse one it does not know? | the convention was real and unwritten: **fifteen of twenty-six** flag-parsing probes left with `Probes.Outcome.REFUSED` on an unknown flag, five swallowed it, eleven died on it, and nothing in the tree said which group a new probe belonged in — `SheetFence`'s shape for the seventh time, a rule kept by habit reading exactly like a rule that is enforced right up until the first file that is not. **It runs its siblings rather than reading them, and that is forced:** a refusal is spelled at least three ways here — a `default` arm, an `else` after a positional collector, an `args[0].equals` guard that ignores everything else — so a grep for any one misses the other two, while the BEHAVIOUR is decidable by handing the probe a flag nothing could know. **The timeout is the apparatus, not a timing assertion:** a probe that swallows the flag does not fail, it RUNS, and some of these build twenty-thousand-tick universes, so each child gets a bounded wait and a child still alive at the bound is a finding rather than a hung sweep — the bound is generous by two orders of magnitude against a refusal, which returns before the JVM has warmed. **Three outcomes and not two**, which is why the first draft was wrong: eleven probes leave 1 by throwing `NumberFormatException` on a flag they read as their positional tick count, and that is neither a refusal nor a swallow — nothing ran — so the discriminator is whether a VERDICT LINE was printed, since a probe that reached its work printed one and a probe that died at its door did not. `swept=` and `refused=` ride `DOOR_CENSUS` (#1221) while `swallowed=`, `crashed=` and `swept_none=` ride the verdict, and `crashed=` was on the census while it read eleven, because #1311 forbids installing a gate at one — at one it demands a unit from whoever trips it — and #1481 took it to zero, so #1485 moved it to the verdict where it can be judged. The distinction #1221 is actually about is the one that move turns on: a POPULATION is how many things there are and a FINDING COUNT is how many of them are wrong, and only the first belongs on a census. One verdict word covers both findings honestly — `EVERY_DOOR_REFUSES` is false whether a door swallowed the flag or died on it, since neither refused — while the two counters keep apart what a reader needs: a swallow means the probe RAN and judged a question nobody asked, a crash means it stopped and blamed the contract. The keeper needed no edit when the eleven were repaired, because a probe that starts refusing properly moves from `crashed` to `refused` on its own. **The order of the tests is a correction:** `NeutralDiff` refuses articulately — `NEUTRALDIFF FAIL bad_argument text="…"`, a FAIL verdict, exit 3 — and read verdict-first the most explicit refusal in the tree classifies as a swallow, so the refusal CODE is asked before the verdict line. Clause 8 with the subject being itself: a probe judging whether its siblings refuse an unknown flag, that did not, would be the tenth instance of a checker exempting itself. The population is read from the tree, generously — a source that so much as mentions a `"--`flag is swept, because being asked costs one JVM start while a door this reading MISSES is the mistake that matters. `--root DIR` points the sweep at a scratch tree; `--list` prints the refusing doors beside the offences, since a run that names only breakage cannot be read as a census. What it cannot see is a probe that refuses for the wrong reason: the verdict says `swallowed=` and not `refused_correctly=`, and which code a refusal owes is #1372's question one population over **The population is read from CODE since #1510**, and until it was, `swept=` counted twenty-five doors and one sentence: `ConfirmationSweep` was in it on the strength of a javadoc quoting D-021's Confirmation verbatim — *"--follow on a v1 run emits at least …"* — a sentence about somebody else's flag, in a probe with no long-option door at all. It refuses through #1481's positional reader rather than through a door, so the verdict was right for the wrong reason. That is #1503's finding one file over and one hour later, in a probe written the same day: `advice.sh` has met it four times (#1157, #1222, #1265, #1276) and `LatticeFence` strips comments because `package-info.java` says `matrix.Main` out loud while explaining what `Main` is for. **The strip narrows the population and cannot narrow the check**, which is what makes it safe — a door is code, so nothing stripping comments removes could have been one — and the generosity that matters is kept, since a comparison, a `case` arm and an `args[0].equals` guard are three spellings and the reading still takes any quoted flag rather than trying to recognise a parser. **`no_door=` is the denominator, and it arrived two units late (#1544).** `swept=26` beside a verdict word reading `EVERY_DOOR_REFUSES` reads as *every probe*; `sources()` walks fifty-five files and twenty-nine of them parse no long option at all, so the sweep judges under half the directory. `OrderTable` was one of the twenty-nine, and a typo in either of its positional arguments left with **1** — this family’s word for *the contract broke* — through an unhandled `NumberFormatException`, with a tick budget below one digest period dying on `ArrayIndexOutOfBoundsException` the same way. That is precisely the defect this probe exists to prevent, in a probe it has never run (#1536 repaired it). The two counters ADD UP to the population by construction — `swept + no_door` is every source `sources()` returned, which is every probe but the shared helper and this file — so the reader sees the population instead of inferring it from a word. Counted and never judged: a probe with no long-option door is not a defect, and `advice.sh` prints `NO_FLAGS` for the same shape one directory over rather than breaking (#1207). What is still not done is feeding a bad POSITIONAL argument to those twenty-nine — a much larger question, because a positional sweep RUNS the probe instead of bouncing off its door, which is the cost `advice.sh` learned when its first door audit hung on `backlog.sh` (#1410). **The identity is asserted rather than argued since #1548**: `sources=` rides the census and `swept + no_door` must exhaust it, or the probe leaves with `DOOR_CENSUS_DOES_NOT_ADD_UP` and a red row. It held BY CONSTRUCTION for exactly one unit — two loop exits, so the sum could not fail — which is a property of control flow and not a check, and `advice.sh` had already written that sentence about its own two loop heads (#1443): *a fifth exit would have broken the sum with no verdict word, no counter, and a census line that simply stopped adding up.* A third `continue` here now fails the sweep; falsified by adding one, which prints `swept=27 no_door=28 sources=56` and exits 1. The classification is one total function returning one of two words, so the exits cannot drift apart from the counters. The counts also left the comments and live only on the census line, because a count in a comment is a count with no producer (#1082) and the one this row's own predecessor carried was wrong by two the day after it landed. |
| `GrammarTwins` | Does the recorder's field order still match the reader's field lists? | `ChronosLine` states the coupling as a virtue — *"the lists are the writer's, copied **deliberately**: when the recorder learns a field the reader learns it in the same breath, exactly as it learns a kind"* — and *in the same breath* is a hope about how somebody will edit two files. Six kinds are spelled out in `ChronosLog`'s print statements and spelled again in `ChronosLine.grammarOf`, and nothing compared them: this is #1053's finding surviving one layer down, since that unit removed the second READER and not the second COPY. **The three failure directions are not symmetric**, which is why the row exists: a writer that gains a field the reader has not learned makes every record of that kind refusable by `offGrammar` — at replay time, long after the world it recorded is gone, and #847 adding `rack` and `id` to `birth` is exactly that edit shape; a reader that gains a field the writer does not write fails quietly, per line, with a message about a value it cannot find rather than a grammar that is wrong; and a reordering is quietest of all, because the reader searches by needle and does not care, while `ChronosLog`'s own javadoc calls field order the grammar. **Both sides are read as text and that is forced, not chosen:** the writer's list is a format string with values interleaved and exists as data at no runtime, so the comparison is between two source files, which makes this a probe (contract clause 2) and not a test. Escaped quotes are unescaped first, so one key pattern reads both files; full-line comments are dropped before anything is matched, because `ChronosLog`'s javadoc names the fields it writes and a checker finding its own subject in prose is what bit `SheetFence` four times. A `{"chronos":` opening with no closing statement is left OUT of the writer's map rather than guessed at, so the kind surfaces as `READER_ONLY` instead of as a silent agreement. `kinds_written=`, `kinds_read=`, `fields_written=` and `fields_read=` ride `TWINS_CENSUS` (#1221) while `read_none=` rides the verdict, since a reading that found no kind on either side must not print the line a matching pair prints (#1207). `--writer` and `--reader` point the reading at scratch copies, which is how all four findings were seen — writer gains a field, reader reorders, reader gains a kind, both files empty — before this row was added. `--list` prints every kind's row and not only the broken ones: a run that names only the breakage cannot be read as a census, and nobody should have to break something to see one. What it does not do is merge the two declarations — a shared source has to be a third thing both derive from, which is a change to D-023's record grammar and not a keeper's business **`writer_only=`, `reader_only=` and `diverged=` are the three findings, one counter each**, so a red row names which direction broke rather than only that one did — and they are named here because `tools/counters.sh` (#1356) reported them unnamed on its first run after this probe landed, which is the mechanism catching the fourth row the day it was built for. `diverged=` is the one that matters: it counts a kind whose field SEQUENCE differs, so a reordering with an unchanged field set is a finding, which is the direction the reader itself cannot notice. |
| `SpecDrift` | Does the shelf's spec still describe the world it specifies? | the reader `docs/spec/README.md` named in the present tense before it existed (#1243), arriving with the shelf's first inhabitant — that page's rule is that a spec lands with its probe in one pull request, or it does not land. It compares the roster in `docs/spec/instrument-lines.md` against the families a private universe prints: `unpredicted=` is a family the run prints and the spec does not name, `unseen=` is a family the spec names and the run did not print, and `read_none=` keeps a spec that parsed no roster red rather than vacuously green. **The table IS the data** — the roster is parsed out of the spec's own markdown rather than restated here, because a second copy of a list is the defect this tree has found five times (#789, #880, #1162). **The second column is read, and that is the correctness**: two of the eight families are conditional — `BIRTH` needs a chronos recorder and a mind grown, `PERF` is emitted by the runner around the world rather than by the world — so a checker demanding all eight from one universe is red on a correct implementation, and one demanding only what it saw would bless a roster with a family missing. Its own first run found the second of those: the spec said seven families, and #603's done-when command could never have seen `BIRTH` because it passes no `--chronos`. The event log is not a family and the boundary is told rather than inferred — 695 event lines at seed 42 make a naive first-token scan report several hundred **The field tables since #604**: law 5 is additive-only, so field order is the contract and a conforming line's fields must BEGIN with the table's fields in the table's order — a prefix check rather than an equality one, because a later version's append is legal and a v1 parser ignores it. `field_drift=` counts positions where the two disagree, and only families the spec has written a table for are read: a family with no table is a node of #255 that has not landed, and demanding one would make this probe red for work nobody has done. `tables=` on the census says how many it found, so a table nobody put under a heading is visible rather than silently skipped. Falsified by renaming a field and by swapping two — one drift and two, on the positions that lied **One universe per invocation since #1442**, not one per field table. `fieldsOf` used to build a fresh 6,000-tick world for every family with a table and `families` built one more for the roster — filed at two tables, and by the time it was paid the spec carried SEVEN, so the probe was building EIGHT arcs on every push. That is this file's own header one level down: *not one expensive step, but a dozen cheap ones with nobody holding the sum*, with each table a reasonable addition and nobody weighing the total. The two readings are the same walk asked two questions, so `walk(ticks, seed)` returns family → the field names of that family's FIRST line and both callers read it; `fieldsIn(line, family)` comes out as a pure function of one line, so the quote-aware splitter is testable without a universe. **6.53 s → 1.66 s**, identical census and identical verdict, and still falsifiable — one field name changed in the spec's first table gives `SPEC field METRIC position 1 — the table says zzz_not_a_field and the line says tick`. **The sweep's `secs=` does NOT show the saving**, and that is stated rather than dressed up: five seconds inside a 130-second sweep is under the run-to-run variance, and the same tree measured 130, 132 and 136 in one afternoon. What shows it is the probe's own wall clock and `BENCH_COST`, where this row sat at 10 s in the morning and is no longer among the three fattest. **`--selftest` drives the splitter since #1508**, and it was the last unfalsified thing here: `fieldsIn` became a pure function of one line in #1442 and was still exercised only through a 6,000-tick arc — the one function in this probe that parses, that had already been wrong once, and whose own comment says its correctness *worked BY LUCK* because today's continuation tokens happen to contain no `=`. Seven cases (`cases=7 failed=0` on the suite's own judged row, so a case silently lost is a red sweep rather than a smaller green one — #898), the shipped function called rather than a copy of its expression, and **the fourth is the only one that fires**: with the quote-aware skip disabled, `equals-inside-the-quotes` reports `got=[tick,top,b,watched]` while `text-with-spaces-and-commas` — #606's own shape, `top="financial district:49,old town:31"` — stays green, because its continuation tokens contain no `=` and the `eq <= 0` guard swallows them for an unrelated reason. That is the luck, and only a value carrying an `=` inside its quotes shows it. Both failure directions are silent — a splitter that invents a field reports a drift that is not there, one that misses a field reports agreement it never checked — and since `field_drift` is judged in the sweep, the second is the one that hides. The seventh is the boundary the sixth sat one character away from (#1514): `fieldsIn` read `line.substring(family.length() + 1)` and threw on a line that IS its family, with no trailing space. `walk` never hands it one — it checks for the space first — so **the live path was safe and the pure function was not**, which is the half a fixture reaches and the whole point of extracting one (#1442). `no-fields-at-all` used `"PERF "`, with the trailing space that keeps the substring in range. |
| `SpeciesReach` | Does the world contain a species no list in the Bestiary names? | #974 split the catalog three ways and the difference is a digest question — `World.digestEntity` hashes whatever is in the world and cannot tell which list a row was written on. Runs the arc, collects every `EnvironmentProgram` species that ever appears, demands `reached ⊆ EVERY`. Sampled every tick, not at the end: a species that lives and dies inside the arc is a digest input for as long as it is there. `untouched=` counts rows the canonical arc never reaches — not a defect, a row no run of this seed has tested. `unlisted=` is what the row is judged on and the counter this description never named: it counts species the arc reached that no list in the Bestiary carries, which is the whole question — `untouched=` beside it is the harmless direction |
| `BoundsCensus` | Which constants in `Config` actually bind? | #1113's shape — a constant that LOOKS like a bound, is DOCUMENTED as a bound, and cannot cause anything to happen. Reads every `public static final` and classifies it by where its readers are: `binds` (a comparison, an arithmetic term), `reports` (only inside a log or a concatenation), `unread`. 81/0/0 today, which is the answer and not a failure to find one — what it locks is the FUTURE knob that arrives wired to nothing, and the rule is BoundsCensus's own: a knob that binds nothing says so in its comment. `silent=` is the judged counter and the whole question: a constant that LOOKS like a bound, is DOCUMENTED as a bound, and causes nothing to happen. `binds`, `reports` and `unread` beside it are the classification; `silent=` is the one that can be wrong |
| `OrderTable` | Is the root door's draw-order table still the order the door draws in? | the keeper #1013 gave a table that never had one (`ORDER_TABLE_HELD orders=6 classes=4 silent=1`). Judged in the sweep since #1175, which measured it at 4.9 s after four months as a self-declared one-off — its own javadoc called it a KEEPER, which is the shape of a thing judged on every push, and nobody had re-read the sentence. **It had no argument door until #1536**: `Long.parseLong` threw on a typo so a bad argument left with 1 — this family’s word for *the contract broke* — and a tick budget below one digest period reached `chain.get(-1)` and died on an `ArrayIndexOutOfBoundsException`, also 1. Both are `REFUSED` now, with a FATAL naming the field or the cadence; the cadence is read from `Config.DIGEST_EVERY_TICKS` rather than written as 100, so the refusal cannot drift from the thing it is about. The probe judging tick-order independence was reporting a typo the way it would report the world losing its determinism. **`ORDER_TABLE_TOO_WIDE` has never fired and cannot**: the roster is read off a booted root and it is 3, no argument moves it, so that refusal is unreachable from a command line — stated in the javadoc beside the constant rather than left to be found, since a guard nobody can trigger is a guard nobody can falsify, and its exit code was two units wrong before anyone noticed (#1533) |

A row per probe, less a known two. `CensusCensor` and `CensusReverdict` have no row
here and none in `bench.sh` either — #816 is the unit that gives them both.
`UnparkStorm` is the reverse and says so in its own row: a catalog entry with no sweep
row, on purpose.

This sentence said **three** until #1473, and the third name was the one state it
claims cannot exist: `CensusSampleSize` had gained a `judge` row and was being judged
on every push while its meaning was written down nowhere. A stale exemption is worse
than no exemption — it tells the next reader that the absence is known and accounted
for. `roster_check` could not see it, because it compares disk against catalog and
disk against bench and has no reader for a claim in prose; `tools/counters.sh` found
it as `no_row=1`, from the other direction, on its first run. The claim here is
decidable — *this named class has no bench row* is one grep — so it is a sentence that
could stop being a claim, and #1473 says so rather than pretending the fix is the
whole answer. Neither side is spelled out as a
number here, and that is #1342's rule rather than an omission: a figure whose
producer is the command printed below it does not want a marker, it wants
deleting. The pair that stood here — *thirty-four rows for thirty-seven probes* —
was true when typed, went to forty-eight and fifty-one while the difference it
existed to explain stayed exactly right, and so read as correct to anyone who
checked the part after the numbers. Nothing reads this table, so until #995's
`DocLint` comparison lands the gap is found by hand:

```sh
comm -23 <(grep -al 'static void main' probes/*.java | sed 's#probes/##;s#\.java##' | sort) \
         <(grep -oE '^\| `[A-Za-z]+` \|' probes/README.md | sed 's/^| `//;s/` |$//' | sort)
```

Add a probe when an investigation demands one; leave it here when the investigation
ends. The next skeptic starts from this bench, not from zero. (Tooling under D-030.)
